<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C语言多语句宏的妙用</title>
    <url>/2021/12/16/C%E8%AF%AD%E8%A8%80%E5%A4%9A%E8%AF%AD%E5%8F%A5%E5%AE%8F%E7%9A%84%E5%A6%99%E7%94%A8/</url>
    <content><![CDATA[<p>#define do{…}while(0)这种奇怪形式的宏定义经常在实际工程中应用，它的意义如下：</p>
<h3 id="1-增加代码的适应性"><a href="#1-增加代码的适应性" class="headerlink" title="1. 增加代码的适应性"></a><strong>1. 增加代码的适应性</strong></h3><p>下面的宏定义没有使用do{…}while(0)</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOO(x) foo(x); bar(x);</span></span><br></pre></td></tr></table></figure>

<p>这样宏定义，单独调用不会出现问题，例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">FOO(<span class="number">100</span>)</span><br></pre></td></tr></table></figure>

<p>宏扩展后变成：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">foo(x);bar(x);</span><br></pre></td></tr></table></figure>

<p>这样调用FOO没有任何问题，但是FOO（x）不能放入控制语句中，例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition) FOO(x); </span><br><span class="line"><span class="keyword">else</span> ...;</span><br></pre></td></tr></table></figure>

<p>经过宏扩展后，变成了</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition) </span><br><span class="line">    foo(x);</span><br><span class="line">    bar(x);</span><br><span class="line"><span class="keyword">else</span> ...;</span><br></pre></td></tr></table></figure>

<p>这样就导致了<strong>语法错误</strong>，语法错误并不可怕，在编译阶段就能发现，更致命的是他有可能导致<strong>逻辑错误，</strong>例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition)</span><br><span class="line">    FOO(x);</span><br></pre></td></tr></table></figure>

<p>这段代码经过扩展后变成：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition)</span><br><span class="line">    foo(x); bar(x);</span><br></pre></td></tr></table></figure>

<p>这样一来，无论condition是true还是false，bar(x)都会被调用。</p>
<p>这时候do{…}while(0)的价值就体现出来了，修改一下FOO的定义</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOO(x) do &#123;</span></span><br><span class="line">        foo(x); </span><br><span class="line">        bar(x); </span><br><span class="line">    &#125; <span class="keyword">while</span> (<span class="number">0</span>)</span><br></pre></td></tr></table></figure>

<p>这样FOO,放入控制语句中就没有问题了。</p>
<p>也许有人说：把foo(x);bar(x)用大括号括起来不就行了吗？比如这样定义：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOO(x)&#123;</span></span><br><span class="line">                foo(x); </span><br><span class="line">                bar(x); </span><br><span class="line">              &#125;</span><br></pre></td></tr></table></figure>

<p>再看下面代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition) </span><br><span class="line">    FOO(x); </span><br><span class="line"><span class="keyword">else</span> ...;</span><br></pre></td></tr></table></figure>

<p>扩展后：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition)</span><br><span class="line">&#123;</span><br><span class="line">    foo(x);</span><br><span class="line">    bar(x);</span><br><span class="line">&#125; ; <span class="comment">//注意最后这个分号，语法错误 else ...;</span></span><br></pre></td></tr></table></figure>

<p>照样语法错误；</p>
<h3 id="2-增加代码的扩展性"><a href="#2-增加代码的扩展性" class="headerlink" title="2.增加代码的扩展性"></a><strong>2.增加代码的扩展性</strong></h3><p>我理解的扩展性，主要是宏定义中还可以引用其他宏，比如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOO(x) do&#123;</span></span><br><span class="line">    OTHER_FOO(x)</span><br><span class="line">&#125; <span class="keyword">while</span>(<span class="number">0</span>)</span><br></pre></td></tr></table></figure>

<p>这样我们不用管OTHER_FOO是但语句还是符合语句，都不会出现问题</p>
<h3 id="3-增加代码的灵活性"><a href="#3-增加代码的灵活性" class="headerlink" title="3.增加代码的灵活性"></a><strong>3.增加代码的灵活性</strong></h3><p> 灵活性主要体现在，我们可以从宏中break出来，例如下面的定义：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOO(x)  do&#123; \</span></span><br><span class="line"><span class="meta">    foo(x);  \</span></span><br><span class="line"><span class="meta">    <span class="meta-keyword">if</span>(condition(x)) \</span></span><br><span class="line"><span class="meta">        break; \</span></span><br><span class="line"><span class="meta">    bar(x) \</span></span><br><span class="line"><span class="meta">    ..... \&#125; while(0)</span></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言面试基础</title>
    <url>/2021/12/16/C%E8%AF%AD%E8%A8%80%E9%9D%A2%E8%AF%95%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h3 id="include两种声明区别"><a href="#include两种声明区别" class="headerlink" title="#include两种声明区别"></a><strong>#include两种声明区别</strong></h3><p><strong>引用的头文件不同</strong><br>#include &lt; &gt; 引用的是编译器的类库路径里面的头文件。<br>#include“ ”引用的是你程序目录的相对路径中的头文件。<br><strong>用法不同</strong><br>#include &lt; &gt; 用来包含标准头文件(例如stdio.h或stdlib.h).<br>#include“ ”用来包含非标准头文件。<br><strong>调用文件的顺序不同</strong><br>#include &lt; &gt; 编译程序会先到标准函数库中调用文件。<br>#include“ ”编译程序会先从当前目录中调用文件。<br><strong>预处理程序的指示不同</strong><br>#include &lt; &gt; 指示预处理程序到预定义的缺省路径下寻找文件。<br>#include“ ”指示预处理程序先到当前目录下寻找文件，再到预定义的缺省路径下寻找文件。</p>
<h3 id="通用gcc编译过程"><a href="#通用gcc编译过程" class="headerlink" title="通用gcc编译过程"></a>通用gcc编译过程</h3><p>预处理 gcc/g++ -E hello.c/.cpp -o hello.i<br>编译 gcc/g++ -S hello.i -o hello.s<br>汇编 gcc/g++ -c hello.s -o hello.o<br>链接 gcc/g++ hello.o -o hello</p>
<h3 id="leetcode刷题sort函数第三个参数cmp必须声明为static的原因"><a href="#leetcode刷题sort函数第三个参数cmp必须声明为static的原因" class="headerlink" title="leetcode刷题sort函数第三个参数cmp必须声明为static的原因"></a>leetcode刷题sort函数第三个参数cmp必须声明为static的原因</h3><p>可以知道其实我们写参数cmp时，是把函数名作为实参传递给了sort函数，而sort函数内部是用一个函数指针去调用这个cmp函数的</p>
<p>我们知道class普通类成员函数cmp需要通过对象名.cmp()来调用，而sort()函数早就定义好了，那个时候哪知道你定义的是什么对象，所以内部是直接cmp()的，那你不加static时，去让sort()直接用cmp()当然会报错</p>
<p>static静态成员函数不用加对象名，就能直接访问函数（这也是静态成员函数的一大优点）所以加了static就不会报错</p>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>Git过滤规则</title>
    <url>/2021/12/03/Git%E8%BF%87%E6%BB%A4%E8%A7%84%E5%88%99/</url>
    <content><![CDATA[<p>一般我们总会有些文件无需纳入 Git 的管理，也不希望它们总出现在未跟踪文件列表。 通常都是些自动生成的文件，比如日志文件，或者编译过程中创建的临时文件等。</p>
<p>在这种情况下，我们可以创建一个名为.gitignore的文件，列出要忽略的文件的模式。</p>
<p>文件 .gitignore 的格式规范如下：</p>
<p>• 所有空行或者以 # 开头的行都会被 Git 忽略。</p>
<p>• 可以使用标准的 glob 模式匹配，它会递归地应用在整个工作区中。</p>
<p>• 匹配模式可以以（/）开头防止递归。</p>
<p>• 匹配模式可以以（/）结尾指定目录。</p>
<p>• 要忽略指定模式以外的文件或目录，可以在模式前加上叹号（!）取反。</p>
<p>所谓的 glob 模式是指 shell 所使用的简化了的正则表达式。 星号（*）匹配零个或多个任意字符；[abc] 匹配任何一个列在方括号中的字符 （这个例子要么匹配一个 a，要么匹配一个 b，要么匹配一个 c）；</p>
<p>问号（?）只匹配一个任意字符；如果在方括号中使用短划线分隔两个字符， 表示所有在这两个字符范围内的都可以匹配（比如 [0-9] 表示匹配所有 0 到 9 的数字）。</p>
<p>使用两个星号（<strong>）表示匹配任意中间目录，比如 a/</strong>/z 可以匹配 a/z 、 a/b/z 或 a/b/c/z 等。</p>
<p><strong>.gitignore文件使用示例如下：</strong></p>
<figure class="highlight jboss-cli"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 忽略所有的 .a 文件</span></span><br><span class="line">*<span class="string">.a</span></span><br><span class="line"><span class="comment"># 但跟踪所有的 lib.a，即便你在前面忽略了 .a 文件</span></span><br><span class="line">!lib.a</span><br><span class="line"><span class="comment"># 只忽略当前目录下的 TODO 文件，而不忽略 subdir/TODO</span></span><br><span class="line"><span class="string">/TODO</span></span><br><span class="line"><span class="comment"># 忽略任何目录下名为 build 的文件夹</span></span><br><span class="line">build/</span><br><span class="line"><span class="comment"># 忽略 doc/notes.txt，但不忽略 doc/server/arch.txt</span></span><br><span class="line">doc/*<span class="string">.txt</span></span><br><span class="line"><span class="comment"># 忽略 doc/ 目录及其所有子目录下的 .pdf 文件</span></span><br><span class="line">doc/**/*<span class="string">.pdf</span></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>I2C</title>
    <url>/2021/12/02/I2C/</url>
    <content><![CDATA[<h3 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a><strong>一、概述</strong></h3><p><strong>1、I2C总线只有两根双向信号线。一根是数据线SDA，另一根是时钟线SCL。</strong></p>
<p>SCL：上升沿将数据输入到每个EEPROM器件中；下降沿驱动EEPROM器件输出数据。（边沿触发）</p>
<p>SDA：双向数据线，为OD门，与其它任意数量的OD与OC门成\线与\关系。</p>
<p>I2C总线通过上拉电阻接正电源。当总线空闲时，两根线均为高电平（SDL=1;SCL=1）。连到总线上的任一器件输出的低电平，都将使总线的信号变低，即各器件的SDA及SCL都是线“与”关系。</p>
<p><strong>2、主设备与从设备</strong></p>
<p>系统中的所有外围器件都具有一个7位的从器件专用地址码，其中高4位为器件类型，由生产厂家制定，低3位为器件引脚定义地址，由使用者定义。</p>
<p>主控器件通过地址码建立多机通信的机制，因此I2C总线省去了外围器件的片选线，这样无论总线上挂接多少个器件，其系统仍然为简约的二线结构。</p>
<p>终端挂载在总线上，有主端和从端之分，主端必须是带有CPU的逻辑模块，在同一总线上同一时刻使能有一个主端，可以有多个从端，从端的数量受地址空间和总线的最大电容 400pF的限制。</p>
<p>主端主要用来驱动SCL line； 从设备对主设备产生响应；</p>
<p>二者都可以传输数据，但是从设备不能发起传输，且传输是受到主设备控制的。</p>
<h3 id="二、协议"><a href="#二、协议" class="headerlink" title="二、协议"></a><strong>二、协议</strong></h3><p><strong>1.空闲状态</strong></p>
<p>I2C总线总线的SDA和SCL两条信号线同时处于高电平时，规定为总线的空闲状态。</p>
<p>此时各个器件的输出级场效应管均处在截止状态，即释放总线，由两条信号线各自的上拉电阻把电平拉高。</p>
<p><strong>2.起始位与停止位的定义</strong></p>
<p>起始信号：当SCL为高期间，SDA由高到低的跳变；启动信号是一种电平跳变时序信号，而不是一个电平信号。</p>
<p>停止信号：当SCL为高期间，SDA由低到高的跳变；停止信号也是一种电平跳变时序信号，而不是一个电平信号。</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154508611-1283932793.png" alt="img"></p>
<p>起始和终止信号都是由主机发出的，在起始信号产生后，总线就处于被占用的状态；在终止信号产生后，总线就处于空闲状态。</p>
<p>接收器件收到一个完整的数据字节后，有可能需要完成一些其它工作，如处理内部中断服务等，可能无法立刻接收下一个字节，这时接收器件可以将SCL线拉成低电平，从而使主机处于等待状态。</p>
<p>直到接收器件准备好接收下一个字节时，再释放SCL线使之为高电平，从而使数据传送可以继续进行。</p>
<p><strong>3.ACK</strong></p>
<p>发送器每发送一个字节，就在时钟脉冲9期间释放数据线，由接收器反馈一个应答信号。</p>
<p>应答信号为低电平时，规定为有效应答位（ACK简称应答位），表示接收器已经成功地接收了该字节；应答信号为高电平时，规定为非应答位（NACK），一般表示接收器接收该字节没有成功。</p>
<p>对于反馈有效应答位ACK的要求是，接收器在第9个时钟脉冲之前的低电平期间将SDA线拉低，并且确保在该时钟的高电平期间为稳定的低电平。</p>
<p>如果接收器是主控器，则在它收到最后一个字节后，发送一个NACK信号，以通知被控发送器结束数据发送，并释放SDA线，以便主控接收器发送一个停止信号P。</p>
<p><strong>4.数据的有效性：</strong></p>
<p>I2C总线进行数据传送时，时钟信号为高电平期间，数据线上的数据必须保持稳定，只有在时钟线上的信号为低电平期间，数据线上的高电平或低电平状态才允许变化。</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154522863-1978594337.png" alt="img"></p>
<p><strong>5.数据的传送：</strong></p>
<p>在I2C总线上传送的每一位数据都有一个时钟脉冲相对应（或同步控制），即在SCL串行时钟的配合下，在SDA上逐位地串行传送每一位数据。数据位的传输是边沿触发。</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154533438-1174691264.png" alt="img"></p>
<h3 id="三、工作过程"><a href="#三、工作过程" class="headerlink" title="三、工作过程"></a><strong>三、工作过程</strong></h3><p>总线上的所有通信都是由主控器引发的。在一次通信中，主控器与被控器总是在扮演着两种不同的角色。</p>
<p><strong>1.主设备向从设备发送数据</strong></p>
<p>主设备发送起始位，这会通知总线上的所有设备传输开始了，接下来主机发送设备地址，与这一地址匹配的slave将继续这一传输过程，而其它slave将会忽略接下来的传输并等待下一次传输的开始。</p>
<p>主设备寻址到从设备后，发送它所要读取或写入的从设备的内部寄存器地址； 之后，发送数据。数据发送完毕后，发送停止位。</p>
<p>写入过程如下：</p>
<ol>
<li>发送起始位</li>
<li>发送从设备的地址和读/写选择位；释放总线，等到EEPROM拉低总线进行应答；如果EEPROM接收成功，则进行应答；若没有握手成功或者发送的数据错误时EEPROM不产生应答，此时要求重发或者终止。</li>
<li>发送想要写入的内部寄存器地址；EEPROM对其发出应答</li>
<li>发送数据</li>
<li>发送停止位</li>
</ol>
<p>EEPROM收到停止信号后，进入到一个内部的写入周期，大概需要10ms，此间任何操作都不会被EEPROM响应</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154545582-458821597.png" alt="img"></p>
<p><strong>2.主控器读取数据的过程</strong></p>
<p>读的过程比较复杂，在从slave读出数据前，你必须先要告诉它哪个内部寄存器是你想要读取的，因此必须先对其进行写入：</p>
<ol>
<li>发送起始位；</li>
<li>发送slave地址+write bit set；</li>
<li>发送内部寄存器地址；</li>
<li>重新发送起始位，即restart；</li>
<li>重新发送slave地址+read bit set；</li>
<li>读取数据</li>
<li>发送停止位</li>
</ol>
<p>主机接收器在接收到最后一个字节后，也不会发出ACK信号。于是，从机发送器释放 SDA线，以允许主机发出P信号结束传输。</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154556618-176659016.png" alt="img"></p>
<p><strong>3、数据传送格式</strong></p>
<p>每一个字节必须保证是8位长度。数据传送时，先传送最高位（MSB），每一个被传送的字节后面都必须跟随一位应答位（即一帧共有9位）。</p>
<p>由于某种原因从机不对主机寻址信号应答时（如从机正在进行实时性的处理工作而无法接收总线上的数据），它必须将数据线置于高电平，而由主机产生一个终止信号以结束总线的数据传送。</p>
<p>如果从机对主机进行了应答，但在数据传送一段时间后无法继续接收更多的数据时，从机可以通过对无法接收的第一个数据字节的“非应答”通知主机，主机则应发出终止信号以结束数据的继续传送。</p>
<p>当主机接收数据时，它收到最后一个数据字节后，必须向从机发出一个结束传送的信号。这个信号是由对从机的“非应答”来实现的。然后，从机释放SDA线，以允许主机产生终止信号。</p>
<p><strong>4、总线的寻址</strong></p>
<p>I2C总线协议有明确的规定：采用7位的寻址字节（寻址字节是起始信号后的第一个字节）。</p>
<p>（1）寻址字节的位定义</p>
<p>D7～D1位组成从机的地址。D0位是数据传送方向位，为“0”时表示主机向从机写数据，为“1”时表示主机由从机读数据。</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154609194-1022219369.png" alt="img"></p>
<p>主机发送地址时，总线上的每个从机都将这7位地址码与自己的地址进行比较，如果相同，则认为自己正被主机寻址，根据R/位将自己确定为发送器或接收器。</p>
<p>从机的地址由固定部分和可编程部分组成。在一个系统中可能希望接入多个相同的从机，从机地址中可编程部分决定了可接入总线该类器件的最大数目。</p>
<p>如一个从机的7位寻址位有4位是固定位，3位是可编程位，这时仅能寻址8个同样的器件，即可以有8个同样的器件接入到该I2C总线系统中。</p>
<p>（2）寻址字节中的特殊地址</p>
<p>固定地址编号0000和1111已被保留作为特殊用途。</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154620663-712911954.png" alt="img"></p>
<p>起始信号后的第一字节的8位为“0000 0000”时，称为通用呼叫地址。通用呼叫地址的用意在第二字节中加以说明。格式为：</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154631711-548685008.png" alt="img"></p>
<p>第二字节为 06H（0110）时，所有能响应通用呼叫地址的从机器件复位，并由硬件装入从机地址的可编程部分。能响应命令的从机器件复位时不拉低SDA和SCL线，以免堵塞总线。</p>
<p>第二字节为 04H（0100）时，所有能响应通用呼叫地址并通过硬件来定义其可编程地址的从机器件将锁定地址中的可编程位，但不进行复位。</p>
<p>如果第二字节的方向位B为“1”，则这两个字节命令称为硬件通用呼叫命令。 在这第二字节的高7位说明自己的地址。接在总线上的智能器件，如单片机或其他微处理器能识别这个地址，并与之传送数据。硬件主器件作为从机使用时，也用这个地址作为从机地址。格式为：</p>
<p>在系统中另一种选择可能是系统复位时硬件主机器件工作在从机接收器方式，这时由系统中的主机先告诉硬件主机器件数据应送往的从机器件地址，当硬件主机器件要发送数据时就可以直接向指定从机器件发送数据了。</p>
<p>（3）起始字节</p>
<p>起始字节是提供给没有I2C总线接口的单片机查询I2C总线时使用的特殊字节。</p>
<p>不具备I2C总线接口的单片机，则必须通过软件不断地检测总线，以便及时地响应总线的请求。</p>
<p>单片机的速度与硬件接口器件的速度就出现了较大的差别，为此，I2C总线上的数据传送要由一个较长的起始过程加以引导。</p>
<p>引导过程由起始信号、起始字节、应答位、重复起始信号（Sr）组成。</p>
<p>请求访问总线的主机发出起始信号后，发送起始字节（0000 0001），另一个单片机可以用一个比较低的速率采样SDA线，直到检测到起始字节中的7个“0”中的一个为止。</p>
<p>在检测到SDA线上的高电平后，单片机就可以用较高的采样速率，以便寻找作为同步信号使用的第二个起始信号Sr。</p>
<p>在起始信号后的应答时钟脉冲仅仅是为了和总线所使用的格式一致，并不要求器件在这个脉冲期间作应答。</p>
]]></content>
      <tags>
        <tag>通信协议</tag>
      </tags>
  </entry>
  <entry>
    <title>Makefile通用模版</title>
    <url>/2021/12/03/Makefile%E9%80%9A%E7%94%A8%E6%A8%A1%E7%89%88/</url>
    <content><![CDATA[<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># 编译一个或者多个.c（.cc）</span></span><br><span class="line"><span class="comment">#                            </span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line">CROSS_COMPILE := </span><br><span class="line"><span class="comment">#arm-linux-gnueabihf-</span></span><br><span class="line">CC = <span class="variable">$(CROSS_COMPILE)</span>gcc</span><br><span class="line">CPP = <span class="variable">$(CROSS_COMPILE)</span>g++</span><br><span class="line">LD = <span class="variable">$(CROSS_COMPILE)</span>ld</span><br><span class="line">AR = <span class="variable">$(CROSS_COMPILE)</span>ar</span><br><span class="line">RANLIB  = <span class="variable">$(CROSS_COMPILE)</span>ranlib</span><br><span class="line">STRIP   = <span class="variable">$(CROSS_COMPILE)</span>strip</span><br><span class="line"></span><br><span class="line"><span class="comment">##include</span></span><br><span class="line">INCFLAGS    += -I./<span class="keyword">include</span></span><br><span class="line">INCFLAGS    += -I./<span class="keyword">include</span>/librtmp</span><br><span class="line"></span><br><span class="line"><span class="comment">##lib</span></span><br><span class="line">LDFLAGS        += -L ./lib/ -lrtmp</span><br><span class="line">LDFLAGS        += -lm -lpthread -lz</span><br><span class="line"></span><br><span class="line">CFLAGS        += -Wall -O2 -Os</span><br><span class="line"></span><br><span class="line">SRC         += ./*.cc</span><br><span class="line">OBJ         = <span class="variable">$(<span class="built_in">patsubst</span> %.cc, %.o, $(<span class="built_in">wildcard</span> <span class="variable">$(SRC)</span>)</span>)</span><br><span class="line"><span class="comment">#SRC          = $(wildcard *.cc) </span></span><br><span class="line"><span class="comment">#OBJ          = $(SRC:%.cc=%.o)</span></span><br><span class="line"></span><br><span class="line">TARGET         = ./bin/rtmp_test</span><br><span class="line"></span><br><span class="line"><span class="comment">##make </span></span><br><span class="line"><span class="section">all: <span class="variable">$(TARGET)</span></span></span><br><span class="line"></span><br><span class="line"><span class="variable">$(TARGET)</span> : <span class="variable">$(OBJ)</span></span><br><span class="line">    <span class="variable">$(CPP)</span> <span class="variable">$(CFLAGS)</span> -o <span class="variable">$(TARGET)</span> <span class="variable">$(OBJ)</span> <span class="variable">$(LDFLAGS)</span>    </span><br><span class="line">    @rm ./*.o</span><br><span class="line">    </span><br><span class="line"><span class="variable">$(OBJ)</span>:%.o:%.cc</span><br><span class="line">    <span class="variable">$(CPP)</span> -c <span class="variable">$(INCFLAGS)</span> <span class="variable">$&lt;</span> -o <span class="variable">$@</span></span><br><span class="line"></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">    rm -f <span class="variable">$(TARGET)</span></span><br><span class="line"></span><br><span class="line"> </span><br></pre></td></tr></table></figure>

<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># c.cpp混合编译的makefile模板</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"></span><br><span class="line">BIN = foyerserver.exe</span><br><span class="line">CC = gcc</span><br><span class="line">CPP = g++</span><br><span class="line"><span class="comment">#这里只加入库头文件路径及库路径</span></span><br><span class="line">INCS = </span><br><span class="line">LIBS = </span><br><span class="line">SUBDIRS =</span><br><span class="line"><span class="comment">#生成依赖信息时的搜索目录，比如到下列目录中搜索一个依赖文件(比如.h文件)</span></span><br><span class="line">DEFINC = -I<span class="string">&quot;./../../base/&quot;</span> -I<span class="string">&quot;./../common&quot;</span> -I<span class="string">&quot;./../../lib/lxnet/&quot;</span> -I<span class="string">&quot;./../../lib/tinyxml/src/&quot;</span></span><br><span class="line"><span class="comment">#给INCS加上依赖搜索路径，分开写可能会产生不一致情况，而且繁琐</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#maintest.c tree/rbtree.c  多了子目录，那就直接添加 目录/*.c即可   所有的源文件--  .c文件列表</span></span><br><span class="line">CSRCS = <span class="variable">$(<span class="built_in">wildcard</span>  ./*.c ./../../base/log.c ./../../base/corsslib.c ./../../base/idmgr.c ./../../base/pool.c)</span></span><br><span class="line">CPPSRCS = <span class="variable">$(<span class="built_in">wildcard</span> ./*.cpp ./../common/backcommand.cpp ./../common/connector.cpp)</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#所有的.o文件列表</span></span><br><span class="line">COBJS := $(CSRCS:.c=.o)</span><br><span class="line">CPPOBJS := $(CPPSRCS:.cpp=.o)</span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#生成依赖信息 -MM是只生成自己的头文件信息，-M 包含了标准库头文件信息。</span></span><br><span class="line"><span class="comment">#-MT 或 -MQ都可以改变生成的依赖  xxx.o:src/xxx.h 为 src/xxx.o:src/xxx.h 当然。前面的 src/xxx.o需自己指定</span></span><br><span class="line"><span class="comment">#格式为 -MM 输入.c或.cpp  查找依赖路径  -MT或-MQ  生成规则，比如src/xxx.o </span></span><br><span class="line">MAKEDEPEND = gcc -MM -MT</span><br><span class="line">CFLAGS =</span><br><span class="line"><span class="comment">#CFLAGS += -Wall -ansi -DWIN32 -DNDEBUG -O2</span></span><br><span class="line">CPPFLAGS =</span><br><span class="line"><span class="comment">#CPPFLAGS += -Wall -DWIN32 -DNDEBUG -O2</span></span><br><span class="line"><span class="comment">#-g 生成调试信息</span></span><br><span class="line"><span class="comment">#-pedantic参数与-ansi一起使用 会自动拒绝编译非ANSI程序</span></span><br><span class="line"><span class="comment">#-fomit-frame-pointer 去除函数框架</span></span><br><span class="line"><span class="comment">#-Wmissing-prototypes -Wstrict-prototypes 检查函数原型</span></span><br><span class="line"><span class="comment">#针对每个.c文件的.d依赖文件列表</span></span><br><span class="line">CDEF = $(CSRCS:.c=.d)</span><br><span class="line">CPPDEF = $(CPPSRCS:.cpp=.d)</span><br><span class="line">PLATS = win32-debug win32-release linux-debug linux-release</span><br><span class="line"><span class="section">none:</span></span><br><span class="line">    @echo <span class="string">&quot;Please choose a platform:&quot;</span></span><br><span class="line">    @echo <span class="string">&quot; <span class="variable">$(PLATS)</span>&quot;</span></span><br><span class="line"><span class="section">win32-debug:</span></span><br><span class="line">    <span class="variable">$(MAKE)</span> all INCS=-I<span class="string">&quot;c:/mingw/include&quot;</span> LIBS=<span class="string">&quot;-L&quot;</span>c:/mingw/lib<span class="string">&quot; -L&quot;</span>./../../lib/lxnet<span class="string">&quot; -llxnet -lws2_32 -L&quot;</span>./../../lib/tinyxml<span class="string">&quot; -ltinyxml&quot;</span> CFLAGS=<span class="string">&quot;-Wall -DWIN32 -DDEBUG -g&quot;</span> CPPFLAGS=<span class="string">&quot;-Wall -DWIN32 -DDEBUG -g&quot;</span></span><br><span class="line"><span class="section">win32-release:</span></span><br><span class="line">    <span class="variable">$(MAKE)</span> all INCS=-I<span class="string">&quot;c:/mingw/include&quot;</span> LIBS=<span class="string">&quot;-L&quot;</span>c:/mingw/lib<span class="string">&quot; -L&quot;</span>./../../lib/lxnet<span class="string">&quot; -llxnet -lws2_32 -L&quot;</span>./../../lib/tinyxml<span class="string">&quot; -ltinyxml&quot;</span> CFLAGS=<span class="string">&quot;-Wall -DWIN32 -DNDEBUG -O2&quot;</span> CPPFLAGS=<span class="string">&quot;-Wall -DWIN32 -DNDEBUG -O2&quot;</span></span><br><span class="line"><span class="section">linux-debug:</span></span><br><span class="line">    <span class="variable">$(MAKE)</span> all INCS=-I<span class="string">&quot;/usr/include&quot;</span> LIBS=<span class="string">&quot;-L&quot;</span>/usr/lib<span class="string">&quot; -L&quot;</span>./../../lib/lxnet<span class="string">&quot; -llxnet -lpthread -L&quot;</span>./../../lib/tinyxml<span class="string">&quot; -ltinyxml&quot;</span> CFLAGS=<span class="string">&quot;-Wall -DDEBUG -g&quot;</span> CPPFLAGS=<span class="string">&quot;-Wall -DDEBUG -g&quot;</span></span><br><span class="line"><span class="section">linux-release:</span></span><br><span class="line">    <span class="variable">$(MAKE)</span> all INCS=-I<span class="string">&quot;/usr/include&quot;</span> LIBS=<span class="string">&quot;-L&quot;</span>/usr/lib<span class="string">&quot; -L&quot;</span>./../../lib/lxnet<span class="string">&quot; -llxnet -lpthread -L&quot;</span>./../../lib/tinyxml<span class="string">&quot; -ltinyxml&quot;</span> CFLAGS=<span class="string">&quot;-Wall -DNDEBUG -O2&quot;</span> CPPFLAGS=<span class="string">&quot;-Wall -DNDEBUG -O2&quot;</span></span><br><span class="line"><span class="section">all:<span class="variable">$(BIN)</span></span></span><br><span class="line"><span class="comment">#生成.o的对自己目录中.h .c的依赖信息.d文件到.c所在的路径中</span></span><br><span class="line"><span class="comment">#$(DEF)文件是.d文件名列表(含目录)，比如tree.d 匹配成功那么%就是tree，然后在尝试%.c，如果成功。则执行规则</span></span><br><span class="line"><span class="comment"># $(&lt;:.c=.o)是获取此.c文件的名字(含路径)，然后变为.o比如 src/xxx.o。 以形成如下</span></span><br><span class="line"><span class="comment"># src/xxx.o : src/xxx.c ***.h  ***.h  最前面！！注意。  </span></span><br><span class="line"><span class="comment"># 此做法是每个.d都和生成他的.c在一个目录里，所以需要这样做。</span></span><br><span class="line"><span class="comment"># $(&lt;:.c=.o)之类的 。此时的&lt;相当于变量$&lt; 。切记</span></span><br><span class="line"><span class="comment"># : : :  含义同下</span></span><br><span class="line"><span class="variable">$(CDEF)</span> : %.d : %.c</span><br><span class="line">    <span class="variable">$(MAKEDEPEND)</span> $(&lt;:.c=.o) <span class="variable">$&lt;</span> <span class="variable">$(DEFINC)</span> &gt; <span class="variable">$@</span></span><br><span class="line"><span class="variable">$(CPPDEF)</span> : %.d : %.cpp</span><br><span class="line">    <span class="variable">$(MAKEDEPEND)</span> $(&lt;:.cpp=.o) <span class="variable">$&lt;</span> <span class="variable">$(DEFINC)</span> &gt; <span class="variable">$@</span></span><br><span class="line"><span class="comment">#先删除依赖信息</span></span><br><span class="line"><span class="comment">#重新生成依赖信息</span></span><br><span class="line"><span class="comment">#这里出现了一个 $(MAKE) 没有定义的变量。这个变量是由 Make 自己定义的，它的值即为自己的位置，方便 Make 递归调用自己。</span></span><br><span class="line"><span class="section">depend:</span></span><br><span class="line">    -rm <span class="variable">$(CDEF)</span></span><br><span class="line">    -rm <span class="variable">$(CPPDEF)</span></span><br><span class="line">    <span class="variable">$(MAKE)</span> <span class="variable">$(CDEF)</span></span><br><span class="line">    <span class="variable">$(MAKE)</span> <span class="variable">$(CPPDEF)</span></span><br><span class="line"><span class="comment">#$(OBJS):%.o :%.c  先用$(OBJS)中的一项，比如foo.o: %.o : %.c  含义为:试着用%.o匹配foo.o。如果成功%就等于foo。如果不成功，</span></span><br><span class="line"><span class="comment"># Make就会警告，然后。给foo.o添加依赖文件foo.c(用foo替换了%.c里的%)</span></span><br><span class="line"><span class="comment"># 也可以不要下面的这个生成规则，因为下面的 include $(DEF)  就隐含了。此处为了明了，易懂。故留着</span></span><br><span class="line"><span class="variable">$(COBJS)</span> : %.o: %.c</span><br><span class="line">    <span class="variable">$(CC)</span> -c <span class="variable">$&lt;</span> -o <span class="variable">$@</span> <span class="variable">$(INCS)</span> <span class="variable">$(DEFINC)</span> <span class="variable">$(CFLAGS)</span></span><br><span class="line"><span class="variable">$(CPPOBJS)</span> : %.o: %.cpp</span><br><span class="line">    <span class="variable">$(CPP)</span> -c <span class="variable">$&lt;</span> -o <span class="variable">$@</span> <span class="variable">$(INCS)</span> <span class="variable">$(DEFINC)</span> <span class="variable">$(CPPFLAGS)</span></span><br><span class="line"><span class="comment"># $@--目标文件，$^--所有的依赖文件，$&lt;--第一个依赖文件。每次$&lt; $@ 代表的值就是列表中的</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="variable">$(BIN)</span> : <span class="variable">$(COBJS)</span> <span class="variable">$(CPPOBJS)</span></span><br><span class="line">    <span class="variable">$(CPP)</span> -o <span class="variable">$(BIN)</span> <span class="variable">$(COBJS)</span> <span class="variable">$(CPPOBJS)</span> <span class="variable">$(LIBS)</span></span><br><span class="line">    -rm <span class="variable">$(COBJS)</span> <span class="variable">$(CPPOBJS)</span></span><br><span class="line"><span class="comment"># 链接为最终目标</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#引入了.o文件对.c和.h的依赖情况。以后.h被修改也会重新生成，可看看.d文件内容即知道为何</span></span><br><span class="line"><span class="comment">#引入了依赖就相当于引入了一系列的规则，因为依赖内容例如： 目录/xxx.o:目录/xxx.c 目录/xxx.h 也相当于隐含的引入了生成规则</span></span><br><span class="line"><span class="comment">#故上面不能在出现如： $(OBJS) : $(DEF)之类。切记</span></span><br><span class="line"><span class="comment">#include $(CDEF) $(CPPDEF)</span></span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>:clean cleanall</span></span><br><span class="line"><span class="comment">#清除所有目标文件以及生成的最终目标文件</span></span><br><span class="line"><span class="section">clean:            </span></span><br><span class="line">    -rm <span class="variable">$(BIN)</span> <span class="variable">$(COBJS)</span> <span class="variable">$(CPPOBJS)</span></span><br><span class="line"><span class="comment">#rm *.d</span></span><br><span class="line"><span class="section">cleanall:</span></span><br><span class="line">    -rm <span class="variable">$(BIN)</span> <span class="variable">$(COBJS)</span> <span class="variable">$(CPPOBJS)</span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>Makefile</tag>
      </tags>
  </entry>
  <entry>
    <title>Makefile面试</title>
    <url>/2021/12/01/Makefile%E9%9D%A2%E8%AF%95/</url>
    <content><![CDATA[<p><em><strong>介绍一下make? 为什么使用make</strong></em></p>
<p>1、包含多个源文件的项目在编译时有长而复杂的命令行，可以通过makefile保存这些命令行来简化该工作<br>2、make可以减少重新编译所需要的时间，因为make可以识别出哪些文件是新修改的<br>3、make维护了当前项目中各文件的相关关系，从而可以在编译前检查是否可以找到所有的文件</p>
<p>makefile：一个文本形式的文件，其中包含一些规则告诉make编译哪些文件以及怎样编译这些文件，每条规则包含以下内容：<br>一个target，即最终创建的东西<br>一个和多个dependencies列表，通常是编译目标文件所需要的其他文件<br>需要执行的一系列commands，用于从指定的相关文件创建目标文件</p>
<p>make执行时按顺序查找名为GNUmakefile，makefile或者Makefile文件，通常，大多数人常用Makefile<br>Makefile规则：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">target: dependency dependency [..]    command    command    [..]</span><br><span class="line">注意：command前面必须是制表符</span><br></pre></td></tr></table></figure>

<p>例子：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">editor: editor.o screen.o keyboard.o</span><br><span class="line">    gcc -o editor editor.o screen.o keyboard.o</span><br><span class="line">editor.o : editor.c editor.h keyboard.h screen.h</span><br><span class="line">    gcc -c editor.c</span><br><span class="line">screen.o: screen.c screen.h</span><br><span class="line">    gcc -c screen.c</span><br><span class="line">keyboard.o : keyboard.c keyboard.h</span><br><span class="line">		gcc -c keyboard.c</span><br><span class="line">clean:</span><br><span class="line">		rm editor *.o</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>Makefile</tag>
      </tags>
  </entry>
  <entry>
    <title>UART</title>
    <url>/2021/12/02/UART/</url>
    <content><![CDATA[<h3 id="UART接口介绍"><a href="#UART接口介绍" class="headerlink" title="UART接口介绍"></a><strong>UART接口介绍</strong></h3><p>通用异步收发传输器，通常称作UART，是一种异步收发传输器，是电脑硬件的一部分。它将要传输的资料在串行通信与并行通信之间加以转换。作为把并行输入信号转成串行输出信号的芯片，UART通常被集成于其他通讯接口的连结上。</p>
<p>UART是一种通用串行数据总线，用于异步通信。该总线双向通信，可以实现全双工传输和接收。在嵌入式设计中，UART用于主机与辅助设备通信，如汽车音响与外接AP之间的通信，与PC机通信包括与监控调试器和其它器件，如EEPROM通信。</p>
<h3 id="UART通信协议"><a href="#UART通信协议" class="headerlink" title="UART通信协议"></a><strong>UART通信协议</strong></h3><p>UART用一条传输线将数据一位位地顺序传送，以字符为传输单位，通信中两个字符间的时间间隔多少是不固定的， 然而在同一个字符中的两个相邻位间的时间间隔是固定的。</p>
<p>数据传送速率用波特率来表示， 指单位时间内载波参数变化的次数， 或每秒钟传送的二进制位数，如每秒钟传送240个字符，而每个字符包含10位（1个起始位， 1个停止位， 8个数据位），这时的波特率为2400Bd。</p>
<p>传输时序如下图：</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211119154408032-135226384.png" alt="img"></p>
<p>在UART中，信号线上共有两种状态， 分别用逻辑1（高电平）和逻辑0（低电平）来区分</p>
<p>在空闲时， 数据线应该保持在逻辑高电平状态</p>
<p>其中各位的意义如下：</p>
<p>起始位（Start Bit）：先发出一个逻辑0信号， 表示传输字符的开始</p>
<p>数据位（Data Bits）：可以是5~8位逻辑0或1. 如ASCII码（7位）， 扩展BCD码（8位）小端传输</p>
<p>校验位（Parity Bit）：数据位加上这一位后， 使得1的位数应为偶数（偶校验）或奇数（奇校验）</p>
<p>停止位（Stop Bit）：它是一个字符数据的结束标志。 可以是1位、1.5位、2位的高电平</p>
<p>空闲位：处于逻辑1状态，表示当前线路上没有资料传送</p>
<h3 id="UART基本结构"><a href="#UART基本结构" class="headerlink" title="UART基本结构"></a><strong>UART基本结构</strong></h3><p>输出缓冲寄存器，它接收CPU从数据总线上送来的并行数据，并加以保存。</p>
<p>输出移位寄存器，它接收从输出缓冲器送来的并行数据，以发送时钟的速率把数据逐位移出，即将并行数据转换为串行数据输出。</p>
<p>输入移位寄存器，它以接收时钟的速率把出现在串行数据输入线上的数据逐位移入，当数据装满后，并行送往输入缓冲寄存器，即将串行数据转换成并行数据。</p>
<p>输入缓冲寄存器，它从输入移位寄存器中接收并行数据，然后由CPU取走。</p>
<p>控制寄存器，它接收CPU送来的控制字，由控制字的内容，决定通信时的传输方式以及数据格式等。例如采用异步方式还是同步方式，数据字符的位数，有无奇偶校验，是奇校验还是偶校验，停止位的位数等参数。</p>
<p>状态寄存器，状态寄存器中存放着接口的各种状态信息，例如输出缓冲区是否空，输入字符是否准备好等。在通信过程中，当符合某种状态时，接口中的状态检测逻辑将状态寄存器的相应位置“1”，以便让CPU查询。</p>
<h3 id="UART通信速率"><a href="#UART通信速率" class="headerlink" title="UART通信速率"></a><strong>UART通信速率</strong></h3><p>数据传输可以首先从最低有效位（LSB）开始。然而，有些UART允许灵活选择先发送最低有效位或最高有效位（MSB）。微控制器中的UART传送数据的 速度范围为每秒几百位到1.5Mb。</p>
<p>例如，嵌入在ElanSC520微控制器中的高速UART通信的速度可以高达1.152Mbps。UART波特率还受发送和接收线对距离（线长度）的影响。</p>
<p>目前，市场上有只支持异步通信和同时支持异步与同步通信的两种硬件可用于UART。前者就是UART名字本身的含义，在摩托罗拉微控制器中被称为串行通信 接口（SCI）。</p>
<p>Microchip微控制器中的通用同步异步收发器（USART）和在富士通微控制器中的UART是后者的两个典型例子。</p>
<h3 id="UART工作原理"><a href="#UART工作原理" class="headerlink" title="UART工作原理"></a><strong>UART工作原理</strong></h3><p>发送数据过程：空闲状态，线路处于高电位；当收到发送数据指令后，拉低线路一个数据位的时间T，接着数据按低位到高位依次发送，数据发送完毕后，接着发送奇偶校验位和停止位（停止位为高电位），一帧数据发送结束。</p>
<p>接收数据过程：空闲状态，线路处于高电位；当检测到线路的下降沿（线路电位由高电位变为低电位）时说明线路有数据传输，按照约定的波特率从低位到高位接收数据，数据接收完毕后，接着接收并比较奇偶校验位是否正确，如果正确则通知后续设备准备接收数据或存入缓存。</p>
<p>由于UART是异步传输，没有传输同步时钟。为了能保证数据传输的正确性，UART采用16倍数据波特率的时钟进行采样。每个数据有16个时钟采样，取中间的采样值，以保证采样不会滑码或误码。</p>
<p>一般UART一帧的数据位数为8，这样即使每个数据有一个时钟的误差，接收端也能正确地采样到数据。</p>
<p>UART的接收数据时序为：当检测到数据的下降沿时，表明线路上有数据进行传输，这时计数器CNT开始计数，当计数器为24=16+8时，采样的值为第0位数据。</p>
<p>当计数器的值为40时，采样的值为第1位数据，依此类推，进行后面6个数据的采样。如果需要进行奇偶校验，则当计数器的值为152时，采样的值即为奇偶位；当计数器的值为168时，采样的值为“1”表示停止位，一帧数据接收完成。</p>
]]></content>
      <tags>
        <tag>通信协议</tag>
      </tags>
  </entry>
  <entry>
    <title>VMware禁用虚拟机自动挂起</title>
    <url>/2021/12/27/VMware%E7%A6%81%E7%94%A8%E8%99%9A%E6%8B%9F%E6%9C%BA%E8%87%AA%E5%8A%A8%E6%8C%82%E8%B5%B7/</url>
    <content><![CDATA[<p>当MAC和虚拟机有文件共享时，VMware自动挂起特性导致一段时间不操作就需要重新连接共享文件服务器，很麻烦，解决方法如下（摘自官网）：</p>
<h3 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h3><p>本文提供了在 VMware Fusion 和 VMware Workstation 中禁用和启用虚拟机挂起功能的步骤。</p>
<h3 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h3><p><strong>要在 VMware Fusion 中禁用虚拟机的挂起功能：</strong></p>
<ol>
<li><p>关闭虚拟机。</p>
</li>
<li><p>找到虚拟机包。</p>
</li>
<li><p>按 Ctrl 并单击虚拟机包，然后单击<strong>显示包内容。</strong></p>
</li>
<li><p>打开.vmx文件以在文本编辑器中进行编辑。</p>
</li>
<li><p>将此行添加到.vmx文件：</p>
 <figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">suspend.disabled</span> = <span class="string">&quot;TRUE&quot;</span></span><br></pre></td></tr></table></figure></li>
<li><p>保存并关闭.vmx文件。</p>
</li>
<li><p>重新启动虚拟机。</p>
</li>
</ol>
<p><strong>要在 VMware Workstation 中禁用虚拟机的挂起功能：</strong></p>
<ol>
<li><p>关闭虚拟机。</p>
</li>
<li><p>找到虚拟机文件夹。</p>
</li>
<li><p>打开.vmx文件以在文本编辑器中进行编辑。</p>
</li>
<li><p>将此行添加到.vmx文件：</p>
 <figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line">sus pend.<span class="keyword">disabled </span>= <span class="string">&quot;TRUE&quot;</span></span><br></pre></td></tr></table></figure></li>
<li><p>保存并关闭.vmx文件。</p>
</li>
<li><p>重新启动虚拟机。</p>
</li>
</ol>
<p><strong>要启用挂起功能，请从.vmx文件中删除以下行：</strong></p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">suspend.disabled</span> = <span class="string">&quot;TRUE&quot;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>mac</tag>
      </tags>
  </entry>
  <entry>
    <title>Vim常用命令</title>
    <url>/2021/12/02/Vim%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p>vim 中有三种工作模式，分别是命令模式，输入模式，末行模式</p>
<p>刚进入编辑器是处于命令模式，切换到输入模式可以输入：i，I，o，O，a，C</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span>：在光标之后插入</span><br><span class="line"><span class="selector-tag">A</span>：在光标所在行末插入</span><br><span class="line"><span class="selector-tag">i</span>：在光标处插入</span><br><span class="line"><span class="selector-tag">I</span>：在光标所在行首插入</span><br><span class="line">o：跳转到光标下一行插入行</span><br><span class="line">O：跳转到光标上一行插入行</span><br><span class="line">C：清除光标所在位置后的所有内容进入输入模式</span><br></pre></td></tr></table></figure>

<p>由命令模式切换到末行模式按shift+：</p>
<p>按esc回退到命令模式<br>输入模式和末行模式无法直接切换</p>
<p>在命令行状态下的快捷操作：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">dd</span>：剪切光标所在的行（在前加数字表示剪切多少行***慎用）</span><br><span class="line">yy：复制光标所在的行（在前加数字表示复制多少行）</span><br><span class="line"><span class="selector-tag">p</span>：粘贴复制的行</span><br><span class="line">gg：光标切换到第一行</span><br><span class="line">G：光标切换到最后一行</span><br><span class="line">行号+G：光标切换到指定的行</span><br><span class="line">u 恢复最后一个指令之前的结果。</span><br><span class="line">Ctrl+r 可以恢复撤消前内容，按多次可恢复多次。</span><br></pre></td></tr></table></figure>

<p>视窗调整：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">Ctrl</span>+f 视窗往下卷一页。</span><br><span class="line"><span class="symbol">Ctrl</span>+<span class="keyword">b</span> 视窗往上卷一页。</span><br><span class="line"><span class="symbol">Ctrl</span>+d 视窗往下卷半页。</span><br><span class="line"><span class="symbol">Ctrl</span>+u 视窗往上卷半页。</span><br><span class="line"><span class="symbol">Ctrl</span>+e 视窗往下卷一行。</span><br><span class="line"><span class="symbol">Ctrl</span>+y 视窗往上卷一行。</span><br></pre></td></tr></table></figure>

<p>在命令行状态下的快捷操作：</p>
<figure class="highlight dos"><table><tr><td class="code"><pre><span class="line">Ctrl+w：删除一个字符串，按标点或单词分割</span><br><span class="line">Ctrl+u：删除到行首</span><br><span class="line">Ctrl+<span class="built_in">Shift</span>+c：复制所选的内容</span><br><span class="line">Ctrl+<span class="built_in">Shift</span>+v：粘贴所选的内容</span><br></pre></td></tr></table></figure>

<p>TIPS：在粘贴了一些代码之后，vim变得比较乱，只要执行gg=G就能搞定</p>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>c编译器预定义宏</title>
    <url>/2021/12/16/c%E7%BC%96%E8%AF%91%E5%99%A8%E9%A2%84%E5%AE%9A%E4%B9%89%E5%AE%8F/</url>
    <content><![CDATA[<p>为了方便处理一些有用的信息，预处理器定义了一些预处理标识符，也就是预定义宏。预定义宏的名称都是以&ldquo;__&rdquo;（两条下划线）开头和结尾的，如果宏名是由两个单词组成，那么中间以&ldquo;_&rdquo;（一条下划线）进行连接。并且，宏名称一般都由大写字符组成。<br />在日常项目编程中，预定义宏尤其对多目标平台代码的编写通常具有重大意义。通过预定义宏，程序员使用&ldquo;#ifdef&rdquo;与&ldquo;#endif&rdquo;等预处理指令，就可使平台相关代码只在适合于当前平台的代码上编译，从而在同一套代码中完成对多平台的支持。从这个意义上讲，平台信息相关的宏越丰富，代码的多平台支持越准确。<br />标准 C 语言提供的一些标准预定义宏如表 1 所示。</p>
<table><caption>表 1 常用的标准预定义宏</caption>
<tbody>
<tr>
<th>宏</th>
<th>描&nbsp;述</th>
</tr>
<tr>
<td>__DATE__</td>
<td>丐前源文件的编泽口期，用&nbsp;&ldquo;Mmm dd yyy&rdquo;形式的字符串常量表示</td>
</tr>
<tr>
<td>__FILE__</td>
<td>当前源文件的名称，用字符串常量表示</td>
</tr>
<tr>
<td>__LINE__</td>
<td>当前源义件中的行号，用十进制整数常量表示，它可以随#line指令改变</td>
</tr>
<tr>
<td>__TIME__</td>
<td>当前源文件的最新编译吋间，用&ldquo;hh:mm:ss&rdquo;形式的宁符串常量表示</td>
</tr>
<tr>
<td>__STDC__</td>
<td>如果今前编泽器符合ISO标准，那么该宏的值为1，否则未定义</td>
</tr>
<tr>
<td>__STDC_VERSION__</td>
<td>如果当前编译器符合C89，那么它被定义为199409L;如果符合C99，那么它被定义为199901L:在其他情况下，该宏为宋定义</td>
</tr>
<tr>
<td>__STDC_HOSTED__</td>
<td>(C99)如果当前是宿主系统，则该宏的值为1;如果当前是独立系统，则该宏的值为0</td>
</tr>
<tr>
<td>__STDC_IEC_559_</td>
<td>(C99)如果浮点数的实现符合IEC 60559标准时，则该宏的值为1，否则为未定义</td>
</tr>
<tr>
<td>__STDC_IEC_559_COMPLEX__</td>
<td>(C99)如果复数运算实现符合IEC60559标准时，则该宏的伉为1,否则为未定义</td>
</tr>
<tr>
<td>__STDC_ISO_10646__</td>
<td>(C99 )定义为长整型常量，yyyymmL表示wchai_t值遵循ISO 10646标准及其指定年月的修订补充，否则该宏为未定义</td>
</tr>
</tbody>
</table>
<p>除标准 C 语言提供的标准宏之外，各种编译器也都提供了自己的自定义预定义宏。可以通过表 2 所示的指令来查看不同编译器对预定义宏的支持情况。</p>
<table><caption>表 2 不同编译器的预定义宏查看指令</caption>
<tbody>
<tr>
<th>编译器</th>
<th>宏指令（c)</th>
<th>宏指令（<a href="http://c.biancheng.net/cplus/" target="_blank" rel="noopener">C++</a>)</th>
</tr>
<tr>
<td>Clang/LLVM</td>
<td>clang -dM -E -x c /dev/null</td>
<td>clang++ -dM -E -x C++ /dev/null</td>
</tr>
<tr>
<td>GNU&nbsp;<a href="http://c.biancheng.net/gcc/" target="_blank" rel="noopener">GCC</a>/G++</td>
<td>gcc -dM -E -x c /dev/null</td>
<td>g++ -dM -E -x&nbsp;C++&nbsp;/dev/null</td>
</tr>
<tr>
<td>Hewlett-Packard C/aC++</td>
<td>cc -dM -E -x c /clev/null</td>
<td>aCC -dM -E -x&nbsp;C++&nbsp;/dev/null</td>
</tr>
<tr>
<td>IBM XL C/C++</td>
<td>xlc -qshowmacros -E /dev/null</td>
<td>xlc++ -qshowmacros -E /dev/null</td>
</tr>
<tr>
<td>Intel ICC7ICPC</td>
<td>icc -dM -E -x c /dev/null</td>
<td>icpc -dM -E -x&nbsp;C++&nbsp;/dev/null</td>
</tr>
<tr>
<td>Oracle Solaris Studio</td>
<td>cc -xduinpmacros -E /dev/null</td>
<td>CC -xduinpmacros -E /clev/null</td>
</tr>
<tr>
<td>Portland Group PGCC/PGCPP</td>
<td>pgcc -dM -E</td>
<td>&nbsp;</td>
</tr>
</tbody>
</table>
<p>对于这些预定义宏的应用，基本上随处可见，下面举例介绍。<br />利用&ldquo;__DATE__&rdquo;和&ldquo;__TIME__&rdquo;宏可以用来确定程序编译的时间。如下面的示例代码所示：</p>
<div class="snippet-container">
<div class="sh_bright snippet-wrap">
<pre class="language-c"><code>int main (void)
&#123;
  printf("Copyright (c) Powered by www.develhome.com\n");
  printf("Compiled on %s at %s\n", __DATE__,__TIME__);
  return 0;
&#125;</code></pre>
</div>
</div>
<p>利用&ldquo;__STDC__&rdquo;与&ldquo;__STDC_VERSION__&rdquo;宏可以编写那些需要兼容标准 C 和非标准 C 编译器的程序，如下面的示例代码所示：</p>
<div class="snippet-container">
<div class="sh_bright snippet-wrap">
<pre class="language-c"><code>#ifdef __STDC__
/* Some version of standard C */
#if defined(__STDC__VERSION__)&amp;&amp;__STDC_VERSION__&gt;=199901L
/* C99 */
#elif defined(__STDC_VERSION__)&amp;&amp;__STDC_VERSION__&gt;=199409L
/* C89 and amendment 1 */
#else
/* C89 but not amendment 1*/
#endif
#else /* __STDC__not defined */
/*Not Standard C*/
#endif</code></pre>
</div>
</div>
<p>利用__FILE__、__LINE__与__FUNCTION__（或者__func__）预定义宏的组合，在调试程序的时候可以很简单地在程序运行期进行异常跟踪。如下面的示例代码所示：</p>
<div class="snippet-container">
<div class="sh_bright snippet-wrap">
<pre class="language-c"><code>#include &lt;stdio.h&gt;
#include &lt;sys/types.h&gt;
#include &lt;sys/stat.h&gt;
#include &lt;fcntl.h&gt;
#include &lt;unistd.h&gt;
#include &lt;stdlib.h&gt;
#define MESSAGE(message,assertion) \
do&#123;\
  if(!(assertion))&#123;\
    printf("line %d in %s(%s)", __LINE__, __FILE__,__FUNCTION__);\
    if(message)&#123;\
      printf(":%s",message);\
    &#125;\
    printf("\n");\
    abort();\
  &#125;\
&#125;while(0)
int OpenFile(const char *filename)
&#123;
  int fd;
  MESSAGE("文件名称不能够为空",filename);
  MESSAGE("文件不存在",0==access(filename,F_OK));
  fd = open(filename,O_RDONLY);
  close(fd);
  return 0;
&#125;
int main(int argc,char **argv)
&#123;
  MESSAGE("命令参数不能够为空",argc==2);
  OpenFile(argv[1]);
  return 0;
&#125;</code></pre>
</div>
</div>
<p>最后还需要注意的是，如果用户重定义&ldquo;#define&ldquo;或取消了&ldquo;#undef&rdquo;预定义宏，那么其结果是&ldquo;未定义&rdquo;的。因此，在代码编写中，应该尽量避免自定义宏与预定义宏名称相同的情况发生。</p>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>git同时拉取更新所有远程分支</title>
    <url>/2021/12/03/git%E5%90%8C%E6%97%B6%E6%8B%89%E5%8F%96%E6%9B%B4%E6%96%B0%E6%89%80%E6%9C%89%E8%BF%9C%E7%A8%8B%E5%88%86%E6%94%AF/</url>
    <content><![CDATA[<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull origin master/git pull</span><br></pre></td></tr></table></figure>

<p>只能更新当前所在分支</p>
<p>想要一次性更新仓库所有分支，可以</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull --all</span><br></pre></td></tr></table></figure>

<p>千万和</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull -a</span><br></pre></td></tr></table></figure>

<p>区分开</p>
]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>macos仅改变鼠标滚动方向同时不改变触控板使其符合windows使用习惯</title>
    <url>/2021/12/03/macos%E4%BB%85%E6%94%B9%E5%8F%98%E9%BC%A0%E6%A0%87%E6%BB%9A%E5%8A%A8%E6%96%B9%E5%90%91%E5%90%8C%E6%97%B6%E4%B8%8D%E6%94%B9%E5%8F%98%E8%A7%A6%E6%8E%A7%E6%9D%BF%E4%BD%BF%E5%85%B6%E7%AC%A6%E5%90%88windows%E4%BD%BF%E7%94%A8%E4%B9%A0%E6%83%AF/</url>
    <content><![CDATA[<p>在网址<a href="https://pilotmoon.com/scrollreverser/%E4%B8%8B%E8%BD%BD%E6%9C%80%E6%96%B0%E7%89%88%E6%9C%ACscroll">https://pilotmoon.com/scrollreverser/下载最新版本scroll</a> reverser</p>
<p>该文件免安装，下载后先将文件手动拖动到系统application文件夹中再打开运行</p>
<ol>
<li>授予两项系统权限</li>
<li>设置登录自启动</li>
<li>软件其他详细设置见下图：</li>
</ol>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211129102500448-1773687420.png" alt="img"></p>
<p>修改成功后即可在您的mac上获得同windows系统相同的鼠标与触控板使用体验</p>
]]></content>
      <tags>
        <tag>mac</tag>
      </tags>
  </entry>
  <entry>
    <title>macos新版本系统brew install报错问题</title>
    <url>/2021/11/30/macos%E6%96%B0%E7%89%88%E6%9C%AC%E7%B3%BB%E7%BB%9Fbrew-install%E6%8A%A5%E9%94%99%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Warning: Bottle missing, falling back to the default domain...</span><br></pre></td></tr></table></figure>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">tar: Error opening archive: Failed to open &#x27;/Users/zupernova/Library/Caches/Homebrew/downloads/551f73f417594bd810c1d61e1cacd24de3cc3e4cce825f0b9f763c0e0d32d84d--gdbm-1.20.big_sur.bottle.tar.gz&#x27;</span><br><span class="line">Error: Failure while executing; `tar --extract --no-same-owner --file /Users/zupernova/Library/Caches/Homebrew/downloads/551f73f417594bd810c1d61e1cacd24de3cc3e4cce825f0b9f763c0e0d32d84d--gdbm-1.20.big_sur.bottle.tar.gz --directory /private/tmp/d20210728-71473-bqhpdw` exited with 1. Here&#x27;s the output:</span><br><span class="line">tar: Error opening archive: Failed to open &#x27;/Users/zupernova/Library/Caches/Homebrew/downloads/551f73f417594bd810c1d61e1cacd24de3cc3e4cce825f0b9f763c0e0d32d84d--gdbm-1.20.big_sur.bottle.tar.gz&#x27;</span><br></pre></td></tr></table></figure>

<p><em><strong>解决方案</strong></em></p>
<p>更改环境变量，将HOMEBREW_BOTTLE_DOMAIN改正确即可</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">echo &#x27;export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles/bottles&#x27; &gt;&gt; ~/.zshrc</span><br><span class="line">source ~/.zshrc</span><br></pre></td></tr></table></figure>

<p>此前brew已经换源为中科大源</p>
]]></content>
      <tags>
        <tag>mac</tag>
      </tags>
  </entry>
  <entry>
    <title>new与malloc的10点区别</title>
    <url>/2021/12/16/new%E4%B8%8Emalloc%E7%9A%8410%E7%82%B9%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h3 id="1-申请的内存所在位置"><a href="#1-申请的内存所在位置" class="headerlink" title="1.申请的内存所在位置"></a><strong>1.申请的内存所在位置</strong></h3><p>new操作符从自由存储区（free store）上为对象动态分配内存空间，而malloc函数从堆上动态分配内存。自由存储区是C++基于new操作符的一个抽象概念，凡是通过new操作符进行内存申请，该内存即为自由存储区。而堆是操作系统中的术语，是操作系统所维护的一块特殊内存，用于程序的内存动态分配，C语言使用malloc从堆上分配内存，使用free释放已分配的对应内存。</p>
<p>那么自由存储区是否能够是堆（问题等价于new是否能在堆上动态分配内存），这取决于operator new 的实现细节。自由存储区不仅可以是堆，还可以是静态存储区，这都看operator new在哪里为对象分配内存。</p>
<p>特别的，new甚至可以不为对象分配内存！定位new的功能可以办到这一点：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> (place_address) type</span><br></pre></td></tr></table></figure>

<p>place_address为一个指针，代表一块内存的地址。当使用上面这种仅以一个地址调用new操作符时，new操作符调用特殊的operator new，也就是下面这个版本：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">new</span> <span class="params">(<span class="keyword">size_t</span>,<span class="keyword">void</span> *)</span> <span class="comment">//不允许重定义这个版本的operator new</span></span></span><br></pre></td></tr></table></figure>

<p>这个operator new不分配任何的内存，它只是简单地返回指针实参，然后右new表达式负责在place_address指定的地址进行对象的初始化工作。</p>
<h3 id="2-返回类型安全性"><a href="#2-返回类型安全性" class="headerlink" title="2.返回类型安全性"></a><strong>2.返回类型安全性</strong></h3><p>new操作符内存分配成功时，返回的是对象类型的指针，类型严格与对象匹配，无须进行类型转换，故new是符合类型安全性的操作符。而malloc内存分配成功则是返回void * ，需要通过强制类型转换将void*指针转换成我们需要的类型。<br>类型安全很大程度上可以等价于内存安全，类型安全的代码不会试图方法自己没被授权的内存区域。关于C++的类型安全性可说的又有很多了。</p>
<h3 id="3-内存分配失败时的返回值"><a href="#3-内存分配失败时的返回值" class="headerlink" title="3.内存分配失败时的返回值"></a><strong>3.内存分配失败时的返回值</strong></h3><p>new内存分配失败时，会抛出bac_alloc异常，它不会返回NULL；malloc分配内存失败时返回NULL。<br>在使用C语言时，我们习惯在malloc分配内存后判断分配是否成功：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *a  = (<span class="keyword">int</span> *)<span class="built_in">malloc</span> ( <span class="keyword">sizeof</span> (<span class="keyword">int</span> ));</span><br><span class="line"><span class="keyword">if</span>(<span class="literal">NULL</span> == a)&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从C语言走入C++阵营的新手可能会把这个习惯带入C++：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> * a = <span class="keyword">new</span> <span class="built_in"><span class="keyword">int</span></span>();</span><br><span class="line"><span class="keyword">if</span>(<span class="literal">NULL</span> == a)&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span>&#123;   </span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实际上这样做一点意义也没有，因为new根本不会返回NULL，而且程序能够执行到if语句已经说明内存分配成功了，如果失败早就抛异常了。正确的做法应该是使用异常机制：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>&#123;</span><br><span class="line">    <span class="keyword">int</span> *a = <span class="keyword">new</span> <span class="built_in"><span class="keyword">int</span></span>();</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in"><span class="keyword">catch</span></span> (bad_alloc)&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-是否需要指定内存大小"><a href="#4-是否需要指定内存大小" class="headerlink" title="4.是否需要指定内存大小"></a><strong>4.是否需要指定内存大小</strong></h3><p>使用new操作符申请内存分配时无须指定内存块的大小，编译器会根据类型信息自行计算，而malloc则需要显式地指出所需内存的尺寸。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span>&#123;</span>...&#125;</span><br><span class="line">A * ptr = <span class="keyword">new</span> A;</span><br><span class="line">A * ptr = (A *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(A)); <span class="comment">//需要显式指定所需内存大小sizeof(A);</span></span><br></pre></td></tr></table></figure>

<p>当然了，我这里使用malloc来为我们自定义类型分配内存是不怎么合适的，请看下一条。</p>
<h3 id="5-是否调用构造函数-析构函数"><a href="#5-是否调用构造函数-析构函数" class="headerlink" title="5.是否调用构造函数/析构函数"></a><strong>5.是否调用构造函数/析构函数</strong></h3><p>使用new操作符来分配对象内存时会经历三个步骤：</p>
<ul>
<li>第一步：调用operator new 函数（对于数组是operator new[]）分配一块足够大的，原始的，未命名的内存空间以便存储特定类型的对象。</li>
<li>第二步：编译器运行相应的构造函数以构造对象，并为其传入初值。</li>
<li>第三部：对象构造完成后，返回一个指向该对象的指针。</li>
</ul>
<p>使用delete操作符来释放对象内存时会经历两个步骤：</p>
<ul>
<li>第一步：调用对象的析构函数。</li>
<li>第二步：编译器调用operator delete(或operator delete[])函数释放内存空间。</li>
</ul>
<p>总之来说，new/delete会调用对象的构造函数/析构函数以完成对象的构造/析构。而malloc则不会。如果你不嫌啰嗦可以看下我的例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span>&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">A</span>() :<span class="built_in">a</span>(<span class="number">1</span>), <span class="built_in">b</span>(<span class="number">1.11</span>)&#123;&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    <span class="keyword">double</span> b;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    A * ptr = (A*)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(A));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在return处设置断点，观看ptr所指内存的内容：</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211118193603275-1751638106.png" alt="img"></p>
<p>可以看出A的默认构造函数并没有被调用，因为数据成员a,b的值并没有得到初始化，这也是上面我为什么说使用malloc/free来处理C++的自定义类型不合适，其实不止自定义类型，标准库中凡是需要构造/析构的类型通通不合适。</p>
<p>而使用new来分配对象时：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    A * ptr = <span class="keyword">new</span> A;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>查看程序生成的汇编代码可以发现，A的默认构造函数被调用了：</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211118193630647-1142183088.png" alt="img"></p>
<h3 id="6-对数组的处理"><a href="#6-对数组的处理" class="headerlink" title="6.对数组的处理"></a><strong>6.对数组的处理</strong></h3><p>C++提供了new[]与delete[]来专门处理数组类型:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">A * ptr = <span class="keyword">new</span> A[<span class="number">10</span>];<span class="comment">//分配10个A对象</span></span><br></pre></td></tr></table></figure>

<p>使用new[]分配的内存必须使用delete[]进行释放：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> [] ptr;</span><br></pre></td></tr></table></figure>

<p>new对数组的支持体现在它会分别调用构造函数函数初始化每一个数组元素，释放对象时为每个对象调用析构函数。注意delete[]要与new[]配套使用，不然会找出数组对象部分释放的现象，造成内存泄漏。</p>
<p>至于malloc，它并知道你在这块内存上要放的数组还是啥别的东西，反正它就给你一块原始的内存，在给你个内存的地址就完事。所以如果要动态分配一个数组的内存，还需要我们手动自定数组的大小：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> * ptr = (<span class="keyword">int</span> *) <span class="built_in">malloc</span>( <span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">int</span>) );<span class="comment">//分配一个10个int元素的数组</span></span><br></pre></td></tr></table></figure>

<h3 id="7-new与malloc是否可以相互调用"><a href="#7-new与malloc是否可以相互调用" class="headerlink" title="7.new与malloc是否可以相互调用"></a><strong>7.new与malloc是否可以相互调用</strong></h3><p>operator new /operator delete的实现可以基于malloc，而malloc的实现不可以去调用new。下面是编写operator new /operator delete 的一种简单方式，其他版本也与之类似：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">new</span> <span class="params">(<span class="keyword">sieze_t</span> size)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">void</span> * mem = <span class="built_in">malloc</span>(size)</span><br><span class="line">        <span class="keyword">return</span> mem;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">bad_alloc</span>();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="built_in"><span class="keyword">delete</span></span>(<span class="keyword">void</span> *mem) <span class="keyword">noexcept</span>&#123;</span><br><span class="line">    <span class="built_in">free</span>(mem);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="8-是否可以被重载"><a href="#8-是否可以被重载" class="headerlink" title="8.是否可以被重载"></a><strong>8.是否可以被重载</strong></h3><p>opeartor new /operator delete可以被重载。标准库是定义了operator new函数和operator delete函数的8个重载版本：</p>
<p>//这些版本可能抛出异常</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span>)</span></span>;</span><br><span class="line"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="keyword">new</span>[](<span class="keyword">size_t</span>);</span><br><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">delete</span> <span class="params">(<span class="keyword">void</span> * )</span><span class="keyword">noexcept</span></span>;</span><br><span class="line"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="keyword">delete</span>[](<span class="keyword">void</span> *<span class="number">0</span>）<span class="keyword">noexcept</span>;</span><br></pre></td></tr></table></figure>

<p>//这些版本承诺不抛出异常</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> ,<span class="keyword">nothrow_t</span>&amp;)</span> <span class="keyword">noexcept</span></span>;</span><br><span class="line"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="keyword">new</span>[](<span class="keyword">size_t</span>, <span class="keyword">nothrow_t</span>&amp; );</span><br><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">delete</span> <span class="params">(<span class="keyword">void</span> *,<span class="keyword">nothrow_t</span>&amp; )</span><span class="keyword">noexcept</span></span>;</span><br><span class="line"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="keyword">delete</span>[](<span class="keyword">void</span> *<span class="number">0</span>,<span class="keyword">nothrow_t</span>&amp; ）<span class="keyword">noexcept</span>;</span><br></pre></td></tr></table></figure>

<p>我们可以自定义上面函数版本中的任意一个，前提是自定义版本必须位于全局作用域或者类作用域中。太细节的东西不在这里讲述，总之，我们知道我们有足够的自由去重载operator new /operator delete ,以决定我们的new与delete如何为对象分配内存，如何回收对象。</p>
<p>而malloc/free并不允许重载。</p>
<h3 id="9-能够直观地重新分配内存"><a href="#9-能够直观地重新分配内存" class="headerlink" title="9.能够直观地重新分配内存"></a><strong>9.能够直观地重新分配内存</strong></h3><p>使用malloc分配的内存后，如果在使用过程中发现内存不足，可以使用realloc函数进行内存重新分配实现内存的扩充。realloc先判断当前的指针所指内存是否有足够的连续空间，如果有，原地扩大可分配的内存地址，并且返回原来的地址指针；如果空间不够，先按照新指定的大小分配空间，将原有数据从头到尾拷贝到新分配的内存区域，而后释放原来的内存区域。</p>
<p>new没有这样直观的配套设施来扩充内存。</p>
<h3 id="10-客户处理内存分配不足"><a href="#10-客户处理内存分配不足" class="headerlink" title="10.客户处理内存分配不足"></a><strong>10.客户处理内存分配不足</strong></h3><p>在operator new抛出异常以反映一个未获得满足的需求之前，它会先调用一个用户指定的错误处理函数，这就是new-handler。new_handler是一个指针类型：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> std&#123;</span><br><span class="line">    <span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*new_handler)</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>指向了一个没有参数没有返回值的函数,即为错误处理函数。为了指定错误处理函数，客户需要调用set_new_handler，这是一个声明于的一个标准库函数:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> std&#123;</span><br><span class="line">    <span class="function">new_handler <span class="title">set_new_handler</span><span class="params">(new_handler p )</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>set_new_handler的参数为new_handler指针，指向了operator new 无法分配足够内存时该调用的函数。其返回值也是个指针，指向set_new_handler被调用前正在执行（但马上就要发生替换）的那个new_handler函数。</p>
<p>对于malloc，客户并不能够去编程决定内存不足以分配时要干什么事，只能看着malloc返回NULL。</p>
<p><strong>总结</strong></p>
<p>将上面所述的10点差别整理成表格：</p>
<table>
<thead>
<tr>
<th><strong>特征</strong></th>
<th><strong>new/delete</strong></th>
<th><strong>malloc/free</strong></th>
</tr>
</thead>
<tbody><tr>
<td>分配内存的位置</td>
<td>自由存储区</td>
<td>堆</td>
</tr>
<tr>
<td>内存分配失败返回值</td>
<td>完整类型指针</td>
<td>void*</td>
</tr>
<tr>
<td>内存分配失败返回值</td>
<td>默认抛出异常</td>
<td>返回NULL</td>
</tr>
<tr>
<td>分配内存的大小</td>
<td>由编译器根据类型计算得出</td>
<td>必须显式指定字节数</td>
</tr>
<tr>
<td>处理数组</td>
<td>有处理数组的new版本new[]</td>
<td>需要用户计算数组的大小后进行内存分配</td>
</tr>
<tr>
<td>已分配内存的扩充</td>
<td>无法直观地处理</td>
<td>使用realloc简单完成</td>
</tr>
<tr>
<td>是否相互调用</td>
<td>可以，看具体的operator new/delete实现</td>
<td>不可调用new</td>
</tr>
<tr>
<td>分配内存时内存不足</td>
<td>客户能够指定处理函数或重新制定分配器</td>
<td>无法通过用户代码进行处理</td>
</tr>
<tr>
<td>函数重载</td>
<td>允许</td>
<td>不允许</td>
</tr>
<tr>
<td>构造函数与析构函数</td>
<td>调用</td>
<td>不调用</td>
</tr>
</tbody></table>
<p>malloc给你的就好像一块原始的土地，你要种什么需要自己在土地上来播种</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211118192846036-150187083.png" alt="img"></p>
<p>而new帮你划好了田地的分块（数组），帮你播了种（构造函数），还提供其他的设施给你使用:</p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211118192905654-391104471.png" alt="img"></p>
<p>当然，malloc并不是说比不上new，它们各自有适用的地方。在C++这种偏重OOP的语言，使用new/delete自然是更合适的。</p>
]]></content>
      <tags>
        <tag>C</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>nor flash启动与nand flash启动的区别</title>
    <url>/2021/12/01/nor-flash%E5%90%AF%E5%8A%A8%E4%B8%8Enand-flash%E5%90%AF%E5%8A%A8%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h3 id="接口区别"><a href="#接口区别" class="headerlink" title="接口区别"></a>接口区别</h3><p>NOR FLASH地址线和数据线分开，来了地址和控制信号，数据就出来。<br>NAND Flash地址线和数据线在一起，需要用程序来控制，才能出数据。<br>通俗的说，就是光给地址不行，要先命令，再给地址，才能读到NAND的数据。而且都是在一个总线完成的。<br>结论是：ARM无法从NAND直接启动。除非装载完程序，才能使用NAND Flash.</p>
<h3 id="性能区别"><a href="#性能区别" class="headerlink" title="性能区别"></a>性能区别</h3><p>NOR的传输效率很高，在1～4MB的小容量时具有很高的成本效益，但是很低的写入和擦除速度大大影响了它的性能。 NAND结构能提供极高的单元密度，可以达到高存储密度，并且写入和擦除的速度也很快。应用NAND的困难在于flash的管理和需要特殊的系统接口。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">a.NOR的读速度比NAND稍快一些。</span><br><span class="line">b.NAND的写入速度比NOR快很多。</span><br><span class="line">c.NAND的<span class="number">4</span>ms擦除速度远比NOR的<span class="number">5</span>s快。</span><br><span class="line">d.大多数写入操作需要先进行擦除操作。</span><br><span class="line">e.NAND的擦除单元更小，相应的擦除电路更少。</span><br></pre></td></tr></table></figure>

<h3 id="s3c2440启动过程详解"><a href="#s3c2440启动过程详解" class="headerlink" title="s3c2440启动过程详解"></a>s3c2440启动过程详解</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">a.地址空间的分配</span><br><span class="line">b.开发板上一般都用SDRAM做内存，flash（nor、nand）来当做ROM。其中nand flash没有地址线，一次至少要读一页(<span class="number">512B</span>).其他两个有地址线</span><br><span class="line">c.nandflash不用来运行代码，只用来存储代码，NORflash，SDRAM可以直接运行代码）</span><br><span class="line">d.s3c2440总共有<span class="number">8</span>个内存banks</span><br><span class="line">  <span class="number">6</span>个内存bank可以当作ROM或者SRAM来使用</span><br><span class="line">  留下的<span class="number">2</span>个bank除了当作ROM 或者SRAM，还可以用SDRAM(各种内存的读写方式不一样)</span><br><span class="line">  <span class="number">7</span>个bank的起始地址是固定的，还有一个灵活的bank的内存地址，并且bank大小也可以改变</span><br><span class="line">e.s3c2440支持两种启动模式：NAND和非NAND（这里是nor flash）。</span><br><span class="line">  具体采用的方式取决于OM0、OM1两个引脚</span><br><span class="line">  OM[<span class="number">1</span>:<span class="number">0</span>所决定的启动方式</span><br><span class="line">  OM[<span class="number">1</span>:<span class="number">0</span>]=<span class="number">00</span>时，处理器从NAND Flash启动</span><br><span class="line">  OM[<span class="number">1</span>:<span class="number">0</span>]=<span class="number">01</span>时，处理器从<span class="number">16</span>位宽度的ROM启动</span><br><span class="line">  OM[<span class="number">1</span>:<span class="number">0</span>]=<span class="number">10</span>时，处理器从<span class="number">32</span>位宽度的ROM启动。</span><br><span class="line">  OM[<span class="number">1</span>:<span class="number">0</span>]=<span class="number">11</span>时，处理器从Test Mode启动。</span><br></pre></td></tr></table></figure>

<p><em><strong>当从NAND启动时</strong></em><br>cpu会自动从NAND flash中读取前4KB的数据放置在片内SRAM里（s3c2440是soc），同时把这段片内SRAM映射到nGCS0片选的空间（即0x00000000）。cpu是从0x00000000开始执行，也就是NAND flash里的前4KB内容。因为NAND FLASH连地址线都没有，不能直接把NAND映射到0x00000000，只好使用片内SRAM做一个载体。通过这个载体把nandflash中大代码复制到RAM(一般是SDRAM)中去执行。</p>
<p><em><strong>当从非NAND flash启动时</strong></em><br>nor flash被映射到0x00000000地址（就是nGCS0，这里就不需要片内SRAM来辅助了，所以片内SRAM的起始地址还是0x40000000）. 然后cpu从0x00000000开始执行（也就是在Norfalsh中执行）。</p>
]]></content>
      <tags>
        <tag>嵌入式</tag>
      </tags>
  </entry>
  <entry>
    <title>static作用</title>
    <url>/2021/12/16/static%E4%BD%9C%E7%94%A8/</url>
    <content><![CDATA[<p>static修饰的函数叫做静态函数，静态函数有两种，根据其出现的地方来分类：</p>
<p>如果这个静态函数出现在类里，那么它是一个静态成员函数；</p>
<p>静态成员函数的作用在于：调用这个函数不会访问或者修改任何对象（非static）数据成员。</p>
<p>其实很好理解，类的静态成员(变量和方法)属于类本身，在类加载的时候就会分配内存，可以通过类名直接去访问；非静态成员（变量和方法）属于类的对象，所以只有在类的对象产生（创建类的实例）时才会分配内存，然后通过类的对象（实例）去访问。</p>
<p>如果它不是出现在类中，那么它是一个普通的全局的静态函数。</p>
<p>这样的static函数与普通函数的区别是：用static修饰的函数，限定在本源码文件中，不能被本源码文件以外的代码文件调用。而普通的函数，默认是extern的，也就是说它可以被其它代码文件调用。</p>
<p>在函数的返回类型前加上关键字static，函数就被定义成为静态函数。普通 函数的定义和声明默认情况下是extern的，但静态函数只是在声明他的文件当中可见，不能被其他文件所用。因此定义静态函数有以下好处：</p>
<p>（1） 其他文件中可以定义相同名字的函数，不会发生冲突。</p>
<p>（2） 静态函数不能被其他文件所用。</p>
<p><strong>关键字static的作用是什么？</strong></p>
<p>这个简单的问题很少有人能回答完全。在C语言中，关键字static有三个明显的作用：<br>（1） 在函数体，一个被声明为静态的变量在这一函数被调用过程中维持其值不变。<br>（2）在模块内（但在函数体外），一个被声明为静态的变量可以被模块内所用函数访问，但不能被模块外其它函数访问。它是一个本地的全局变量。<br>（3）在模块内，一个被声明为静态的函数只可被这一模块内的其它函数调用。那就是，这个函数被限制在声明它的模块的本地范围内使用。<br>大多数应试者能正确回答第一部分，一部分能正确回答第二部分，同是很少的人能懂得第三部分。这是一个应试者的严重的缺点，因为他显然不懂得本地化数据和代码范围的好处和重要性。</p>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>关于git add操作时总会上传mac系统隐藏文件.DS_store的解决方案</title>
    <url>/2021/12/03/%E5%85%B3%E4%BA%8Egit-add%E6%93%8D%E4%BD%9C%E6%97%B6%E6%80%BB%E4%BC%9A%E4%B8%8A%E4%BC%A0mac%E7%B3%BB%E7%BB%9F%E9%9A%90%E8%97%8F%E6%96%87%E4%BB%B6-DS-store%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<p>.DS_Store是Mac OS保存文件夹的自定义属性的隐藏文件，如文件的图标位置或背景色，相当于Windows的desktop.ini。</p>
<p>当git仓库中进行了多项文件修改时，传统的方法为了剔除大量的.DS_Store文件只能手动一项一项添加待加入的文件，很麻烦。</p>
<p>为了解决这一问题，有如下两种方案：</p>
<p><strong>方案一：禁止.DS_store生成</strong></p>
<p>打开 “终端” ，复制黏贴下面的命令，回车执行，重启Mac即可生效。</p>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line">defaults <span class="keyword">write</span> com.apple.desktopservices DSDontWriteNetworkStores -<span class="type">bool</span> <span class="keyword">TRUE</span></span><br></pre></td></tr></table></figure>

<p><strong>方案二：设置git过滤规则</strong></p>
<p>具体参考<a href="https://www.cnblogs.com/zupernova/p/15529106.html">Git过滤规则</a></p>
<p>在根目录下创建文件.gitignore，写入内容</p>
<figure class="highlight jboss-cli"><table><tr><td class="code"><pre><span class="line"><span class="string">.DS_Store</span></span><br><span class="line">*<span class="string">.code-workspace</span></span><br></pre></td></tr></table></figure>

<p>即可让git在执行add操作时屏蔽所有符合条件的文件，使其不出现在未追踪文件列表中</p>
]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>关于malloc和free链表节点遇到的问题</title>
    <url>/2021/12/03/%E5%85%B3%E4%BA%8Emalloc%E5%92%8Cfree%E9%93%BE%E8%A1%A8%E8%8A%82%E7%82%B9%E9%81%87%E5%88%B0%E7%9A%84%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>链表初始化代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">Node* <span class="title">list_init</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;</span><br><span class="line">    Node *head = (Node *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    Node *p1 = (Node *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    Node *p2 = (Node *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    Node *p3 = (Node *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    Node *p4 = (Node *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    Node *p5 = (Node *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    head-&gt;val = <span class="number">-1</span>;</span><br><span class="line">    p1-&gt;val = <span class="number">11</span>;</span><br><span class="line">    p2-&gt;val = <span class="number">5</span>;</span><br><span class="line">    p3-&gt;val = <span class="number">7</span>;</span><br><span class="line">    p4-&gt;val = <span class="number">10</span>;</span><br><span class="line">    p5-&gt;val = <span class="number">1</span>;</span><br><span class="line">    head-&gt;next = p1;</span><br><span class="line">    p1-&gt;next = p2;</span><br><span class="line">    p2-&gt;next = p3;</span><br><span class="line">    p3-&gt;next = p4;</span><br><span class="line">    p4-&gt;next = p5;</span><br><span class="line">    p5-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>链表删除代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">list_deinit</span><span class="params">(Node* head)</span></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(head)&#123;</span><br><span class="line">        Node* tmp = head;</span><br><span class="line">        head = head-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(tmp);</span><br><span class="line">        tmp = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>链表打印代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">list_print</span><span class="params">(Node* head)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> first = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (head)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(first)&#123;</span><br><span class="line">            first=<span class="number">0</span>;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;head-&gt;&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;(%d)-&gt;&quot;</span>, head-&gt;val);</span><br><span class="line">        &#125;       </span><br><span class="line">        head = head-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;NULL\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这时，在主函数中：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Node *head = list_init();</span><br><span class="line">list_deinit(head);</span><br><span class="line">list_print(head);</span><br></pre></td></tr></table></figure>

<p>运行程序会报段错误，疑问点在于：list_print函数中已经加入了head=NULL的特殊情况判断逻辑，但还是在deinit调用之后报错</p>
<p>经过在init和deinit函数中打印链表各节点地址发现，deinit函数中传入的只是head指针的一个副本；因此，在deinit函数中，head指针的副本以及链表其他节点已经全部释放空间并指向了NULL</p>
<p>但是在主函数中，head指针指向的结构体被释放了空间，但是并没有赋值为NULL，因此，在deinit之后再调用list_print,实际上是传入了一个野指针（此时的head），因此报了段错误。</p>
<p>修改后的程序代码源文件如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">list_deinit</span><span class="params">(Node **head)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (*head)</span><br><span class="line">    &#123;</span><br><span class="line">        Node** tmp = head;</span><br><span class="line">        *head = (*head)-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(*tmp);</span><br><span class="line">        *tmp = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="comment">//二级指针方法可以销毁head本身，并不是传入的副本</span></span><br><span class="line"></span><br><span class="line">list_deinit(&amp;head);<span class="comment">//采用此方法调用</span></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>同时批量编译同一文件夹里的.c文件为同名的目标文件</title>
    <url>/2021/12/03/%E5%90%8C%E6%97%B6%E6%89%B9%E9%87%8F%E7%BC%96%E8%AF%91%E5%90%8C%E4%B8%80%E6%96%87%E4%BB%B6%E5%A4%B9%E9%87%8C%E7%9A%84-c%E6%96%87%E4%BB%B6%E4%B8%BA%E5%90%8C%E5%90%8D%E7%9A%84%E7%9B%AE%E6%A0%87%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">SRC=<span class="variable">$(<span class="built_in">wildcard</span> *.c)</span> //同时将文件夹内所有.c编译为同名的目标文件</span><br><span class="line">OBJ=<span class="variable">$(<span class="built_in">patsubst</span> %.c,%,<span class="variable">$(SRC)</span>)</span></span><br><span class="line"><span class="section">%:%.c//此处使用了内建隐含规则</span></span><br><span class="line">    cc -c <span class="variable">$&lt;</span> -o <span class="variable">$@</span></span><br><span class="line"><span class="section">all:<span class="variable">$(OBJ)</span></span></span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>Makefile</tag>
      </tags>
  </entry>
  <entry>
    <title>嵌入c&amp;linux书籍介绍</title>
    <url>/2021/12/01/%E5%B5%8C%E5%85%A5c-linux%E4%B9%A6%E7%B1%8D%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<h3 id="一、入门系列"><a href="#一、入门系列" class="headerlink" title="一、入门系列"></a>一、入门系列</h3><p><strong>1.《C语言程序设计：现代方法》（第2版）</strong><br>推荐理由：时至今日， C语言仍然是计算机领域的通用语言之一，但今天的 C语言已经和最初的时候大不相同了。本书最主要的一个目的就是通过一种“现代方法”来介绍 C语言，书中强调标准 C，强调软件工程，不再强调“手工优化”。这一版中紧密结合了 C99标准，并与 C89标准进行对照，补充了 C99中的最新特性。本书分为 C语言的基础特性、 C语言的高级特性、 C语言标准库和参考资料 4个部分。每章末尾都有一个“问与答”小节给出一系列与该章内容相关的问题及答案，此外还包含适量的习题。<br>本书是为大学本科阶段的 C语言课程编写的教材，同时也非常适合作为其他课程的辅助用书。</p>
<p><strong>2.《C语言程序设计》（第2版）谭浩强版本</strong><br>这本书堪称经典之作初学者学习可以看看，这个就是零基础入门学习C语言的，上手快。但也要坚持上机，要是只看书，不在电脑上运行一下看看，是永远学不会的。关键在实践！坚持！<br>不过这本书被诟病的地方也不少，可以看完上面那本再看这本，很多东西就懂了。</p>
<p><strong>3.《程序员修炼之道》</strong><br>《程序员修炼之道》由一系列的独立的部分组成，涵盖的主题从个人责任、职业发展，直到用于使代码保持灵活、并且易于改编和复用的各种架构技术。利用许多富有娱乐性的奇闻轶事、有思想性的例子以及有趣的类比，全面阐释了软件开发的许多不同方面的最佳实践和重大陷阱。无论你是初学者，是有经验的程序员，还是软件项目经理，本书都适合你阅读。</p>
<p><strong>4.《C和指针》</strong><br>看到书名很让人担心翻译的英语水平。实际不然，翻译的很好。只能说标题党了。看封面不难理解作者用pointers的意思吧，再说了书又不是只讲指针。书名用《C语言指导》更好些，<br>这是一本全面的C语言入门书。当然入门的深度和高度都比国内的教材高太多了。所以，如果你能直接从这本书开始学的话，起点会比较高，当然能学懂的话，说明你很有才。<br>一般情况下，本书的部分内容更适合有C基础的人看。如ADT、递归、指针和数组的部分，书中所述的思想是国内教材所缺乏的。看完本书，能得到一个正确的C语言观。</p>
<p><strong>5.《C primer plus》（入门首选）</strong><br>C prime Plus这本书看了两遍，练习题基本上都自己独立做完了。题目没怎么主动算法能力（毕竟不是算法的书），但是每个细节说的很清楚。初学者很容易找到信心的。学完c primer plus之后可以来看上面谭大爷的书找错误。</p>
<p><strong>6.《高质量程序设计指南》（一定要看）</strong><br>大一上学期的时候，一个偶然的机会接触了本书的第一版，引发了对软件工程的思考，让我很早就意识到代码规范的重要性，为今后学习打下了坚实基础，真的很感激这本书，虽然其内容都很简单，但是在我迷茫的时候真的给了我很多启迪。</p>
<p><strong>7.《C/C++深层探索》</strong><br>很早读过的书，很不错，姚的另外一本c标准：标准和实现也非常好。原创佳作~~语言的扩充成为C++，我们知道C语言是一种程式语言，而C++则为对象化语言，因此C++比C更加接近人类的语言，因此第四代语言就是人类语言，这就是说人类也是按照程式来行动的，也是一种程式动物或者程式生物。人类根据一定的世界的部分而创造的语言本不与世界一致或者总一致，因此才有扩充，但是基本词汇只有这么多，因此没办法表述所有的事物，因此不得不将词汇表扩充至一切声响，这就是音乐的美丽，音乐就是现代语言的最终发展。可见音乐的重要性。</p>
<p><strong>8.《从问题到程序》（最佳高校教材）</strong><br>既适合初入门到的小子，也适合相见恨晚的匹夫.这里不得不赞一下老裘借鉴得好,而且里面又简略提到不少CS里面的概念:<br>讲单词计数的时候顺带介绍了有限状态机;程序设计语言里的副作用,前条件,后条件,短路求值.习题也是很不错的,高斯消元,3n+1问题,约瑟夫环,实现一个简单”虚拟机”和”汇编器”还要弄单步执行功能不少open problem.<br>风格严谨,十分强调程序的强健和安全,测试.越界访问从头到尾都在强调,后面还自己实现了个通用整数输入检查函数,还有通用错误信息处理函数,错误处理讨论得很详细.代码简练,命名规范.老早就讲了函数,”强调通过函数抽象建立清晰结构的重要性”.提供大量的模式,实例和建议,教会初学者设计、权衡.内容不依赖任何具体C实现,讨论IDE好处和坏处.<br>作为第一本C语言是很合适的.</p>
<h3 id="二、内核-驱动系列"><a href="#二、内核-驱动系列" class="headerlink" title="二、内核/驱动系列"></a>二、内核/驱动系列</h3><p><strong>1.《Linux C编程一站式学习》</strong><br>此书内容涵盖极广：C的基本语法，简单的数据结构，C与汇编的联系，计算机系统结构，操作系统，正则表达式，TCP/IP，无所不包。如此一来似乎样样通而样样不精。其实不是这么回事。作者内容穿插得非常好，用十分简单的方式把每个方面最重要的东西阐明了。所以，其实这是本入门书，当然也适合各个方面都了解之后总结用。看完这本书可能觉得什么都懂一点但什么都不完全会，不要紧，后面的参考文献多数都是经典。入门书嘛，但求上手快。这本书上手就非常快。里面几乎一事一例，不多不少，恰到好处。而且例子基本都简单小巧可爱，不会的地方复制代码调试即可。</p>
<p>“我本来就是菜鸟一个，怎么了？国内这破环境，真正的大家才不稀罕写书，都捞钱去了。”其实中文书水平普遍低下，主要就是缺少宋劲杉老师这样的“菜鸟”。本书适合做零基础的初学者学习C语言的第一本教材，帮助读者打下牢固的基础。有一定的编程经验但知识体系不够完整的读者也可以对照本书查缺补漏，从而更深入地理解程序的工作原理。本书最初是为北京亚嵌教育研究中心的嵌入式Linux系统工程师就业班课程量身定做的教材之一，也适合作为高等院校程序设计基础课程的教材。本书对于C语言的语法介绍得非常全面，对C99标准做了很多解读，因此也可以作为一本精简的C语言语法参考书。</p>
<p><strong>2.《Linux内核设计与实现》（第3版）</strong><br>这书估计慕名而来的人都会在第一时间略感失望，首先书很薄，而且讲解不求深入。如果一个人在第一次翻阅此书的时候有这样的印象，那应该好好反省下自己是否太浮躁了。<br>其实这部书的定位有点不高不低，但也正因如此，它是最适合过渡阶段的内核学习者阅读的一部书。正确的阅读顺序或许应是这样的：恐龙书or现代OS-&gt;LDK-&gt;情景分析之类的详解书。<br>LDK很适合在你系统地学习了OS理论之后，直接看代码详解又觉得暂且还不够功力的学习者，它可以带你由理论学习阶段逐渐过渡到实践阶段。对于这样一部书，要是太厚就有点骗钱的嫌疑，要是太深入又会让人觉得作者故意显摆自己的学识。LDK算得上是恰到好处。<br>另外，本书后面的参考文献十分值得一读，要是您读完本书之后觉得不错，建议把它推荐的参考文献也找来读一读，或许会让您有更惊艳的感受。</p>
<p><strong>3.《Linux设备驱动程序》（第3版）</strong><br>适合中低水平的人。Linux 设备驱动模型真心复杂！对于写Linux驱动的人来说, 这本书应该是教科书级别的吧, 必读.</p>
<p><strong>4.《深入Linux内核架构》</strong><br>觉得是linux内核的一大作，坊间关于《深入理解linux内核》的传说，本人用自己的拙学是这么理解的。对于可以有较好的英文阅读能力的人，可以不用看毛德操的老师的书，后者已经完全可以替代了。注意现在比较的逻辑，并没有拿这本书去调戏《深入理解》，毕竟本人认为本书阅读时间该是有操作系统概念，然后还没有深入代码研究的阶段。所以同样还在摸索的你我，不要被本书的页数给吓到了，这本书我每天晚上花了3个小时，差不多花了45天阅读完，建议一口气看完，不然就打不到效果了，当然如果你是在校学生，我建议花一个学期对着源码研究。现在这本书也已经被我成功推荐到我们的team了…</p>
<h3 id="三、应用系列"><a href="#三、应用系列" class="headerlink" title="三、应用系列"></a>三、应用系列</h3><p><strong>1.《UNIX环境高级编程》</strong><br>好书的妙处之一，就是能给你与作者交流的感觉。技术书籍常犯两个毛病，一个是着眼点太低，堆砌细节（比如谭浩强的《C程序设计》），读起来好像听和尚念经，无法交流。再一个就是着眼点太高，兜售哲学（比如ESR的《The Art of UNIX Programming》），读起来好像听于丹老师讲论语，不敢交流。此书的经典性就在于不高不低不多不少，把UNIX系统编程的来龙去脉向你娓娓道来。很多地方都可以让你感觉到，你的疑惑作者在写书的时候已经了如指掌。从疑惑到顿悟的那一瞬间的畅快感是学习最大的快乐。所以，我们的口号就是：有问题，找APUE。</p>
<p><strong>2.《UNIX网络编程》</strong><br>还是在大二就买了这本书，但一直没拿起来看，各种拖延。了解 linux 下的网络编程，这本很赞。其中讲到了较为底层的网络编程系统调用和几种网络通信模式，譬如阻塞式，非阻塞式，I/O 多路复用等。但离实践还是由于一定的距离，网络编程中重点不在于系统调用，而是对具体的项目想要设计与之适应的网络模式。W.Richard Stevens 爹爹的书，每本都可以是经典。荐！</p>
<h3 id="四、封神系列"><a href="#四、封神系列" class="headerlink" title="四、封神系列"></a>四、封神系列</h3><p><strong>1.《C陷阱与缺陷》</strong><br>这是一本小册子，有让人继续读下去的欲望，倒不是因为页数少好欺负，是因为书中所说的几乎所有需要注意的地方作为一个程序员都有可能遇到，作者叙述起来很有意思，丝毫没有说教的感觉，举的例子很简单却一针见血。<br>此书作为一本常备读物是非常合适的，没事经常翻翻加深印象。</p>
<p><strong>2.《C专家编程》</strong><br>一年前我翻了翻这本书就觉得很棒，但那是我并不“主修”C，也没好好看，最近在认真读这本书，真是赞叹不已。<br>它使你对C的使用有深入了解，最后还介绍了一些C++，如果你以前没太多接触过C++，只知道C，通过这本书打开通往C++之门也不错。书中还提到了一些当年那些传说中Hacker的的故事，挺风趣的。<br>但是看这本书还是要有些背景的。你要学过编译原理，虽然不需要学的太深太好，但至少对里面的一些概念要有所了解，否则对里面内存分配的部分（事实上很多是针对编译器的），你会感到吃力。还有，你要有些Unix/Linux的文化背景，比如Unix的C编程风格，还有Unix里的一些命令，工具。</p>
<p><strong>3.《C语言程序设计》K&amp;R版</strong><br>拿到这本薄薄的书，很多人开始怀疑，C语言是这么几百页能讲清楚的么。看完这本书，我想答案已经很明了，却真的让人感到震憾。什么是好书？无法删减的书才是真正的好书。K&amp;R的书一如C语言的设计理念：简单而高效<br>里面的习题建议都认真做一遍，而且是在linux下用vi来做，用makefile来编译，用shell脚本来进行测试，本来第八章的题就是和linux相关的<br>计算机的大学生们不应只会在WINDOWS下用VC来编程，而都应该在linux环境下进行程序设计，因为linux本身就是为开发者准备的操作系统</p>
<p><strong>4.《C语言解惑》</strong><br>本书脱胎于作者在C语言的摇篮——贝尔实验室教授C语言的讲稿，几乎涵盖了C语言各个方面的难点，并包含了一些其他书籍很少分析到的问题。在每个谜题后面都有详尽的解题分析，使读者能够清晰地把握C语言的构造与含义，学会处理许多常见的限制和陷阱，是一本绝佳的C语言练习册。</p>
<p><strong>5.《你必须知道的495个C语言问题》</strong><br>但比教材经典，最好手边一本教材，一边翻，一边看本书。建议集中时间看，然后再重新复习！很实用的书，比c语言陷阱，c语言解惑要深刻！！！广度还行，深度不足，适合查缺补漏。</p>
<p><strong>6.《C语言参考手册(原书第5版)》</strong><br>这是C99确定发布后出版的参考手册。相比K&amp;R要更加接近现在。K&amp;R适合入门，而这本书不读，恐怕不算”学过C语言“。</p>
<p><strong>7.《C语言接口与实现》</strong><br>另外，就我个人感觉而言，这本书的语言属于那种简单准确的风格，与原文的语义一致性很高，基本上没有因炫耀文字而牺牲准确性之处。新手当做兴趣书看或者老手老复习下也可以。可以加深对ADT的理解。</p>
<p><strong>8.《深入理解计算机系统》（修订版或第3版）</strong><br>这本书是引导你如何练内功的，但是要是我来说的话，我个人认为这本书是在你学完数据结构和导论之间去看，因为这本书只是让你去深入理解计算机导论里面的一些概念，算是高配版本的计算机导论，目的是为了引出来操作系统、组成原理这些专业核心的课程。你要是指望看完这本书你就能左手写个App右手写个Web动态网站的话就错了，这本书的意义正在于他的这个英文版的名字，Computer System — A Programmer’s Perspective，一个程序员的修养，如何利用计算机的工作流程去优化自己写的东西这个才是这本书的目的。</p>
<p><strong>9.《C语言的科学和艺术》</strong><br>本书的作者因为对本科生的教育做出了杰出的贡献而获得了Bing Award奖，而这本书也正体现了他作为一名教师，深入浅出的教学方法和易于理解又引人入胜的行文风格。<br>文中所用的例子也都非常符合本节所要讲述的内容，并且把不得以而用到的以后章节的知识以一种genlib库的形式封装了起来，隐藏了C的复杂性，从而避免了初学者的困惑。这样，在读完本书后，会发现，我们不仅仅学到了C的知识，而且把库的编写方法、习惯都潜移默化的留在了心中。在书中很多地方都会有作者关于软件工程和优秀程序设计风格的见解，如接口的编写等等，都对我们打下扎实基础起到了积极的作用。<br>特别需要指出的一点是：这本书对于C语言中比较困难的部分：如指针、C风格字符串、数组和指针的关系、数组和字符串的关系，都有“一针见血”式的透彻分析，使初学者能够容易的明白其中的知识，也使有经验的读者能够抓住重点理解更加深入。对于这些比较精髓的知识，特别是指针和数组名的区别，会在文中多次被提醒：分配内存、左值！<br>初学者在编程中，很少接触文件的操作，但是文件操作非常重要，无论初学者还是有一定经验的读者都应该对C标准库中的文件函数熟练的掌握，这本书对文件的介绍会让你有系统理解，而且对使用这些函数时常会犯的错误有先知一般的预见，从而避免了初学者遇到问题调试时的辛苦周折。<br>如果非要说说这本书的缺点，我想就是，没有把genlib库的代码刻成cd附在书里，这多少会给初学者上机调试造成了不便，好在网上有这本书中的源代码和其他资源，而且书后也有完整的代码。其实换种思路想，这也可以算是一件好事，国内学生的动手能力差，那就应该在敲代码的同时把她理解了吧，呵呵，有点自虐倾向-_-b<br>最后，无论如何，如果你想学习C语言，那么看看这本书吧，她很好的！（而且不必在乎什么“C语言已死”这样的胡说八道）作为一种应用最广的面向过程的语言，她会让你对计算机程序设计形成一种必要的经典的思考模式！</p>
<p><strong>10.《数据结构与算法分析C语言系列》</strong><br>因为最近需要复习数据结构与算法，所以网上搜索了下这方面的经典书籍。这本书的C语言版本高居榜首，获得一致好评，正好该书又有Java语言的版本，就买来拜读一下。前后大概花了1个月的时间将该书看了两遍，书中的主要数据结构都敲代码实现了一遍，现在算是将以前的数据结构课程都回忆了起来，对比当时上学用的谭浩强的那本数据结构教程，真是天壤之别。有时间的话可以在这本书的基础上看一下&lt;&lt;算法导论&gt;&gt;。</p>
<p>这本书确实是很好的数据结构与算法分析的最佳入门教程，不过看这本书还是要有点数据结构的基础。通过Java语言描述，讨论了主要的数据结构：表、栈、队列、树、散列、优先队列、不相交集合和图；同时讨论了经典的排序算法：插入排序、希尔排序、堆排序、归并排序、快速排序；介绍了5种常用算法：贪婪算法、分治算法、动态规划、随机化算法、回溯算法；并讨论了Java Collection中相关数据结构的实现：ArrayList、LinkedList、TreeSet、TreeMap、HashSet、HashMap、PriorityQueue。</p>
<p><strong>11.《Linux程序设计》</strong><br>《Linux程序设计》是我的Linux编程入门书籍，也是做为教材使用了一整个学期，在阅读和学习这本书的时候产生了很多的疑问，书里也没有对应的解答，直到……直到我看了APUE，带着这些问题去学习APUE，产生了巨大的能量。总之，推荐这本书，但是这本书也只是入门书籍，站在《Linux程序设计》的肩膀上，学习APUE，在Linux的世界里遨游吧！</p>
<p><strong>12.《现代编译原理》</strong><br>翻了这么多本书，这是我看过的唯一一本讲具体怎么构建一个编译器的书。同时这本书所构建的编译器就像作者说的那样，简单但是并不平庸，拥有很多挺先进的特性。也能算是一个优化编译器。<br>但是要跟着这本书做下来还是有一定难度的，需要扎实的C语言功底。</p>
<p><strong>13.《重构-改善既有代码的设计》</strong><br>大师Martin Fowler的经验之谈，看后有种醍醐灌顶、欲罢不能的感觉。重构也是当今敏捷开发一项不可或缺的技艺，建议所有有设计和项目开发经验的开发者都应读一下。</p>
<p><strong>14.《老码识途-从机器码到框架的系统观逆向修炼之路》</strong><br>我们《软件开发环境》老师写的书，先教你通过反汇编来分析、修改、自己写底层机器码，后面着重探讨面向对象特性在底层的实现和体现。<br>知识点都是底层的干货，对理解高层封装出来的一些概念的本质灰常有帮助。比如指针本质上就是个4字节的地址，指针类型只是由编译器识别，然后体现在控制访问多少个字节的CPU指令上；<br>比如函数是怎么实现调用、传参、返回的，传参又有寄存器传值、压栈传值、压栈传地址等方式，跨语言调用函数时调用惯例的协调。<br>总之弄懂了这些底层的机制，对高层语言的理解会透彻很多。<br>不过最好有一点汇编基础再读，否则略艰涩。<br>另一个特点是全书一直贯彻一种”猜测——实证”的思想，跟作者交流过这本书好几次，感觉这种思想是他最想传达的东西。</p>
<p><strong>15.《C语言进阶》</strong><br>这本书应该适用于学过C，但是想温习一下的人。里面有一部分基础语法，但是也有很多高级的东西。函数指针与指针函数，指针数组与数组指针，预定义，预编译，调试之类。但是感觉最后一章的常用算法有种多余的感觉。如果想应付面试，看这本书应该也没有错，里面有很多笔试喜欢考的sizeof的东西。</p>
<p><strong>16.《实用C语言编程》</strong></p>
<p>很老的一本C语言书,可以说是我的C语言启蒙书,里面的资料,尤其是附录是我现在还经常翻阅的原因,书写的很朴实,也如书名,确实实用,易懂.把这本书吃透了,找个工作,那是再容易不过了,所以说一本好书需要时间来检验它,在岁月中沉淀下来…岁月检验过的好书，不解释。</p>
]]></content>
      <tags>
        <tag>C</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>嵌入式经典面试题</title>
    <url>/2021/11/30/%E5%B5%8C%E5%85%A5%E5%BC%8F%E7%BB%8F%E5%85%B8%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    <content><![CDATA[<h3 id="预处理器"><a href="#预处理器" class="headerlink" title="预处理器"></a>预处理器</h3><p><em><strong>1.用预处理指令#define 声明一个常数，用以表明1年中有多少秒（忽略闰年问题）</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SECONDS_PER_YEAR (60UL * 60 * 24 * 365)</span></span><br></pre></td></tr></table></figure>

<p>我在这想看到几件事情：</p>
<p>1). #define 语法的基本知识（例如：不能以分号结束，括号的使用，等等）</p>
<p>2). 懂得预处理器将为你计算常数表达式的值，因此，直接写出你是如何计算一年中有多少秒而不是计算出实际的值，是更清晰而没有代价的。</p>
<p>3). 意识到这个表达式将使一个16位机的整型数溢出-因此要用到长整型符号L,告诉编译器这个常数是的长整型数。</p>
<p>4). 如果你在你的表达式中用到UL（表示无符号长整型），那么你有了一个好的起点。记住，第一印象很重要。</p>
<p><em><strong>2.写一个“标准”宏MIN，这个宏输入两个参数并返回较小的一个。</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN(A,B) ((A) &lt;= (B) ?(A) :(B))</span></span><br></pre></td></tr></table></figure>

<p>C/C++试题集 C/C++ Development这个测试是为下面的目的而设的：</p>
<p>1). 标识#define在宏中应用的基本知识。这是很重要的，因为直到嵌入(inline)操作符变为标准C的一部分，宏是方便产生嵌入代码的唯一方法，对于嵌入式系统来说，为了能达到要求的性能，嵌入代码经常是必须的方法。</p>
<p>2). 三重条件操作符的知识。这个操作符存在C语言中的原因是它使得编译器能产生比if-then-else更优化的代码，了解这个用法是很重要的。</p>
<p>3). 懂得在宏中小心地把参数用括号括起来</p>
<p>4). 我也用这个问题开始讨论宏的副作用，例如：当你写下面的代码时会发生什么事？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">least = MIN(*p++, b);</span><br></pre></td></tr></table></figure>

<p><em><strong>3.预处理器标识#error的目的是什么？</strong></em></p>
<p>如果你不知道答案，请看参考文献1。这问题对区分一个正常的伙计和一个书呆子是很有用的。只有书呆子才会读C语言课本的附录去找出象这种问题的答案。当然如果你不是在找一个书呆子，那么应试者最好希望自己不要知道答案。</p>
<h3 id="死循环"><a href="#死循环" class="headerlink" title="死循环"></a>死循环</h3><p><em><strong>4.嵌入式系统中经常要用到无限循环，你怎么样用C编写死循环呢？</strong></em></p>
<p>这个问题用几个解决方案。我首选的方案是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(<span class="number">1</span>) &#123; &#125;</span><br></pre></td></tr></table></figure>

<p>一些程序员更喜欢如下方案：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(;;) &#123; &#125;</span><br></pre></td></tr></table></figure>

<p>这个实现方式让我为难，因为这个语法没有确切表达到底怎么回事。如果一个应试者给出这个作为方案，我将用这个作为一个机会去探究他们这样做的基本原理。如果他们的基本答案是：“我被教着这样做，但从没有想到过为什么。”这会给我留下一个坏印象。</p>
<p>第三个方案是用 goto</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Loop:</span><br><span class="line">…</span><br><span class="line"><span class="keyword">goto</span> Loop;</span><br></pre></td></tr></table></figure>

<p>应试者如给出上面的方案，这说明或者他是一个汇编语言程序员（这也许是好事）或者他是一个想进入新领域的BASIC/FORTRAN程序员。</p>
<h3 id="数据声明"><a href="#数据声明" class="headerlink" title="数据声明"></a>数据声明</h3><p><em><strong>5.用变量a给出下面的定义</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">a) 一个整型数（An integer）</span><br><span class="line">b) 一个指向整型数的指针（A pointer to an integer）</span><br><span class="line">c) 一个指向指针的的指针，它指向的指针是指向一个整型数（A pointer to a pointer to an integer）</span><br><span class="line">d) 一个有<span class="number">10</span>个整型数的数组（An <span class="built_in">array</span> of <span class="number">10</span> integers）</span><br><span class="line">e) 一个有<span class="number">10</span>个指针的数组，该指针是指向一个整型数的（An <span class="built_in">array</span> of <span class="number">10</span> pointers to integers）</span><br><span class="line">f) 一个指向有<span class="number">10</span>个整型数数组的指针（A pointer to an <span class="built_in">array</span> of <span class="number">10</span> integers）</span><br><span class="line">g) 一个指向函数的指针，该函数有一个整型参数并返回一个整型数（A pointer to a function that takes an integer as an argument <span class="keyword">and</span> returns an integer）</span><br><span class="line">h) 一个有<span class="number">10</span>个指针的数组，该指针指向一个函数，该函数有一个整型参数并返回一个整型数（ An <span class="built_in">array</span> of ten pointers to functions that take an integer argument <span class="keyword">and</span> <span class="keyword">return</span> an integer ）</span><br></pre></td></tr></table></figure>

<p>答案是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">a) <span class="keyword">int</span> a; <span class="comment">// An integer</span></span><br><span class="line">b) <span class="keyword">int</span> *a; <span class="comment">// A pointer to an integer</span></span><br><span class="line">c) <span class="keyword">int</span> **a; <span class="comment">// A pointer to a pointer to an integer</span></span><br><span class="line">d) <span class="keyword">int</span> a[<span class="number">10</span>]; <span class="comment">// An array of 10 integers</span></span><br><span class="line">e) <span class="keyword">int</span> *a[<span class="number">10</span>]; <span class="comment">// An array of 10 pointers to integers</span></span><br><span class="line">f) <span class="keyword">int</span> (*a)[<span class="number">10</span>]; <span class="comment">// A pointer to an array of 10 integers</span></span><br><span class="line">g) <span class="keyword">int</span> (*a)(<span class="keyword">int</span>); <span class="comment">// A pointer to a function a that takes an integer argument and returns an integer</span></span><br><span class="line">h) <span class="keyword">int</span> (*a[<span class="number">10</span>])(<span class="keyword">int</span>); <span class="comment">// An array of 10 pointers to functions that take an integer argument and return an integer</span></span><br></pre></td></tr></table></figure>

<p>人们经常声称这里有几个问题是那种要翻一下书才能回答的问题，我同意这种说法。当我写这篇文章时，为了确定语法的正确性，我的确查了一下书。</p>
<p>但是当我被面试的时候，我期望被问到这个问题（或者相近的问题）。因为在被面试的这段时间里，我确定我知道这个问题的答案。应试者如果不知道</p>
<p>所有的答案（或至少大部分答案），那么也就没有为这次面试做准备，如果该面试者没有为这次面试做准备，那么他又能为什么出准备呢？</p>
<h3 id="Static"><a href="#Static" class="headerlink" title="Static"></a>Static</h3><p><em><strong>6.关键字static的作用是什么？</strong></em></p>
<p>这个简单的问题很少有人能回答完全。在C语言中，关键字static有三个明显的作用：</p>
<p>1). 在函数体内，一个被声明为静态的变量在这一函数被调用过程中维持其值不变。</p>
<p>2). 在模块内（但在函数体外），一个被声明为静态的变量可以被模块内所用函数访问，但不能被模块外其它函数访问。它是一个本地的全局变量。</p>
<p>3). 在模块内，一个被声明为静态的函数只可被这一模块内的其它函数调用。那就是，这个函数被限制在声明它的模块的本地范围内使用。</p>
<p>大多数应试者能正确回答第一部分，一部分能正确回答第二部分，同是很少的人能懂得第三部分。这是一个应试者的严重的缺点，因为他显然不懂得本地化数据和代码范围的好处和重要性。</p>
<h3 id="Const"><a href="#Const" class="headerlink" title="Const"></a>Const</h3><p><em><strong>7.关键字const是什么含意？</strong></em></p>
<p>我只要一听到被面试者说：“const意味着常数”，我就知道我正在和一个业余者打交道。去年Dan Saks已经在他的文章里完全概括了const的所有用法，因此ESP(译者：Embedded Systems Programming)的每一位读者应该非常熟悉const能做什么和不能做什么.</p>
<p>如果你从没有读到那篇文章，只要能说出const意味着“只读”就可以了。尽管这个答案不是完全的答案，但我接受它作为一个正确的答案。（如果你想知道更详细的答案，仔细读一下Saks的文章吧。）如果应试者能正确回答这个问题，我将问他一个附加的问题：下面的声明都是什么意思？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> a;</span><br><span class="line"><span class="keyword">int</span> <span class="keyword">const</span> a;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> *a;</span><br><span class="line"><span class="keyword">int</span> * <span class="keyword">const</span> a;</span><br><span class="line"><span class="keyword">int</span> <span class="keyword">const</span> * a <span class="keyword">const</span>;</span><br></pre></td></tr></table></figure>

<p>前两个的作用是一样，a是一个常整型数。第三个意味着a是一个指向常整型数的指针（也就是，整型数是不可修改的，但指针可以）。第四个意思a是一个指向整型数的常指针（也就是说，指针指向的整型数是可以修改的，但指针是不可修改的）。最后一个意味着a是一个指向常整型数的常指针（也就是说，指针指向的整型数是不可修改的，同时指针也是不可修改的）。如果应试者能正确回答这些问题，那么他就给我留下了一个好印象。顺带提一句，也许你可能会问，即使不用关键字const，也还是能很容易写出功能正确的程序，那么我为什么还要如此看重关键字const呢？我也如下的几下理由：</p>
<p>1). 关键字const的作用是为给读你代码的人传达非常有用的信息，实际上，声明一个参数为常量是为了告诉了用户这个参数的应用目的。如果你曾花很多时间清理其它人留下的垃圾，你就会很快学会感谢这点多余的信息。（当然，懂得用const的程序员很少会留下的垃圾让别人来清理的。）</p>
<p>2). 通过给优化器一些附加的信息，使用关键字const也许能产生更紧凑的代码。</p>
<p>3). 合理地使用关键字const可以使编译器很自然地保护那些不希望被改变的参数，防止其被无意的代码修改。简而言之，这样可以减少bug的出现。</p>
<h3 id="Volatile"><a href="#Volatile" class="headerlink" title="Volatile"></a>Volatile</h3><p><em><strong>8.关键字volatile有什么含意 并给出三个不同的例子。（定义寄存器地址前面用volatile）</strong></em></p>
<p>一个定义为volatile的变量是说这变量可能会被意想不到地改变，这样，编译器就不会去假设这个变量的值了。精确地说就是，优化器在用到这个变量时必须每次都小心地重新读取这个变量的值，而不是使用保存在寄存器里的备份。下面是volatile变量的几个例子：</p>
<p>1). 并行设备的硬件寄存器（如：状态寄存器）</p>
<p>2). 一个中断服务子程序中会访问到的非自动变量(Non-automatic variables)</p>
<p>3). 多线程应用中被几个任务共享的变量</p>
<p>回答不出这个问题的人是不会被雇佣的。我认为这是区分C程序员和嵌入式系统程序员的最基本的问题。嵌入式系统程序员经常同硬件、中断、RTOS等等打交道，所用这些都要求volatile变量。不懂得volatile内容将会带来灾难。</p>
<p>假设被面试者正确地回答了这是问题（嗯，怀疑这否会是这样），我将稍微深究一下，看一下这家伙是不是直正懂得volatile完全的重要性。</p>
<p>1). 一个参数既可以是const还可以是volatile吗？解释为什么。</p>
<p>2). 一个指针可以是volatile 吗？解释为什么。</p>
<p>3). 下面的函数有什么错误：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">square</span><span class="params">(<span class="keyword">volatile</span> <span class="keyword">int</span> *ptr)</span></span>&#123;</span><br><span class="line">		<span class="keyword">return</span> ptr *ptr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是答案：</p>
<p>1). 是的。一个例子是只读的状态寄存器。它是volatile因为它可能被意想不到地改变。它是const因为程序不应该试图去修改它。</p>
<p>2). 是的。尽管这并不很常见。一个例子是当一个中服务子程序修该一个指向一个buffer的指针时。</p>
<p>3). 这段代码的有个恶作剧。这段代码的目的是用来返指针<em>ptr指向值的平方，但是，由于</em>ptr指向一个volatile型参数，编译器将产生类似下面的代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">square</span><span class="params">(<span class="keyword">volatile</span> <span class="keyword">int</span> *ptr)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a, b;</span><br><span class="line">    a = *ptr;</span><br><span class="line">    b = *ptr;</span><br><span class="line">    <span class="keyword">return</span> a * b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于*ptr的值可能被意想不到地该变，因此a和b可能是不同的。结果，这段代码可能返不是你所期望的平方值！正确的代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">square</span><span class="params">(<span class="keyword">volatile</span> <span class="keyword">int</span> *ptr)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    a = *ptr;</span><br><span class="line">    <span class="keyword">return</span> a * a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="位操作"><a href="#位操作" class="headerlink" title="位操作"></a>位操作</h3><p><em><strong>9.嵌入式系统总是要用户对变量或寄存器进行位操作。给定一个整型变量a，写两段代码，第一个设置a的bit 3，第二个清除a 的bit 3。在以上两个操作中，要保持其它位不变。</strong></em></p>
<p>对这个问题有三种基本的反应</p>
<p>1). 不知道如何下手。该被面者从没做过任何嵌入式系统的工作。</p>
<p>2). 用bit fields。Bit fields是被扔到C语言死角的东西，它保证你的代码在不同编译器之间是不可移植的，同时也保证了的你的代码是不可重用的。我最近不幸看到Infineon为其较复杂的通信芯片写的驱动程序，它用到了bit fields因此完全对我无用，因为我的编译器用其它的方式来实现bit fields的。从道德讲：永远不要让一个非嵌入式的家伙粘实际硬件的边。</p>
<p>3). 用 #defines 和 bit masks 操作。这是一个有极高可移植性的方法，是应该被用到的方法。最佳的解决方案如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BIT3 (0x1&lt;&lt;3)</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> a;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">set_bit3</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;</span><br><span class="line">    a |= BIT3;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear_bit3</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;</span><br><span class="line">    a &amp;= ~BIT3;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一些人喜欢为设置和清除值而定义一个掩码同时定义一些说明常数，这也是可以接受的。我希望看到几个要点：说明常数、|=和&amp;=~操作。</p>
<h3 id="访问固定的内存位置"><a href="#访问固定的内存位置" class="headerlink" title="访问固定的内存位置"></a>访问固定的内存位置</h3><p><em><strong>10.嵌入式系统经常具有要求程序员去访问某特定的内存位置的特点。在某工程中，要求设置一绝对地址为0x67a9的整型变量的值为0xaa66。编译器是一个纯粹的ANSI编译器。写代码去完成这一任务。</strong></em></p>
<p>这一问题测试你是否知道为了访问一绝对地址把一个整型数强制转换（typecast）为一指针是合法的。这一问题的实现方式随着个人风格不同而不同。典型的类似代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *ptr;</span><br><span class="line">ptr = (<span class="keyword">int</span> *)<span class="number">0x67a9</span>;</span><br><span class="line">*ptr = <span class="number">0xaa55</span>;</span><br></pre></td></tr></table></figure>

<p>一个较晦涩的方法是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">*(<span class="keyword">int</span> * <span class="keyword">const</span>)(<span class="number">0x67a9</span>) = <span class="number">0xaa55</span>;</span><br></pre></td></tr></table></figure>

<p>即使你的品味更接近第二种方案，但我建议你在面试时使用第一种方案。</p>
<h3 id="中断"><a href="#中断" class="headerlink" title="中断"></a>中断</h3><p><em><strong>11.中断是嵌入式系统中重要的组成部分，这导致了很多编译开发商提供一种扩展—让标准C支持中断。具代表事实是，产生了一个新的关键字interrupt。下面的代码就使用了interrupt关键字去定义了一个中断服务子程序(ISR)，请评论一下这段代码的。</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">__interrupt <span class="keyword">double</span> <span class="title">compute_area</span> <span class="params">(<span class="keyword">double</span> radius)</span></span>&#123;</span><br><span class="line">    <span class="keyword">double</span> area = PI * radius * radius;</span><br><span class="line">    <span class="built_in">printf</span>(” Area = %f”, area);</span><br><span class="line">    <span class="keyword">return</span> area;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个函数有太多的错误了，以至让人不知从何说起了：</p>
<p>1). ISR 不能返回一个值。如果你不懂这个，那么你不会被雇用的。</p>
<p>2). ISR 不能传递参数。如果你没有看到这一点，你被雇用的机会等同第一项。</p>
<p>3). 在许多的处理器/编译器中，浮点一般都是不可重入的。有些处理器/编译器需要让额处的寄存器入栈，有些处理器/编译器就是不允许在ISR中做浮点运算。此外，ISR应该是短而有效率的，在ISR中做浮点运算是不明智的。</p>
<p>4). 与第三点一脉相承，printf()经常有重入和性能上的问题。如果你丢掉了第三和第四点，我不会太为难你的。不用说，如果你能得到后两点，那么你的被雇用前景越来越光明了。</p>
<h3 id="代码例子"><a href="#代码例子" class="headerlink" title="代码例子"></a>代码例子</h3><p><em><strong>12.下面的代码输出是什么，为什么？</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> a = <span class="number">6</span>;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">-20</span>;</span><br><span class="line">    (a+b &gt; <span class="number">6</span>) <span class="built_in">puts</span>(<span class="string">&quot;&gt; 6&quot;</span>) : <span class="built_in">puts</span>(<span class="string">&quot;&lt;= 6&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个问题测试你是否懂得C语言中的整数自动转换原则，我发现有些开发者懂得极少这些东西。不管如何，这无符号整型问题的答案是输出是“&gt;6”。原因是当表达式中存在有符号类型和无符号类型时所有的操作数都自动转换为无符号类型。 因此-20变成了一个非常大的正整数，所以该表达式计算出的结果大于6。这一点对于应当频繁用到无符号数据类型的嵌入式系统来说是丰常重要的。如果你答错了这个问题，你也就到了得不到这份工作的边缘。</p>
<p><em><strong>13.评价下面的代码片断：</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> zero = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> compzero = <span class="number">0xFFFF</span>;</span><br><span class="line"><span class="comment">/*1&#x27;s complement of zero */</span></span><br></pre></td></tr></table></figure>

<p>对于一个int型不是16位的处理器为说，上面的代码是不正确的。应编写如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> compzero = ~<span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>这一问题真正能揭露出应试者是否懂得处理器字长的重要性。在我的经验里，好的嵌入式程序员非常准确地明白硬件的细节和它的局限，然而PC机程序往往把硬件作为一个无法避免的烦恼。</p>
<p>到了这个阶段，应试者或者完全垂头丧气了或者信心满满志在必得。如果显然应试者不是很好，那么这个测试就在这里结束了。但如果显然应试者做得不错，那么我就扔出下面的追加问题，这些问题是比较难的，我想仅仅非常优秀的应试者能做得不错。提出这些问题，我希望更多看到应试者应付问题的方法，而不是答案。不管如何，你就当是这个娱乐吧…</p>
<h3 id="动态内存分配"><a href="#动态内存分配" class="headerlink" title="动态内存分配"></a>动态内存分配</h3><p><em><strong>14.尽管不像非嵌入式计算机那么常见，嵌入式系统还是有从堆（heap）中动态分配内存的过程的。那么嵌入式系统中，动态分配内存可能发生的问题是什么？</strong></em></p>
<p>这里，我期望应试者能提到内存碎片，碎片收集的问题，变量的持行时间等等。这个主题已经在ESP杂志中被广泛地讨论过了（主要是 P.J. Plauger, 他的解释远远超过我这里能提到的任何解释），所有回过头看一下这些杂志吧！让应试者进入一种虚假的安全感觉后，我拿出这么一个小节目：下面的代码片段的输出是什么，为什么？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *ptr;</span><br><span class="line"><span class="keyword">if</span> ((ptr = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="number">0</span>)) == <span class="literal">NULL</span>)</span><br><span class="line">    <span class="built_in">puts</span>(“Got a null pointer”);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="built_in">puts</span>(“Got a valid pointer”);</span><br></pre></td></tr></table></figure>

<p>这是一个有趣的问题。最近在我的一个同事不经意把0值传给了函数malloc，得到了一个合法的指针之后，我才想到这个问题。这就是上面的代码，该代码的输出是“Got a valid pointer”。我用这个来开始讨论这样的一问题，看看被面试者是否想到库例程这样做是正确。得到正确的答案固然重要，但解决问题的方法和你做决定的基本原理更重要些。</p>
<h3 id="Typedef"><a href="#Typedef" class="headerlink" title="Typedef"></a>Typedef</h3><p><em><strong>15.Typedef 在C语言中频繁用以声明一个已经存在的数据类型的同义字。也可以用预处理器做类似的事。例如，思考一下下面的例子：</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> dPS struct s *</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">s</span> * <span class="title">tPS</span>;</span></span><br></pre></td></tr></table></figure>

<p>以上两种情况的意图都是要定义dPS 和 tPS 作为一个指向结构s指针。哪种方法更好呢？（如果有的话）为什么？</p>
<p>这是一个非常微妙的问题，任何人答对这个问题（正当的原因）是应当被恭喜的。答案是：typedef更好。思考下面的例子：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">dPS p1,p2;</span><br><span class="line">tPS p3,p4;</span><br></pre></td></tr></table></figure>

<p>第一个扩展为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">s</span> * <span class="title">p1</span>, <span class="title">p2</span>;</span></span><br></pre></td></tr></table></figure>

<p>上面的代码定义p1为一个指向结构的指，p2为一个实际的结构，这也许不是你想要的。第二个例子正确地定义了p3 和p4 两个指针。</p>
<h3 id="晦涩的语法"><a href="#晦涩的语法" class="headerlink" title="晦涩的语法"></a>晦涩的语法</h3><p><em><strong>16.C语言同意一些令人震惊的结构,下面的结构是合法的吗，如果是它做些什么？</strong></em></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">5</span>, b = <span class="number">7</span>, c;</span><br><span class="line">c = a+++b;</span><br></pre></td></tr></table></figure>

<p>这个问题将做为这个测验的一个愉快的结尾。不管你相不相信，上面的例子是完全合乎语法的。问题是编译器如何处理它？水平不高的编译作者实际上会争论这个问题，根据最处理原则，编译器应当能处理尽可能所有合法的用法。因此，上面的代码被处理成：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">c = a++ + b;</span><br></pre></td></tr></table></figure>

<p>因此, 这段代码持行后a = 6, b = 7, c = 12。</p>
<p>如果你知道答案，或猜出正确答案，做得好。如果你不知道答案，我也不把这个当作问题。我发现这个问题的最大好处是:这是一个关于代码编写风格，代码的可读性，代码的可修改</p>
]]></content>
      <tags>
        <tag>嵌入式</tag>
      </tags>
  </entry>
  <entry>
    <title>经典C内存管理疑难杂例</title>
    <url>/2021/12/16/%E7%BB%8F%E5%85%B8C%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86%E7%96%91%E9%9A%BE%E6%9D%82%E4%BE%8B/</url>
    <content><![CDATA[<h3 id="一定要弄懂GetMemory"><a href="#一定要弄懂GetMemory" class="headerlink" title="一定要弄懂GetMemory"></a>一定要弄懂GetMemory</h3><h3 id="堆栈"><a href="#堆栈" class="headerlink" title="堆栈"></a><strong>堆栈</strong></h3><p>栈中分配局部变量空间，是系统自动分配空间。定义一个 char a；系统会自动在栈上为其开辟空间。由于栈上的空间是自动分配自动回收的，所以栈上的数据的生存周期只是在函数的运行过程中，运行后就释放掉，不可以再访问。</p>
<p>堆区分配程序员申请的内存空间，堆上的数据只要程序员不释放空间，就一直可以访问到，不过缺点是一旦忘记释放会造成内存泄露。</p>
<p>静态区是分配静态变量，全局变量空间的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">inta = <span class="number">0</span>; 全局初始化区  </span><br><span class="line"><span class="keyword">char</span>*p1; 全局未初始化区  </span><br><span class="line">main()&#123;  </span><br><span class="line">  <span class="keyword">int</span> b;      <span class="comment">//栈  </span></span><br><span class="line">  <span class="keyword">char</span> s[] = <span class="string">&quot;abc&quot;</span>;    <span class="comment">//栈  </span></span><br><span class="line">  <span class="keyword">char</span> *p2;      <span class="comment">//栈  </span></span><br><span class="line">  <span class="keyword">char</span> *p3 = <span class="string">&quot;123456&quot;</span>;   <span class="comment">// 123456\0在常量区，p3在栈上。  </span></span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">int</span> c =<span class="number">0</span>；      <span class="comment">//全局（静态）初始化区  </span></span><br><span class="line">  p1 = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="number">10</span>);    <span class="comment">//堆  </span></span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure>

<h3 id="GetMemory1"><a href="#GetMemory1" class="headerlink" title="GetMemory1"></a><strong>GetMemory1</strong></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">voidGetMemory1(<span class="keyword">char</span> *p)    </span><br><span class="line">&#123;</span><br><span class="line">  p = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="number">100</span>);    </span><br><span class="line">&#125;    </span><br><span class="line">voidTest1(<span class="keyword">void</span>)    </span><br><span class="line">&#123;    </span><br><span class="line">  <span class="keyword">char</span> *str = <span class="literal">NULL</span>;    </span><br><span class="line">  GetMemory1(str);      </span><br><span class="line">  <span class="built_in">strcpy</span>(str, <span class="string">&quot;hello world&quot;</span>);    </span><br><span class="line">  <span class="built_in">printf</span>(str);  <span class="comment">//str一直是空，程序崩溃     </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果： </p>
<p><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211118195523563-1384916852.png" alt="img"></p>
<p>分析：</p>
<p>毛病出在函数GetMemory1 中。<strong>编译器总是要为函数的每个参数制作临时副本</strong>，指针参数p的副本是 _p，编译器使 _p = p。如果函数体内的程序修改了_p的内容，就导致参数p的内容作相应的修改。这就是指针可以用作输出参数的原因。在本例中，_p申请了新的内存，只是把 _p所指的内存地址改变了，但是p丝毫未变。所以函数GetMemory并不能输出任何东西。事实上，每执行一次GetMemory1就会泄露一块内存，因为没有用free释放内存。<strong>Test1中调用GetMemory1时，函数参数为str的副本不是str本身</strong>。</p>
<h3 id="GetMemory2"><a href="#GetMemory2" class="headerlink" title="GetMemory2"></a><strong>GetMemory2</strong></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">voidGetMemory2(<span class="keyword">char</span> **p, <span class="keyword">int</span> num)    </span><br><span class="line">&#123;</span><br><span class="line">  *p = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(num);    </span><br><span class="line">&#125;    </span><br><span class="line">voidTest2(<span class="keyword">void</span>)    </span><br><span class="line">&#123;    </span><br><span class="line">  <span class="keyword">char</span> *str = <span class="literal">NULL</span>;    </span><br><span class="line">  GetMemory2(&amp;str, <span class="number">100</span>);    </span><br><span class="line">  <span class="built_in">strcpy</span>(str, <span class="string">&quot;hello&quot;</span>);      </span><br><span class="line">  <span class="built_in">printf</span>(str);        </span><br><span class="line">&#125;    </span><br></pre></td></tr></table></figure>


<p>结果：输出hello </p>
<p>分析：动态分配的内存不会自动释放；</p>
<p>没有测试是否成功分配了内存，应该有if (*p == NULL) { ……} 之类的语句处理内存分配失败的其情况。</p>
<h3 id="GetMemory3"><a href="#GetMemory3" class="headerlink" title="GetMemory3"></a><strong>GetMemory3</strong></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">char</span>* <span class="title">GetMemory3</span><span class="params">(<span class="keyword">void</span>)</span>    </span></span><br><span class="line"><span class="function"></span>&#123;      </span><br><span class="line">  <span class="keyword">char</span> p[] = <span class="string">&quot;hello world&quot;</span>;    </span><br><span class="line">  <span class="keyword">return</span> p;    </span><br><span class="line">&#125;    </span><br><span class="line">voidTest3(<span class="keyword">void</span>)    </span><br><span class="line">&#123;    </span><br><span class="line">  <span class="keyword">char</span> *str = <span class="literal">NULL</span>;    </span><br><span class="line">  str = GetMemory3();        </span><br><span class="line">  <span class="built_in">printf</span>(str);    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果：输出乱码。 </p>
<p>分析：字符数组p存在于<strong>栈空间，是局部变量，函数返回后，内存空间被释放</strong>，因此输出无效值。字符数组的值是可以修改的，例如p[0] = ‘t‘。</p>
<h3 id="GetMemory4"><a href="#GetMemory4" class="headerlink" title="GetMemory4"></a><strong>GetMemory4</strong></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">char</span>*<span class="title">GetMemory4</span><span class="params">(<span class="keyword">void</span>)</span>    </span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">  <span class="keyword">char</span> *p = <span class="string">&quot;hello&quot;</span>;    </span><br><span class="line">  <span class="keyword">return</span> p;    </span><br><span class="line">&#125;    </span><br><span class="line">voidTest4(<span class="keyword">void</span>)    </span><br><span class="line">&#123;    </span><br><span class="line">  <span class="keyword">char</span> *str = <span class="literal">NULL</span>;    </span><br><span class="line">  str = GetMemory4();   </span><br><span class="line">  <span class="built_in">cout</span>&lt;&lt; str &lt;&lt; <span class="built_in">endl</span>;    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果：输出hello<br>分析：p指向的是字符串常量，字符串常量保存在只读的数据段，是全局区域，但不是像全局变量那样保存在普通数据段（静态存储区）。无法对p所指的内存的内容修改，例如p[0] = ‘y;这样的修改是错误的。</p>
<h3 id="GetMemory5"><a href="#GetMemory5" class="headerlink" title="GetMemory5"></a><strong>GetMemory5</strong></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">char</span>*<span class="title">GetMemory5</span><span class="params">(<span class="keyword">void</span>)</span>    </span></span><br><span class="line"><span class="function"></span>&#123;     </span><br><span class="line">  <span class="keyword">return</span> <span class="string">&quot;hello&quot;</span>;    </span><br><span class="line">&#125;    </span><br><span class="line">voidTest3(<span class="keyword">void</span>)    </span><br><span class="line">&#123;    </span><br><span class="line">  <span class="keyword">char</span> *str = <span class="literal">NULL</span>;    </span><br><span class="line">  str = GetMemory5();     </span><br><span class="line">  <span class="built_in">printf</span>(str);    </span><br><span class="line">&#125;    </span><br></pre></td></tr></table></figure>


<p>结果：输出hello</p>
<p>分析：直接返回常量区。</p>
<h3 id="GetMemory6"><a href="#GetMemory6" class="headerlink" title="GetMemory6"></a><strong>GetMemory6</strong></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">voidGetMemory6(<span class="keyword">void</span>) &#123;  </span><br><span class="line">  <span class="keyword">char</span> *str = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(<span class="number">100</span>);  </span><br><span class="line">  <span class="built_in">strcpy</span>(str, <span class="string">&quot;hello&quot;</span>);  </span><br><span class="line">  <span class="built_in">free</span>(str);  </span><br><span class="line">  <span class="comment">//str = NULL，加上这句程序才不会有野指针  </span></span><br><span class="line">  <span class="keyword">if</span> (str != <span class="literal">NULL</span>) &#123;  </span><br><span class="line">    <span class="built_in">strcpy</span>(str, <span class="string">&quot;world&quot;</span>);  </span><br><span class="line">    <span class="built_in">printf</span>(str);  </span><br><span class="line">  &#125;  </span><br><span class="line">&#125;  </span><br><span class="line">voidmain()&#123;    </span><br><span class="line">  GetMemory6();  </span><br><span class="line">&#125;    </span><br></pre></td></tr></table></figure>

<p>结果：能够输出world，但程序存在问题。</p>
<p>分析：程序出现了<strong>野指针</strong>。</p>
<p><strong>野指针</strong>只会出现在像C和C++这种没有自动内存垃圾回收功能的高级语言中, 所以java或c#肯定不会有野指针的概念. <strong>当我们用malloc为一个指针分配一个空间后, 用完这个指针，把它free掉，但是没有让这个指针指向NULL或某一个特定的空间</strong>。如上面程序一样，将str进行free后，只是释放了指针所指的内存，但指针并没有释放掉，此时指针所指的是垃圾内存；这样的话，if语句永为真，if判断无效。delete也存在同样的问题。</p>
<p><strong>防止产生野指针</strong>：</p>
<ul>
<li>指针变量一定要初始化为NULL，因为任何指针变量刚被创建时不会自动成为NULL指针，它的缺省值是随机的。</li>
<li>当free或delete后，将指针指向NULL。通常判断一个指针是否合法，都是使用if语句测试该指针是否为NULL。</li>
</ul>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>编译选项学习总结</title>
    <url>/2021/12/16/%E7%BC%96%E8%AF%91%E9%80%89%E9%A1%B9%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h3 id="基本选项"><a href="#基本选项" class="headerlink" title="基本选项"></a>基本选项</h3><p>一般来讲，C/C++从源代码到可执行程序之间要经历四个步骤：</p>
<p>预处理：展开头文件/宏替换/去掉注释/条件编译</p>
<p>编译：检查语法，生成汇编</p>
<p>汇编：汇编代码转换机器码</p>
<p>链接：链接到一起生成可执行程序</p>
<p><em><strong>-E：只进行预处理，不编译</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc -E <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>命令行显示出预处理信息，但不生成文件</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><span class="line"><span class="attribute">gcc</span> -E hello.c &gt; <span class="number">1</span>.txt</span><br></pre></td></tr></table></figure>

<p>将相关预处理信息重定向到文件1.txt中，文件内容显示如下</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">signed</span> <span class="keyword">char</span> <span class="keyword">__int8_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">char</span> <span class="keyword">__uint8_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">short</span> <span class="keyword">__int16_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">short</span> <span class="keyword">__uint16_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> <span class="keyword">__int32_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="keyword">__uint32_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> <span class="keyword">__int64_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> <span class="keyword">__uint64_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">__darwin_intptr_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="keyword">__darwin_natural_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> <span class="keyword">__darwin_ct_rune_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line"> <span class="keyword">char</span> __mbstate8[<span class="number">128</span>];</span><br><span class="line"> <span class="keyword">long</span> <span class="keyword">long</span> _mbstateL;</span><br><span class="line">&#125; <span class="keyword">__mbstate_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">__mbstate_t</span> <span class="keyword">__darwin_mbstate_t</span>;</span><br><span class="line">									.</span><br><span class="line">                  .</span><br><span class="line">                  .</span><br></pre></td></tr></table></figure>

<p><em><strong>-S：只编译，不汇编</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc -S <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>生成文件test.s，文件内容如下：</p>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">	.section	__TEXT,__text,regular,pure_instructions</span><br><span class="line">	.build_version macos, <span class="number">11</span>, <span class="number">0</span>	sdk_version <span class="number">11</span>, <span class="number">3</span></span><br><span class="line">	.globl	_main                           <span class="comment">## -- Begin function main</span></span><br><span class="line">	.p2align	<span class="number">4</span>, <span class="number">0x90</span></span><br><span class="line">_main:                                  <span class="comment">## @main</span></span><br><span class="line">	.cfi_startproc</span><br><span class="line"><span class="comment">## %bb.0:</span></span><br><span class="line">	pushq	%rbp</span><br><span class="line">	.cfi_def_cfa_offset <span class="number">16</span></span><br><span class="line">	.cfi_offset %rbp, -<span class="number">16</span></span><br><span class="line">	movq	%rsp, %rbp</span><br><span class="line">	.cfi_def_cfa_register %rbp</span><br><span class="line">	subq	$16, %rsp</span><br><span class="line">	movl	$0, -<span class="number">4</span>(%rbp)</span><br><span class="line">	leaq	L<span class="number">_</span>.str(%rip), %rdi</span><br><span class="line">	movb	$0, %al</span><br><span class="line">	callq	_printf</span><br><span class="line">	xorl	%ecx, %ecx</span><br><span class="line">	movl	%eax, -<span class="number">8</span>(%rbp)                  <span class="comment">## 4-byte Spill</span></span><br><span class="line">	movl	%ecx, %eax</span><br><span class="line">	addq	$16, %rsp</span><br><span class="line">	popq	%rbp</span><br><span class="line">	retq</span><br><span class="line">	.cfi_endproc</span><br><span class="line">                                        <span class="comment">## -- End function</span></span><br><span class="line">	.section	__TEXT,__cstring,cstring_literals</span><br><span class="line">L<span class="number">_</span>.str:                                 <span class="comment">## @.str</span></span><br><span class="line">	.asciz	<span class="string">&quot;hello\n&quot;</span></span><br><span class="line">.subsections_via_symbols</span><br></pre></td></tr></table></figure>

<p>表明此时根据源文件生成了汇编代码</p>
<p><em><strong>-c：只编译、汇编，不链接</strong></em></p>
<p>执行指令</p>
<figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">gcc -<span class="keyword">c</span> test.<span class="keyword">c</span></span><br></pre></td></tr></table></figure>

<p>此时源文件经过编译和汇编直接生成二进制文件test.o，没有其他中间文件生成</p>
<p><em><strong>-g：包含调试信息</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc -g <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>可见不光生成了可执行文件a.out，同时还在当前文件夹下生成了具备调试信息的文件a.out.dSYM，此时可以使用调试工具gdb对可执行文件进行调试</p>
<p><em><strong>-I：指定include包含文件的搜索目录</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc -I <span class="keyword">include</span>/  -c <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>表明在gcc执行过程中，除了标准库中的头文件，还可以从指定的目录去搜索头文件</p>
<p><em><strong>-o：输出成指定文件名</strong></em></p>
<p>执行指令</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">gcc</span> -o <span class="keyword">b</span> test.c</span><br></pre></td></tr></table></figure>

<p>表明指定生成的二进制文件名称为b，之后运行程序b，和a.out运行是同样的结果</p>
<p><em><strong>-lpthread：多线程编程</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc <span class="keyword">test</span>.c -lpthread</span><br></pre></td></tr></table></figure>

<p>指明gcc在链接阶段链接pthread库，从而编译包含多线程头文件的程序</p>
<p><em><strong>-lm：程序使用了math.h中声明的库函数</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc <span class="keyword">test</span>.c -lm</span><br></pre></td></tr></table></figure>

<h3 id="高级选项"><a href="#高级选项" class="headerlink" title="高级选项"></a>高级选项</h3><p><em><strong>-v：详细输出编译过程中所采用的每一个选项</strong></em></p>
<p>执行指令</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">gcc -v</span></span><br></pre></td></tr></table></figure>

<p>得到结果</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">Configured with: --prefix=<span class="regexp">/Applications/</span>Xcode.app<span class="regexp">/Contents/</span>Developer<span class="regexp">/usr --with-gxx-include-dir=/</span>Applications<span class="regexp">/Xcode.app/</span>Contents<span class="regexp">/Developer/</span>Platforms<span class="regexp">/MacOSX.platform/</span>Developer<span class="regexp">/SDKs/</span>MacOSX.sdk<span class="regexp">/usr/i</span>nclude<span class="regexp">/c++/</span><span class="number">4.2</span>.<span class="number">1</span></span><br><span class="line">Apple clang version <span class="number">12.0</span>.<span class="number">5</span> (clang-<span class="number">1205.0</span>.<span class="number">22.11</span>)</span><br><span class="line">Target: x86_64-apple-darwin20.<span class="number">5.0</span></span><br><span class="line">Thread model: posix</span><br><span class="line">InstalledDir: <span class="regexp">/Applications/</span>Xcode.app<span class="regexp">/Contents/</span>Developer<span class="regexp">/Toolchains/</span>XcodeDefault.xctoolchain<span class="regexp">/usr/</span>bin</span><br></pre></td></tr></table></figure>

<p><em><strong>-C：预处理时保留注释信息</strong></em></p>
<p>执行指令</p>
<figure class="highlight mathematica"><table><tr><td class="code"><pre><span class="line"><span class="variable">gcc</span> <span class="operator">-</span><span class="built_in">E</span> <span class="operator">-</span><span class="built_in">C</span> <span class="variable">test</span><span class="operator">.</span><span class="variable">c</span> </span><br></pre></td></tr></table></figure>

<p>得到结果</p>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line">* <span class="keyword">Version</span> <span class="number">2.0</span> (the <span class="string">&#x27;License&#x27;</span>). You may <span class="keyword">not</span> use this file <span class="keyword">except</span> <span class="keyword">in</span></span><br><span class="line"> * compliance <span class="keyword">with</span> the License. The rights granted <span class="keyword">to</span> you under the License</span><br><span class="line"> * may <span class="keyword">not</span> be used <span class="keyword">to</span> <span class="keyword">create</span>, <span class="keyword">or</span> <span class="keyword">enable</span> the creation <span class="keyword">or</span> redistribution <span class="keyword">of</span>,</span><br><span class="line"> * unlawful <span class="keyword">or</span> unlicensed copies <span class="keyword">of</span> an Apple operating <span class="keyword">system</span>, <span class="keyword">or</span> <span class="keyword">to</span></span><br><span class="line"> * circumvent, violate, <span class="keyword">or</span> <span class="keyword">enable</span> the circumvention <span class="keyword">or</span> violation <span class="keyword">of</span>, <span class="keyword">any</span></span><br><span class="line"> * terms <span class="keyword">of</span> an Apple operating <span class="keyword">system</span> software license agreement.</span><br><span class="line"> *</span><br><span class="line"> * Please obtain a <span class="keyword">copy</span> <span class="keyword">of</span> the License at</span><br><span class="line"> * http://www.opensource.apple.com/apsl/ <span class="keyword">and</span> <span class="keyword">read</span> it <span class="keyword">before</span> <span class="keyword">using</span> this file.</span><br><span class="line">																			.</span><br><span class="line">                                      .</span><br><span class="line"><span class="type">void</span> clearerr(FILE *);</span><br><span class="line"><span class="type">int</span> fclose(FILE *);</span><br><span class="line"><span class="type">int</span> feof(FILE *);</span><br><span class="line"><span class="type">int</span> ferror(FILE *);</span><br><span class="line"><span class="type">int</span> fflush(FILE *);</span><br><span class="line"><span class="type">int</span> fgetc(FILE *);</span><br><span class="line"><span class="type">int</span> fgetpos(FILE * <span class="keyword">restrict</span>, fpos_t *);</span><br><span class="line"><span class="type">char</span> *fgets(<span class="type">char</span> * <span class="keyword">restrict</span>, <span class="type">int</span>, FILE *);</span><br><span class="line">																			.</span><br><span class="line">                                      .</span><br><span class="line">                                      .</span><br></pre></td></tr></table></figure>

<p>可见和单纯执行-E选项时相比，还多加了注释信息</p>
<p><em><strong>-ggdb：在可执行文件中包含可供GDB使用的调试信息</strong></em></p>
<p>-g选项和-ggdb选项只有细微的差别：</p>
<p>-g以OS本地格式（stabs，COFF，XCOFF或DWARF 2）产生调试信息。</p>
<p>-ggdb生成专门用于gdb的调试信息。</p>
<p><em><strong>-fverbose-asm：在编译成汇编语言时，把C变量的名称作为汇编语言中的注释</strong></em></p>
<p>执行指令</p>
<figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">gcc test.<span class="keyword">c</span> -S -fverbose-<span class="keyword">asm</span></span><br></pre></td></tr></table></figure>

<p>得到结果</p>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line"> 	.section	__TEXT,__text,regular,pure_instructions</span><br><span class="line">	.build_version macos, <span class="number">11</span>, <span class="number">0</span>	sdk_version <span class="number">11</span>, <span class="number">3</span></span><br><span class="line">	.globl	_main                           <span class="comment">## -- Begin function main</span></span><br><span class="line">	.p2align	<span class="number">4</span>, <span class="number">0x90</span></span><br><span class="line">_main:                                  <span class="comment">## @main</span></span><br><span class="line">	.cfi_startproc</span><br><span class="line"><span class="comment">## %bb.0:</span></span><br><span class="line">	pushq	%rbp</span><br><span class="line">	.cfi_def_cfa_offset <span class="number">16</span></span><br><span class="line">	.cfi_offset %rbp, -<span class="number">16</span></span><br><span class="line">	movq	%rsp, %rbp</span><br><span class="line">	.cfi_def_cfa_register %rbp</span><br><span class="line">	subq	$16, %rsp</span><br><span class="line">	movl	$0, -<span class="number">4</span>(%rbp)</span><br><span class="line">	movl	$0, -<span class="number">8</span>(%rbp)</span><br><span class="line">	movl	$1, -<span class="number">12</span>(%rbp)</span><br><span class="line">	leaq	L<span class="number">_</span>.str(%rip), %rdi</span><br><span class="line">	movb	$0, %al</span><br><span class="line">	callq	_printf</span><br><span class="line">	xorl	%ecx, %ecx</span><br><span class="line">	movl	%eax, -<span class="number">16</span>(%rbp)                 <span class="comment">## 4-byte Spill</span></span><br><span class="line">	movl	%ecx, %eax</span><br><span class="line">	addq	$16, %rsp</span><br><span class="line">	popq	%rbp</span><br><span class="line">	retq</span><br><span class="line">	.cfi_endproc</span><br><span class="line">                                        <span class="comment">## -- End function</span></span><br><span class="line">	.section	__TEXT,__cstring,cstring_literals</span><br><span class="line">L<span class="number">_</span>.str:                                 <span class="comment">## @.str</span></span><br><span class="line">	.asciz	<span class="string">&quot;hello\n&quot;</span></span><br><span class="line"></span><br><span class="line">.subsections_via_symbols</span><br></pre></td></tr></table></figure>

<p><em><strong>-save-temps：自动输出预处理文件、汇编文件、对象文件，编译正常进行</strong></em></p>
<p>执行指令</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">gcc -<span class="keyword">save</span>-temps <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>之后使用ls命令查看目录中文件状态</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">a.<span class="keyword">out</span>   <span class="keyword">test</span>.bc <span class="keyword">test</span>.c  <span class="keyword">test</span>.i  <span class="keyword">test</span>.o  <span class="keyword">test</span>.s</span><br></pre></td></tr></table></figure>

<p>可见保留了所有的中间文件</p>
<p><em><strong>-fsyntax-only：只测试源文件语法是否正确，不会进行任何编译操作</strong></em></p>
<p>执行指令</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc -fsyntax-only <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>当源文件没有语法错误时，没有任何输出，当故意修改出一处语法错误时，得到结果</p>
<figure class="highlight scilab"><table><tr><td class="code"><pre><span class="line">test.c:<span class="number">4</span>:<span class="number">21</span>: <span class="built_in">error</span>: expected <span class="string">&#x27;;&#x27;</span> at <span class="keyword">end</span> of declaration</span><br><span class="line">    int i = <span class="number">0</span>, j = <span class="number">1</span> <span class="built_in">printf</span>(<span class="string">&quot;hello\n&quot;</span>);</span><br><span class="line">                    ^</span><br><span class="line">                    ;</span><br><span class="line"><span class="number">1</span> <span class="built_in">error</span> generated.</span><br></pre></td></tr></table></figure>

<p>显示出源文件语法中存在的错误</p>
<p><em><strong>-llibrary：连接时搜索指定的函数库</strong></em></p>
<p>例如：多线程编程例子</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc <span class="keyword">test</span>.c -lpthread</span><br></pre></td></tr></table></figure>

<p><em><strong>-Idirectory：指定额外的头文件搜索路径</strong></em></p>
<p>例如：</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">gcc -I ../<span class="keyword">include</span> <span class="keyword">test</span>.c</span><br></pre></td></tr></table></figure>

<p>添加目录../include为头文件搜索路径</p>
<p><em><strong>-Ldirectory：指定额外的函数库搜索路径</strong></em></p>
<p>例如：</p>
<figure class="highlight gams"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">gcc</span></span> -L ../lib test.c</span><br></pre></td></tr></table></figure>

<p>添加目录../iib为函数库搜索路径</p>
<p><em><strong>-static：禁止使用动态库</strong></em></p>
<p>详见下节</p>
<p><em><strong>-shared：尽量使用动态库</strong></em></p>
<p>详见下节</p>
<h3 id="库的创建与使用"><a href="#库的创建与使用" class="headerlink" title="库的创建与使用"></a>库的创建与使用</h3><p><em><strong>静态库</strong></em></p>
<p>以创建静态库static_lib.a为例</p>
<p>1、编写C源文件static_lib.c，其中写入需要重复调用的函数，执行命令</p>
<figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">gcc -<span class="keyword">c</span> static_lib.<span class="keyword">c</span></span><br></pre></td></tr></table></figure>

<p>生成目标文件static_lib.o</p>
<p>2、使用ar工具创建静态库</p>
<figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">ar rcs<span class="keyword"> static</span>_lib.a<span class="keyword"> static</span>_lib.o</span><br></pre></td></tr></table></figure>

<p>3、编写C头文件static_lib.h，其中写入这些函数的原型声明</p>
<p>4、编写主函数app.c，引入头文件static_lib.h，这样就可以正常使用那些自定义的可复用函数了</p>
<p>5、执行命令</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">gcc <span class="keyword">app</span>.c -static ./static_lib.a -o <span class="keyword">app</span></span><br></pre></td></tr></table></figure>

<p>编译生成可执行文件app</p>
<p><em><strong>动态库</strong></em></p>
<p>以创建动态库share_lib.so为例</p>
<p>1、编写C源文件share_lib.c，写入需要重复调用的函数，执行命令</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line">gcc -<span class="keyword">shared </span>-fPIC -o <span class="keyword">share_lib.so </span><span class="keyword">share_lib.c</span></span><br></pre></td></tr></table></figure>

<p>生成动态库文件share_lib.so</p>
<p>2、编写C头文件share_lib.h，写入函数的原型声明</p>
<p>3、编写主函数app.c，引入头文件share_lib.h，之后就可以调用在动态库中自定义的函数了</p>
<p>4、执行命令</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">gcc <span class="keyword">app</span>.c ./share_lib.<span class="keyword">so</span> -o <span class="keyword">app</span></span><br></pre></td></tr></table></figure>

<p>编译生成可执行文件app</p>
<h3 id="出错提示选项"><a href="#出错提示选项" class="headerlink" title="出错提示选项"></a>出错提示选项</h3><figure class="highlight haml"><table><tr><td class="code"><pre><span class="line">-<span class="ruby">Wall：会打开一些很有用的警告选项，建议编译时加此选项。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">W </span></span><br><span class="line"><span class="ruby"></span>-<span class="ruby">Wextra：打印一些额外的警告信息。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">w：禁止显示所有警告信息。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wshadow：当一个局部变量遮盖住了另一个局部变量，或者全局变量时，给出警告。很有用的选项，建议打开。 -Wall 并不会打开此项。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wpointer-arith：对函数指针或者void *类型的指针进行算术操作时给出警告。也很有用。 -Wall 并不会打开此项。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wcast-qual：当强制转化丢掉了类型修饰符时给出警告。 -Wall 并不会打开此项。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Waggregate-<span class="keyword">return</span>：如果定义或调用了返回结构体或联合体的函数，编译器就发出警告。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Winline：无论是声明为 inline 或者是指定了-finline-functions 选项，如果某函数不能内联，编译器都将发出警告。如果你的代码含有很多 inline 函数的话，这是很有用的选项。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Werror：把警告当作错误。出现任何警告就放弃编译。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wunreachable-code：如果编译器探测到永远不会执行到的代码，就给出警告。也是比较有用的选项。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wcast-align：一旦某个指针类型强制转换导致目标所需的地址对齐增加时，编译器就发出警告。</span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wundef：当一个没有定义的符号出现在 <span class="comment">#if 中时，给出警告。</span></span></span><br><span class="line"><span class="ruby"></span></span><br><span class="line">-<span class="ruby">Wredundant-decls：如果在同一个可见域内某定义多次声明，编译器就发出警告，即使这些重复声明有效并且毫无差别。</span></span><br></pre></td></tr></table></figure>



<h3 id="gcc常见警告含义"><a href="#gcc常见警告含义" class="headerlink" title="gcc常见警告含义"></a>gcc常见警告含义</h3><figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line">unused-<span class="function"><span class="keyword">function</span></span>:警告声明但是没有定义的static函数;</span><br><span class="line">unused- label:声明但是未使用的标签;</span><br><span class="line">unused-<span class="keyword">parameter</span>:警告未使用的函数参数;</span><br><span class="line">unused-variable:声明但是未使用的本地变量;</span><br><span class="line">unused-<span class="keyword">value</span>:计算了但是未使用的值;</span><br><span class="line"><span class="keyword">format</span>:printf和scanf这样的函数中的格式字符串的使用不当;</span><br><span class="line"><span class="keyword">implicit</span>-<span class="built_in">int</span>:未指定类型;</span><br><span class="line"><span class="keyword">implicit</span>-<span class="function"><span class="keyword">function</span></span>:函数在声明前使用;</span><br><span class="line"><span class="built_in">char</span>- subscripts:使用<span class="built_in">char</span>类作为数组下标(因为<span class="built_in">char</span>可能是有符号数);</span><br><span class="line">missingbraces:大括号不匹配;</span><br><span class="line">parentheses: 圆括号不匹配;</span><br><span class="line"><span class="keyword">return</span>-<span class="keyword">type</span>:函数有无返回值以及返回值类型不匹配;</span><br><span class="line"><span class="keyword">sequence</span>-point:违反顺序点的代码,比如 a[i] = c[i++];</span><br><span class="line">switch:switch语句缺少<span class="keyword">default</span>或者switch使用枚举变量为索引时缺少某个变量的<span class="keyword">case</span>;</span><br><span class="line">strict- aliasing=n:使用n设置对指针变量指向的对象类型产生警告的限制程度,默认n=<span class="number">3</span>;只有在-fstrict-aliasing设置的情况下有效;</span><br><span class="line">unknow-pragmas:使用未知的#pragma指令;</span><br><span class="line">uninitialized:使用的变量为初始化,只在-O2时有效;</span><br></pre></td></tr></table></figure>



<h3 id="其它选项"><a href="#其它选项" class="headerlink" title="其它选项"></a>其它选项</h3><p><em><strong>语言标准</strong></em></p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">-ansi：ANSI标准</span><br><span class="line"><span class="attribute">-std</span>=c99：C99标准</span><br><span class="line"><span class="attribute">-std</span>=gnu89：ISO/IEC 9899:1990 以及GNU扩充</span><br><span class="line"><span class="attribute">-std</span>=gnu99：ISO/IEC 9899:1999 以及GNU扩充</span><br><span class="line">-trigraphs：支持ISO C三字符组</span><br></pre></td></tr></table></figure>

<p><em><strong>优化选项</strong></em></p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"><span class="deletion">-O0：关闭所有优化选项</span></span><br><span class="line"><span class="deletion">-O1：第一级别优化，使用此选项可使可执行文件更小、运行更快，并不会增加太多编译时间，可以简写为-O</span></span><br><span class="line"><span class="deletion">-O2：第二级别优化，采用了几乎所有的优化技术，使用此选项会延长编译时间</span></span><br><span class="line"><span class="deletion">-O3：第三级别优化，在-O2的基础上增加了产生inline函数、使用寄存器等优化技术</span></span><br><span class="line"><span class="deletion">-Os：此选项类似于-O2，作用是优化所占用的空间，但不会进行性能优化，常用于生成最终版本</span></span><br></pre></td></tr></table></figure>

<p><em><strong>自定义扩展名</strong></em></p>
<p>-x：使用此选项可以指定自定义的源文件扩展名，类型有c、c-header、cpp-output、assembler、assembler-with-cpp、none</p>
<p>凡-x后面所列的所有文件都会被视为其指定的类型，要想改变类型可以再一次使用-x选项，或者使用-x none回到默认设置</p>
<p>示例：gcc -o test test.c -x assembler test.asm -x c test2.c</p>
]]></content>
      <tags>
        <tag>C</tag>
        <tag>GCC</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机网络面试</title>
    <url>/2021/12/02/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E9%9D%A2%E8%AF%95/</url>
    <content><![CDATA[<p>OSI:物理层,数据链路层,网络层,传输层,会话层,表示层和应用层</p>
<p>TCP/IP:数据链路层,网络层,传输层,应用层</p>
<p>TCP与UDP区别：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>、基于连接与无连接；</span><br><span class="line"><span class="number">2</span>、对系统资源的要求（TCP较多，UDP少）；</span><br><span class="line"><span class="number">3</span>、UDP程序结构较简单；</span><br><span class="line"><span class="number">4</span>、流模式与数据报模式 ；</span><br><span class="line"><span class="number">5</span>、TCP保证数据正确性，UDP可能丢包；</span><br><span class="line"><span class="number">6</span>、TCP保证数据顺序，UDP不保证。</span><br></pre></td></tr></table></figure>

<h3 id="Reno：一种TCP拥塞控制算法"><a href="#Reno：一种TCP拥塞控制算法" class="headerlink" title="Reno：一种TCP拥塞控制算法"></a><strong>Reno：一种TCP拥塞控制算法</strong></h3><p><strong><img src="https://img2020.cnblogs.com/blog/1943620/202111/1943620-20211118190939824-2142245455.png" alt="img"></strong></p>
<p>Reno 被许多教材（例如：《计算机网络——自顶向下的方法》）所介绍，适用于低延时、低带宽的网络，它将拥塞控制的过程分为四个阶段：<strong>慢启动、拥塞避免、快重传和快恢复</strong>，对应的状态如下所示：</p>
<ul>
<li>慢启动阶段思路是不要一开始就发送大量的数据，先探测一下网络的拥塞程度，也就是说由小到大逐渐增加拥塞窗口的大小，在没有出现丢包时每收到一个 ACK 就将拥塞窗口大小加一（单位是 MSS，最大单个报文段长度），每轮次发送窗口增加一倍，呈指数增长，若出现丢包，则将拥塞窗口减半，进入拥塞避免阶段；</li>
<li>当窗口达到慢启动阈值或出现丢包时，进入拥塞避免阶段，窗口每轮次加一，呈线性增长；当收到对一个报文的三个重复的 ACK 时，认为这个报文的下一个报文丢失了，进入快重传阶段，要求接收方在收到一个失序的报文段后就立即发出重复确认（为的是使发送方及早知道有报文段没有到达对方，可提高网络吞吐量约20%）而不要等到自己发送数据时捎带确认；</li>
<li>快重传完成后进入快恢复阶段，将慢启动阈值修改为当前拥塞窗口值的一半，同时拥塞窗口值等于慢启动阈值，然后进入拥塞避免阶段，重复上述过程。</li>
</ul>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>目前有非常多的 TCP 的拥塞控制协议，例如：</p>
<ul>
<li><strong>基于丢包的拥塞控制</strong>：将丢包视为出现拥塞，采取缓慢探测的方式，逐渐增大拥塞窗口，当出现丢包时，将拥塞窗口减小，如 Reno、Cubic 等。</li>
<li><strong>基于时延的拥塞控制</strong>：将时延增加视为出现拥塞，延时增加时增大拥塞窗口，延时减小时减小拥塞窗口，如 Vegas、FastTCP 等。</li>
<li><strong>基于链路容量的拥塞控制</strong>：实时测量网络带宽和时延，认为网络上报文总量大于带宽时延乘积时出现了拥塞，如 BBR。</li>
<li><strong>基于学习的拥塞控制</strong>：没有特定的拥塞信号，而是借助评价函数，基于训练数据，使用机器学习的方法形成一个控制策略，如 Remy。</li>
</ul>
]]></content>
      <tags>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>进程间通信方式</title>
    <url>/2021/12/02/%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<p><strong>1、无名管道通信</strong></p>
<p>　　无名管道（ pipe ）：管道是一种半双工的通信方式，数据只能单向流动，而且只能在具有亲缘关系的进程间使用。进程的亲缘关系通常是指父子进程关系。</p>
<p><strong>2、有名管道通信</strong></p>
<p>　　有名管道 （named pipe） ： 有名管道也是半双工的通信方式，但是它允许无亲缘关系进程间的通信。</p>
<p><strong>3、消息队列通信</strong></p>
<p>　　消息队列（ message queue ） ： 消息队列是由消息的链表，存放在内核中并由消息队列标识符标识。消息队列克服了信号传递信息少、管道只能承载无格式字节流以及缓冲区大小受限等缺点。</p>
<p><strong>4、信号量通信</strong></p>
<p>　　信号量（ semophore ） ： 信号量是一个计数器，可以用来控制多个进程对共享资源的访问。它常作为一种锁机制，防止某进程正在访问共享资源时，其他进程也访问该资源。因此，主要作为进程间以及同一进程内不同线程之间的同步手段。</p>
<p><strong>5、信号</strong></p>
<p>　　信号 （ sinal ） ： 信号是一种比较复杂的通信方式，用于通知接收进程某个事件已经发生。</p>
<p><strong>6、共享内存通信</strong></p>
<p>　　共享内存（ shared memory ） ：共享内存就是映射一段能被其他进程所访问的内存，这段共享内存由一个进程创建，但多个进程都可以访问。共享内存是最快的 IPC 方式，它是针对其他进程间通信方式运行效率低而专门设计的。它往往与其他通信机制，如信号量配合使用，来实现进程间的同步和通信。</p>
<p><strong>7、套接字通信</strong></p>
<p>套接字（ socket ） ： 套接口也是一种进程间通信机制，与其他通信机制不同的是，它可用于不同机器间的进程通信。</p>
<p><strong>各通信方式的比较和优缺点：</strong></p>
<ol>
<li>管道：速度慢，容量有限，<strong>只有父子进程能通讯</strong></li>
<li>有名管道：任何进程间都能通讯，但速度慢</li>
<li>消息队列： 容量受到系统限制，且要注意第一次读的时候，要考虑上一次没有读完数据的问题，消息队列可以不再局限于父子进程，而允许任意进程通过共享消息队列来实现进程间通信，并由系统调用函数来实现消息发送和接收之间的同步，从而使得用户在使用消息缓冲进行通信时不再需要考虑同步问题，使用方便，但是信息的复制需要额外消耗CPU的时间，不适宜于信息量大或操作频繁的场合。<strong>此种方法不太常用</strong></li>
<li>信号量： 不能用来传递复杂消息，只能用来同步</li>
<li>共享内存：利用内存缓冲区直接交换信息，无须复制，快捷、信息量大是其优点。共享内存块提供了在任意数量的进程之间进行高效双向通信的机制。每个使用者都可以读取写入数据，但是所有程序之间必须达成并遵守一定的协议，以防止诸如在读取信息之前覆写内存空间等竞争状态的出现。</li>
</ol>
<p><strong>进程间通信方式的选择</strong></p>
<ul>
<li>PIPE和FIFO(有名管道)用来实现进程间相互发送非常短小的、频率很高的消息，<strong>这两种方式通常适用于两个进程间的通信</strong></li>
<li>共享内存用来实现进程间共享的、非常庞大的、读写操作频率很高的数据；这种方法适用于多进程间的通信</li>
<li>其他考虑用socket。主要应用在<strong>分布式开发中</strong></li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>重定位知识点总结</title>
    <url>/2021/12/01/%E9%87%8D%E5%AE%9A%E4%BD%8D%E7%9F%A5%E8%AF%86%E7%82%B9%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p><strong>什么是重定位？为什么需要重定位？</strong></p>
<h3 id="一、必须知道的几个概念。"><a href="#一、必须知道的几个概念。" class="headerlink" title="一、必须知道的几个概念。"></a><strong>一、必须知道的几个概念。</strong></h3><p><strong>1、链接地址和运行地址。</strong></p>
<p>a.运行地址，顾名思义就是程序运行的时候的地址，也就是你用工具将代码下载到RAM的那个地址，也叫加载地址。</p>
<p>b.链接地址，由链接脚本指定的地址。为什么需要链接脚本指定地址呢？你想一下，在c语言编程中，当我们需要调用一个A函数的时候，编译器是怎么找到这个A函数？编译器肯定是知道它被放在哪里才可以找到它。那就是链接脚本的作用，链接脚本其实在程序被执行之前都已经指定A函数一个地址编号，以后所有的函数调用我们都会去这个地址编号那里寻找A函数。有点类似于c语言的指针变量。</p>
<p><strong>2、位置有关码与位置无关码。</strong></p>
<p>a.位置有关码，就是这句代码的执行正确与否还需要取决于当前的地址，也就是说跟地址已经绑定了的，例如：ldr PC, _main，就是PC指针必须跳转到_main（函数名就是一个地址）这个地址去，代码执行成功与否就相当于受到了这个地址的约束，假如这个地址的内容不存放_main这个函数，就会出错了。</p>
<p>b.位置无关码，就是这句代码在哪里运行都可以的，跟所处的地址无关，跟位置有关码相反。</p>
<h3 id="二、重定位需要理解的一些问题。"><a href="#二、重定位需要理解的一些问题。" class="headerlink" title="二、重定位需要理解的一些问题。"></a><strong>二、重定位需要理解的一些问题。</strong></h3><p><strong>1、链接地址跟运行地址不同的情况下会出现什么情况？</strong></p>
<p>答：以上面举的函数A为例，当链接地址跟运行地址不同的时候，假如链接地址是0x1000，运行地址（加载地址）是0x0000，链接脚本指定函数A将来是要存放到（基地址+偏移量）=0x1000+0x0001=0x1001地址的，但是程序在下载的时候却把这个程序下载到0x0000，所以函数A的地址实际上是存放在（基地址+偏移量）=0x0000+0x0001=0x0001这个地址的。当程序运行到一行位置有关码例如：ldr PC, A ，编译器首先就会按照链接脚本指定的A的那个地址0x1001寻找A函数，但是因为加载地址跟链接地址不同的原因，实际上A函数已经被放到了0x0001，所以执行就会出错。所以，当这两个地址不同的时候，执行一段位置有关码的时候就会发生不可预估的错误。</p>
<p><strong>2、为什么会出现链接地址跟运行地址不同的情况？</strong></p>
<p>答：当一块芯片启动的时候，依靠内部的SRAM，可以运行一小段代码，而因为DDR还没初始化，注定了开始的运行地址是在内部SRAM中的。当我们需要运行一个操作系统，那么点的内存怎么够运行呢？所以这时候就需要初始化DDR才可，而因为我们知道这代码将来都是在DDR上面运行的，所以链接脚本指定的链接地址肯定是DDR上面的地址，所以这就出现了链接地址跟运行地址不同的情况了。</p>
<p><strong>3、什么是重定位？</strong></p>
<p>答：由于出现1这样的问题，就需要使用重定位这种方式解决上面的问题了。那什么是重定位呢？重定位就是在链接地址跟运行地址不同的情况下，执行一段位置无关码，这段位置无关码的作用就是将原来的那份代码全部复制到链接地址那里去，然后自己再长跳转到新的那份代码的刚刚执行的那个位置。这样就实现了链接地址跟运行地址一致的情况了。</p>
<p><strong>4、为什么需要重定位？</strong></p>
<p>答：就是链接地址跟运行地址不同，在这个情况下我们可以有两种方案：<br>a.全部使用位置无关码。<br>b.进行重定位让这两个地址相同。<br>我们知道，如果是一个小代码，使用a时可以的，但是一个大的代码文件很难保证全部都使用位置无关码的，这也是不现实的，所以必须使用重定位解决这个问题。</p>
]]></content>
      <tags>
        <tag>嵌入式</tag>
      </tags>
  </entry>
  <entry>
    <title>静态存储区、堆、栈区别</title>
    <url>/2021/12/16/%E9%9D%99%E6%80%81%E5%AD%98%E5%82%A8%E5%8C%BA%E3%80%81%E5%A0%86%E3%80%81%E6%A0%88%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h3 id="一、内存基本构成"><a href="#一、内存基本构成" class="headerlink" title="一、内存基本构成"></a>一、内存基本构成</h3><p>可编程内存在基本上分为这样的几大部分：<strong>静态存储区、堆区和栈区</strong>。他们的功能不同，对他们使用方式也就不同。</p>
<p>静态存储区：内存在程序编译的时候就已经分配好，这块内存在程序的整个运行期间都存在。它主要存放静态数据、全局数据和常量。</p>
<p>栈区：在执行函数时，函数内局部变量的存储单元都可以在栈上创建，函数执行结束时这些存储单元自动被释放。栈内存分配运算内置于处理器的指令集中，效率很高，但是分配的内存容量有限。</p>
<p>堆区：亦称动态内存分配。程序在运行的时候用malloc或new申请任意大小的内存，程序员自己负责在适当的时候用free或delete释放内存。动态内存的生存期可以由我们决定，如果我们不释放内存，程序将在最后才释放掉动态内存。 但是，良好的编程习惯是：如果某动态内存不再使用，需要将其释放掉，否则，我们认为发生了内存泄漏现象。</p>
<h3 id="二、三者之间的区别"><a href="#二、三者之间的区别" class="headerlink" title="二、三者之间的区别"></a>二、三者之间的区别</h3><p>我们通过代码段来看看对这样的三部分内存需要怎样的操作和不同，以及应该注意怎样的地方。</p>
<p>例一：静态存储区与栈区</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span>* p = “Hello World1”;</span><br><span class="line"><span class="keyword">char</span> a[] = “Hello World2”;</span><br><span class="line">p[<span class="number">2</span>] = ‘A’;</span><br><span class="line">a[<span class="number">2</span>] = ‘A’;</span><br><span class="line"><span class="keyword">char</span>* p1 = “Hello World1;”</span><br></pre></td></tr></table></figure>

<p>这个程序是有错误的，错误发生在p[2] = ‘A’这行代码处，为什么呢，是变量p和变量数组a都存在于栈区的（任何临时变量都是处于栈区的，包括在main（）函数中定义的变量）。但是，数据“Hello World1”和数据“Hello World2”是存储于不同的区域的。</p>
<p>因为数据“Hello World2”存在于数组中，所以，此数据存储于栈区，对它修改是没有任何问题的。因为指针变量p仅仅能够存储某个存储空间的地址，数据“Hello World1”为字符串常量，所以存储在静态存储区。虽然通过p[2]可以访问到静态存储区中的第三个数据单元，即字符‘l’所在的存储的单元。但是因为数据“Hello World1”为字符串常量，不可以改变，所以在程序运行时，会报告内存错误。并且，如果此时对p和p1输出的时候会发现p和p1里面保存的地址是完全相同的。换句话说，在数据区只保留一份相同的数据（见图1－1）。</p>
<p>例二：栈区与堆区</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">char</span>* <span class="title">f1</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="keyword">char</span>* p = <span class="literal">NULL</span>;</span><br><span class="line">  <span class="keyword">char</span> a;</span><br><span class="line">  p = &amp;a;</span><br><span class="line">  <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">char</span>* <span class="title">f2</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="keyword">char</span>* p = <span class="literal">NULL</span>:</span><br><span class="line">  p =(<span class="keyword">char</span>*) <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">4</span>];</span><br><span class="line">  <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这两个函数都是将某个存储空间的地址返回，二者有何区别呢？f1()函数虽然返回的是一个存储空间，但是此空间为临时空间。也就是说，此空间只有短暂的生命周期，它的生命周期在函数f1()调用结束时，也就失去了它的生命价值，即：此空间被释放掉。所以，当调用f1()函数时，如果程序中有下面的语句：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span>* p ;</span><br><span class="line">p = f1();</span><br><span class="line">*p = ‘a’;</span><br></pre></td></tr></table></figure>

<p>此时，编译并不会报告错误，但是在程序运行时，会发生异常错误。因为，你对不应该操作的内存（即，已经释放掉的存储空间）进行了操作。但是，相比之下，f2()函数不会有任何问题。因为，new这个命令是在堆中申请存储空间，一旦申请成功，除非你将其delete或者程序终结，这块内存将一直存在。也可以这样理解，堆内存是共享单元，能够被多个函数共同访问。如果你需要有多个数据返回却苦无办法，堆内存将是一个很好的选择。但是一定要避免下面的事情发生：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span></span>&#123;</span><br><span class="line">  …</span><br><span class="line">  <span class="keyword">char</span> * p;</span><br><span class="line">  p = (<span class="keyword">char</span>*)<span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">100</span>];</span><br><span class="line">  …</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个程序做了一件很<a href="https://www.baidu.com/s?wd=%E6%97%A0%E6%84%8F%E4%B9%89&tn=24004469_oem_dg&rsv_dl=gh_pl_sl_csd">无意义</a>并且会带来很大危害的事情。因为，虽然申请了堆内存，p保存了堆内存的首地址。但是，此变量是临时变量，当函数调用结束时p变量消失。也就是说，再也没有变量存储这块堆内存的首地址，我们将永远无法再使用那块堆内存了。但是，这块堆内存却一直标识被你所使用（因为没有到程序结束，你也没有将其delete，所以这块堆内存一直被标识拥有者是当前您的程序），进而其他进程或程序无法使用。我们将这种不道德的“流氓行为”（我们不用，却也不让别人使用）称为内存泄漏。这是我们C++程序员的大忌！！请大家一定要避免这件事情的发生。</p>
<p>总之，对于堆区、栈区和静态存储区它们之间最大的不同在于，栈的生命周期很短暂。但是堆区和静态存储区的生命周期相当于与程序的生命同时存在（如果您不在程序运行中间将堆内存delete的话），我们将这种变量或数据成为全局变量或数据。但是，对于堆区的内存空间使用更加灵活，因为它允许你在不需要它的时候，随时将它释放掉，而静态存储区将一直存在于程序的整个生命周期中。</p>
<p>希望大家记住下面的规则：</p>
<ol>
<li>用malloc 或new 申请内存之后，应该立即检查指针值是否为NULL。防止使用指针值为NULL 的内存。</li>
<li>不要忘记为数组和动态内存赋初值。防止将未被初始化的内存作为右值使用。</li>
<li>避免数组或指针的下标越界，特别要当心发生“多1”或者“少1”操作。</li>
<li>动态内存的申请与释放必须配对，防止内存泄漏。</li>
<li>用free 或delete 释放了内存之后，立即将指针设置为NULL，防止产生“野指针”。</li>
</ol>
<h3 id="三、堆与栈的讨论："><a href="#三、堆与栈的讨论：" class="headerlink" title="三、堆与栈的讨论："></a>三、堆与栈的讨论：</h3><h4 id="管理方式："><a href="#管理方式：" class="headerlink" title="管理方式："></a>管理方式：</h4><p>  堆中资源由程序员控制（容易产生memory leak）。<br>  栈资源由编译器自动管理，无需手工控制。</p>
<h4 id="系统响应："><a href="#系统响应：" class="headerlink" title="系统响应："></a>系统响应：</h4><p>  对于堆，应知道系统有一个记录空闲内存地址的链表，当系统收到程序申请时，遍历该链表，寻找第一个空间大于申请空间的堆结点，删除空闲结点链表中的该结点，并将该结点空间分配给程序（大多数系统会在这块内存空间首地址记录本次分配的大小，这样delete才能正确释放本内存空间，另外系统会将多余的部分重新放入空闲链表中）。<br>  对于栈，只要栈的剩余空间大于所申请空间，系统为程序提供内存，否则报异常提示栈溢出。</p>
<h4 id="空间大小："><a href="#空间大小：" class="headerlink" title="空间大小："></a>空间大小：</h4><p>  堆是不连续的内存区域（因为系统是用链表来存储空闲内存地址，自然不是连续的），堆大小受限于计算机系统中有效的虚拟内存（32bit系统理论上是4G），所以堆的空间比较灵活，比较大。<br>  栈是一块连续的内存区域，大小是操作系统预定好的，windows下栈大小是2M（也有是1M，在编译时确定，VC中可设置）。</p>
<h4 id="碎片问题："><a href="#碎片问题：" class="headerlink" title="碎片问题："></a>碎片问题：</h4><p>  对于堆，频繁的new/delete会造成大量碎片，使程序效率降低。<br>  对于栈，它是一个先进后出的队列，进出一一对应，不会产生碎片。</p>
<h4 id="生长方向："><a href="#生长方向：" class="headerlink" title="生长方向："></a>生长方向：</h4><p>  堆向上，向高地址方向增长。<br>  栈向下，向低地址方向增长。</p>
<h4 id="分配方式："><a href="#分配方式：" class="headerlink" title="分配方式："></a>分配方式：</h4><p>  堆都是动态分配（没有静态分配的堆）。<br>  栈有静态分配和动态分配，静态分配由编译器完成（如局部变量分配），动态分配由alloca函数分配，但栈的动态分配的资源由编译器进行释放，无需程序员实现。</p>
<h4 id="分配效率："><a href="#分配效率：" class="headerlink" title="分配效率："></a>分配效率：</h4><p>  堆由C/C++函数库提供，机制很复杂。所以堆的效率比栈低很多。<br>  栈是极其系统提供的数据结构，计算机在底层对栈提供支持，分配专门寄存器存放栈地址，栈操作有专门指令</p>
]]></content>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
</search>
