<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>c编译器预定义宏</title>
    <url>/2021/11/30/c%E7%BC%96%E8%AF%91%E5%99%A8%E9%A2%84%E5%AE%9A%E4%B9%89%E5%AE%8F/</url>
    <content><![CDATA[<p>为了方便处理一些有用的信息，预处理器定义了一些预处理标识符，也就是预定义宏。预定义宏的名称都是以&ldquo;__&rdquo;（两条下划线）开头和结尾的，如果宏名是由两个单词组成，那么中间以&ldquo;_&rdquo;（一条下划线）进行连接。并且，宏名称一般都由大写字符组成。<br />在日常项目编程中，预定义宏尤其对多目标平台代码的编写通常具有重大意义。通过预定义宏，程序员使用&ldquo;#ifdef&rdquo;与&ldquo;#endif&rdquo;等预处理指令，就可使平台相关代码只在适合于当前平台的代码上编译，从而在同一套代码中完成对多平台的支持。从这个意义上讲，平台信息相关的宏越丰富，代码的多平台支持越准确。<br />标准 C 语言提供的一些标准预定义宏如表 1 所示。</p>
<table><caption>表 1 常用的标准预定义宏</caption>
<tbody>
<tr>
<th>宏</th>
<th>描&nbsp;述</th>
</tr>
<tr>
<td>__DATE__</td>
<td>丐前源文件的编泽口期，用&nbsp;&ldquo;Mmm dd yyy&rdquo;形式的字符串常量表示</td>
</tr>
<tr>
<td>__FILE__</td>
<td>当前源文件的名称，用字符串常量表示</td>
</tr>
<tr>
<td>__LINE__</td>
<td>当前源义件中的行号，用十进制整数常量表示，它可以随#line指令改变</td>
</tr>
<tr>
<td>__TIME__</td>
<td>当前源文件的最新编译吋间，用&ldquo;hh:mm:ss&rdquo;形式的宁符串常量表示</td>
</tr>
<tr>
<td>__STDC__</td>
<td>如果今前编泽器符合ISO标准，那么该宏的值为1，否则未定义</td>
</tr>
<tr>
<td>__STDC_VERSION__</td>
<td>如果当前编译器符合C89，那么它被定义为199409L;如果符合C99，那么它被定义为199901L:在其他情况下，该宏为宋定义</td>
</tr>
<tr>
<td>__STDC_HOSTED__</td>
<td>(C99)如果当前是宿主系统，则该宏的值为1;如果当前是独立系统，则该宏的值为0</td>
</tr>
<tr>
<td>__STDC_IEC_559_</td>
<td>(C99)如果浮点数的实现符合IEC 60559标准时，则该宏的值为1，否则为未定义</td>
</tr>
<tr>
<td>__STDC_IEC_559_COMPLEX__</td>
<td>(C99)如果复数运算实现符合IEC60559标准时，则该宏的伉为1,否则为未定义</td>
</tr>
<tr>
<td>__STDC_ISO_10646__</td>
<td>(C99 )定义为长整型常量，yyyymmL表示wchai_t值遵循ISO 10646标准及其指定年月的修订补充，否则该宏为未定义</td>
</tr>
</tbody>
</table>
<p>除标准 C 语言提供的标准宏之外，各种编译器也都提供了自己的自定义预定义宏。可以通过表 2 所示的指令来查看不同编译器对预定义宏的支持情况。</p>
<table><caption>表 2 不同编译器的预定义宏查看指令</caption>
<tbody>
<tr>
<th>编译器</th>
<th>宏指令（c)</th>
<th>宏指令（<a href="http://c.biancheng.net/cplus/" target="_blank" rel="noopener">C++</a>)</th>
</tr>
<tr>
<td>Clang/LLVM</td>
<td>clang -dM -E -x c /dev/null</td>
<td>clang++ -dM -E -x C++ /dev/null</td>
</tr>
<tr>
<td>GNU&nbsp;<a href="http://c.biancheng.net/gcc/" target="_blank" rel="noopener">GCC</a>/G++</td>
<td>gcc -dM -E -x c /dev/null</td>
<td>g++ -dM -E -x&nbsp;C++&nbsp;/dev/null</td>
</tr>
<tr>
<td>Hewlett-Packard C/aC++</td>
<td>cc -dM -E -x c /clev/null</td>
<td>aCC -dM -E -x&nbsp;C++&nbsp;/dev/null</td>
</tr>
<tr>
<td>IBM XL C/C++</td>
<td>xlc -qshowmacros -E /dev/null</td>
<td>xlc++ -qshowmacros -E /dev/null</td>
</tr>
<tr>
<td>Intel ICC7ICPC</td>
<td>icc -dM -E -x c /dev/null</td>
<td>icpc -dM -E -x&nbsp;C++&nbsp;/dev/null</td>
</tr>
<tr>
<td>Oracle Solaris Studio</td>
<td>cc -xduinpmacros -E /dev/null</td>
<td>CC -xduinpmacros -E /clev/null</td>
</tr>
<tr>
<td>Portland Group PGCC/PGCPP</td>
<td>pgcc -dM -E</td>
<td>&nbsp;</td>
</tr>
</tbody>
</table>
<p>对于这些预定义宏的应用，基本上随处可见，下面举例介绍。<br />利用&ldquo;__DATE__&rdquo;和&ldquo;__TIME__&rdquo;宏可以用来确定程序编译的时间。如下面的示例代码所示：</p>
<div class="snippet-container">
<div class="sh_bright snippet-wrap">
<pre class="language-c"><code>int main (void)
&#123;
  printf("Copyright (c) Powered by www.develhome.com\n");
  printf("Compiled on %s at %s\n", __DATE__,__TIME__);
  return 0;
&#125;</code></pre>
</div>
</div>
<p>利用&ldquo;__STDC__&rdquo;与&ldquo;__STDC_VERSION__&rdquo;宏可以编写那些需要兼容标准 C 和非标准 C 编译器的程序，如下面的示例代码所示：</p>
<div class="snippet-container">
<div class="sh_bright snippet-wrap">
<pre class="language-c"><code>#ifdef __STDC__
/* Some version of standard C */
#if defined(__STDC__VERSION__)&amp;&amp;__STDC_VERSION__&gt;=199901L
/* C99 */
#elif defined(__STDC_VERSION__)&amp;&amp;__STDC_VERSION__&gt;=199409L
/* C89 and amendment 1 */
#else
/* C89 but not amendment 1*/
#endif
#else /* __STDC__not defined */
/*Not Standard C*/
#endif</code></pre>
</div>
</div>
<p>利用__FILE__、__LINE__与__FUNCTION__（或者__func__）预定义宏的组合，在调试程序的时候可以很简单地在程序运行期进行异常跟踪。如下面的示例代码所示：</p>
<div class="snippet-container">
<div class="sh_bright snippet-wrap">
<pre class="language-c"><code>#include &lt;stdio.h&gt;
#include &lt;sys/types.h&gt;
#include &lt;sys/stat.h&gt;
#include &lt;fcntl.h&gt;
#include &lt;unistd.h&gt;
#include &lt;stdlib.h&gt;
#define MESSAGE(message,assertion) \
do&#123;\
  if(!(assertion))&#123;\
    printf("line %d in %s(%s)", __LINE__, __FILE__,__FUNCTION__);\
    if(message)&#123;\
      printf(":%s",message);\
    &#125;\
    printf("\n");\
    abort();\
  &#125;\
&#125;while(0)
int OpenFile(const char *filename)
&#123;
  int fd;
  MESSAGE("文件名称不能够为空",filename);
  MESSAGE("文件不存在",0==access(filename,F_OK));
  fd = open(filename,O_RDONLY);
  close(fd);
  return 0;
&#125;
int main(int argc,char **argv)
&#123;
  MESSAGE("命令参数不能够为空",argc==2);
  OpenFile(argv[1]);
  return 0;
&#125;</code></pre>
</div>
</div>
<p>最后还需要注意的是，如果用户重定义&ldquo;#define&ldquo;或取消了&ldquo;#undef&rdquo;预定义宏，那么其结果是&ldquo;未定义&rdquo;的。因此，在代码编写中，应该尽量避免自定义宏与预定义宏名称相同的情况发生。</p>]]></content>
      <tags>
        <tag>C language</tag>
      </tags>
  </entry>
  <entry>
    <title>#define do{...}while(0)形式的意义</title>
    <url>/2021/11/30/define-do-while-0-%E5%BD%A2%E5%BC%8F%E7%9A%84%E6%84%8F%E4%B9%89/</url>
    <content><![CDATA[<p data-diff-id="ct-diff-id-D9Htsp8w" data-pm-slice="0 0 []">#define do{...}while(0)这种奇怪形式的宏定义经常在实际工程中应用，它的意义如下：</p>
<h3 data-diff-id="ct-diff-id-PHbppXn7"><strong>1.增加代码的适应性</strong></h3>
<p data-diff-id="ct-diff-id-zIVlUqma">下面的宏定义没有使用do{...}while(0)</p>
<pre class="language-c"><code>#define FOO(x) foo(x); bar(x);</code></pre>
<p data-diff-id="ct-diff-id-zCsP8Swy">这样宏定义，单独调用不会出现问题，例如：</p>
<pre class="language-c"><code>FOO(100)</code></pre>
<p data-diff-id="ct-diff-id-rENV22QO">宏扩展后变成：</p>
<pre class="language-c"><code>foo(x);bar(x);</code></pre>
<p data-diff-id="ct-diff-id-Mui25CcR">这样调用FOO没有任何问题，但是FOO（x）不能放入控制语句中，例如：</p>
<pre class="language-c"><code>if (condition) FOO(x); 
else ...;</code></pre>
<p data-diff-id="ct-diff-id-HxmHc3sk">经过宏扩展后，变成了</p>
<pre class="language-c"><code>if (condition) 
    foo(x);
    bar(x);
else ...;</code></pre>
<p data-diff-id="ct-diff-id-bizWf1Js">这样就导致了<strong>语法错误</strong>，语法错误并不可怕，在编译阶段就能发现，更致命的是他有可能导致<strong>逻辑错误，</strong>例如：</p>
<pre class="language-c"><code>if (condition)
    FOO(x);</code></pre>
<p data-diff-id="ct-diff-id-154odpIM">这段代码经过扩展后变成：</p>
<pre class="language-c"><code>if (condition)
    foo(x); bar(x);</code></pre>
<p data-diff-id="ct-diff-id-Rq84tLNO">这样一来，无论condition是true还是false，bar(x)都会被调用。</p>
<p data-diff-id="ct-diff-id-bZLCR6Yl">这时候do{...}while(0)的价值就体现出来了，修改一下FOO的定义</p>
<pre class="language-c"><code>#define FOO(x) do &#123;
        foo(x); 
        bar(x); 
    &#125; while (0)</code></pre>
<p data-diff-id="ct-diff-id-MxlXgo18">这样FOO,放入控制语句中就没有问题了。</p>
<p data-diff-id="ct-diff-id-K42E77MY">也许有人说：把foo(x);bar(x)用大括号括起来不就行了吗？比如这样定义：</p>
<pre class="language-c"><code>#define FOO(x)&#123;
                foo(x); 
                bar(x); 
              &#125;</code></pre>
<p data-diff-id="ct-diff-id-j02IpSa7">再看下面代码：</p>
<pre class="language-c"><code>if (condition) 
    FOO(x); 
else ...;</code></pre>
<p data-diff-id="ct-diff-id-ZaXoBAw3">扩展后：</p>
<pre class="language-c"><code>if (condition)
&#123;
    foo(x);
    bar(x);
&#125; ; //注意最后这个分号，语法错误 else ...;</code></pre>
<p data-diff-id="ct-diff-id-bOT9Tu2A">照样语法错误；</p>
<h3 data-diff-id="ct-diff-id-UJJjaV3j"><strong>2.增加代码的扩展性</strong></h3>
<p data-diff-id="ct-diff-id-kY1xHXWo">我理解的扩展性，主要是宏定义中还可以引用其他宏，比如：</p>
<pre class="language-c"><code>#define FOO(x) do&#123;
    OTHER_FOO(x)
&#125; while(0)</code></pre>
<p data-diff-id="ct-diff-id-E95GUq0W">这样我们不用管OTHER_FOO是但语句还是符合语句，都不会出现问题</p>
<h3 data-diff-id="ct-diff-id-Ok6ehnk3"><strong>3.增加代码的灵活性</strong></h3>
<p data-diff-id="ct-diff-id-jFczi2Tc">&nbsp;灵活性主要体现在，我们可以从宏中break出来，例如下面的定义：</p>
<pre class="language-c"><code>#define FOO(x)  do&#123; \
    foo(x);  \
    if(condition(x)) \
        break; \
    bar(x) \
    ..... \&#125; while(0)</code></pre>]]></content>
      <tags>
        <tag>C language</tag>
      </tags>
  </entry>
  <entry>
    <title>macos新版本系统brew install报错问题</title>
    <url>/2021/11/30/macos%E6%96%B0%E7%89%88%E6%9C%AC%E7%B3%BB%E7%BB%9Fbrew-install%E6%8A%A5%E9%94%99%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<div class="cnblogs_code">
<pre>Warning: Bottle missing, falling back to the default domain...</pre>
</div>
<div class="cnblogs_code">
<pre><span style="color: #0000ff;">tar</span>: Error opening archive: Failed to open <span style="color: #800000;">'</span><span style="color: #800000;">/Users/zupernova/Library/Caches/Homebrew/downloads/551f73f417594bd810c1d61e1cacd24de3cc3e4cce825f0b9f763c0e0d32d84d--gdbm-1.20.big_sur.bottle.tar.gz</span><span style="color: #800000;">'</span><span style="color: #000000;">
Error: Failure </span><span style="color: #0000ff;">while</span> executing; `<span style="color: #0000ff;">tar</span> --extract --no-same-owner --<span style="color: #0000ff;">file</span> /Users/zupernova/Library/Caches/Homebrew/downloads/551f73f417594bd810c1d61e1cacd24de3cc3e4cce825f0b9f763c0e0d32d84d--gdbm-<span style="color: #800080;">1.20</span>.big_sur.bottle.<span style="color: #0000ff;">tar</span>.gz --directory /private/tmp/d20210728-<span style="color: #800080;">71473</span>-bqhpdw` exited with <span style="color: #800080;">1</span>. Here<span style="color: #800000;">'</span><span style="color: #800000;">s the output:</span>
<span style="color: #0000ff;">tar</span>: Error opening archive: Failed to open <span style="color: #800000;">'</span><span style="color: #800000;">/Users/zupernova/Library/Caches/Homebrew/downloads/551f73f417594bd810c1d61e1cacd24de3cc3e4cce825f0b9f763c0e0d32d84d--gdbm-1.20.big_sur.bottle.tar.gz</span><span style="color: #800000;">'</span></pre>
</div>
<h3 id="id-解决方案" class="ct-heading" data-diff-id="ct-diff-id-1gpbd8W7">解决方案</h3>
<p data-diff-id="ct-diff-id-c3Xy0kEA">更改环境变量，将HOMEBREW_BOTTLE_DOMAIN改正确即可</p>
<div class="cnblogs_code">
<pre><span style="color: #0000ff;">echo</span> <span style="color: #800000;">'</span><span style="color: #800000;">export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles/bottles</span><span style="color: #800000;">'</span> &gt;&gt; ~/<span style="color: #000000;">.zshrc
source </span>~/.zshrc</pre>
</div>
<p data-diff-id="ct-diff-id-c3Xy0kEA"><span style="font-family: 'PingFang SC', 'Helvetica Neue', Helvetica, Arial, sans-serif; font-size: 14px;">此前brew已经换源为中科大源</span></p>]]></content>
      <tags>
        <tag>开发小tips</tag>
      </tags>
  </entry>
  <entry>
    <title>嵌入式经典面试题</title>
    <url>/2021/11/30/%E5%B5%8C%E5%85%A5%E5%BC%8F%E7%BB%8F%E5%85%B8%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    <content><![CDATA[<h2>预处理器（Preprocessor）</span></h2>
<h4>1.用预处理指令#define 声明一个常数，用以表明1年中有多少秒（忽略闰年问题）</span></h4>
<pre class="language-c"><code>#define SECONDS_PER_YEAR (60UL * 60 * 24 * 365)</code></pre>
我在这想看到几件事情：</span></p>
1). #define 语法的基本知识（例如：不能以分号结束，括号的使用，等等）</span></p>
2). 懂得预处理器将为你计算常数表达式的值，因此，直接写出你是如何计算一年中有多少秒而不是计算出实际的值，是更清晰而没有代价的。</span></p>
3). 意识到这个表达式将使一个16位机的整型数溢出-因此要用到长整型符号L,告诉编译器这个常数是的长整型数。</span></p>
4). 如果你在你的表达式中用到UL（表示无符号长整型），那么你有了一个好的起点。记住，第一印象很重要。</span></p>
<h4>2.写一个&ldquo;标准&rdquo;宏MIN，这个宏输入两个参数并返回较小的一个。</span></h4>
<pre class="language-c"><code>&nbsp;#define MIN(A,B) ((A) &lt;= (B) ?(A) :(B))</code></pre>
C/C++试题集 C/C++ Development这个测试是为下面的目的而设的：</span></p>
1). 标识#define在宏中应用的基本知识。这是很重要的，因为直到嵌入(inline)操作符变为标准C的一部分，宏是方便产生嵌入代码的唯一方法，对于嵌入式系统来说，为了能达到要求的性能，嵌入代码经常是必须的方法。</span></p>
2). 三重条件操作符的知识。这个操作符存在C语言中的原因是它使得编译器能产生比if-then-else更优化的代码，了解这个用法是很重要的。</span></p>
3). 懂得在宏中小心地把参数用括号括起来</span></p>
4). 我也用这个问题开始讨论宏的副作用，例如：当你写下面的代码时会发生什么事？</span></p>
<pre class="language-c"><code>least = MIN(*p++, b);</code></pre>
<h4>3.预处理器标识#error的目的是什么？</span></h4>
如果你不知道答案，请看参考文献1。这问题对区分一个正常的伙计和一个书呆子是很有用的。只有书呆子才会读C语言课本的附录去找出象这种问题的答案。当然如果你不是在找一个书呆子，那么应试者最好希望自己不要知道答案。</span></p>
<h2>死循环（Infinite loops）</span></h2>
<h4>4.嵌入式系统中经常要用到无限循环，你怎么样用C编写死循环呢？</span></h4>
这个问题用几个解决方案。我首选的方案是：</span></p>
<pre class="language-c"><code>while(1) &#123; &#125;</code></pre>
一些程序员更喜欢如下方案：</span></p>
<pre class="language-c"><code>for(;;) &#123; &#125;</code></pre>
这个实现方式让我为难，因为这个语法没有确切表达到底怎么回事。如果一个应试者给出这个作为方案，我将用这个作为一个机会去探究他们这样做的基本原理。如果他们的基本答案是：&ldquo;我被教着这样做，但从没有想到过为什么。&rdquo;这会给我留下一个坏印象。</span></p>
第三个方案是用&nbsp;goto</span></p>
<pre class="language-c"><code>Loop:
&hellip;
goto Loop;</code></pre>
应试者如给出上面的方案，这说明或者他是一个汇编语言程序员（这也许是好事）或者他是一个想进入新领域的BASIC/FORTRAN程序员。</span></p>
<h2>数据声明（Data declarations）</span></h2>
<h4>5.用变量a给出下面的定义</span></h4>
<pre class="language-c"><code>a) 一个整型数（An integer）
b) 一个指向整型数的指针（A pointer to an integer）
c) 一个指向指针的的指针，它指向的指针是指向一个整型数（A pointer to a pointer to an integer）
d) 一个有10个整型数的数组（An array of 10 integers）
e) 一个有10个指针的数组，该指针是指向一个整型数的（An array of 10 pointers to integers）
f) 一个指向有10个整型数数组的指针（A pointer to an array of 10 integers）
g) 一个指向函数的指针，该函数有一个整型参数并返回一个整型数（A pointer to a function that takes an integer as an argument and returns an integer）
h) 一个有10个指针的数组，该指针指向一个函数，该函数有一个整型参数并返回一个整型数（ An array of ten pointers to functions that take an integer argument and return an integer ）</code></pre>
答案是：</span></p>
<pre class="language-c"><code>a) int a; // An integer
b) int *a; // A pointer to an integer
c) int **a; // A pointer to a pointer to an integer
d) int a[10]; // An array of 10 integers
e) int *a[10]; // An array of 10 pointers to integers
f) int (*a)[10]; // A pointer to an array of 10 integers
g) int (*a)(int); // A pointer to a function a that takes an integer argument and returns an integer
h) int (*a[10])(int); // An array of 10 pointers to functions that take an integer argument and return an integer</code></pre>
人们经常声称这里有几个问题是那种要翻一下书才能回答的问题，我同意这种说法。当我写这篇文章时，为了确定语法的正确性，我的确查了一下书。</span></p>
但是当我被面试的时候，我期望被问到这个问题（或者相近的问题）。因为在被面试的这段时间里，我确定我知道这个问题的答案。应试者如果不知道</span></p>
所有的答案（或至少大部分答案），那么也就没有为这次面试做准备，如果该面试者没有为这次面试做准备，那么他又能为什么出准备呢？</span></p>
<h2>Static</span></h2>
<h4>6.关键字static的作用是什么？</span></h4>
这个简单的问题很少有人能回答完全。在C语言中，关键字static有三个明显的作用：</span></p>
1). 在函数体内，一个被声明为静态的变量在这一函数被调用过程中维持其值不变。</span></p>
2). 在模块内（但在函数体外），一个被声明为静态的变量可以被模块内所用函数访问，但不能被模块外其它函数访问。它是一个本地的全局变量。</span></p>
3). 在模块内，一个被声明为静态的函数只可被这一模块内的其它函数调用。那就是，这个函数被限制在声明它的模块的本地范围内使用。</span></p>
大多数应试者能正确回答第一部分，一部分能正确回答第二部分，同是很少的人能懂得第三部分。这是一个应试者的严重的缺点，因为他显然不懂得本地化数据和代码范围的好处和重要性。</span></p>
<h2>Const</span></h2>
<h4>7.关键字const是什么含意？</span></h4>
我只要一听到被面试者说：&ldquo;const意味着常数&rdquo;，我就知道我正在和一个业余者打交道。去年Dan Saks已经在他的文章里完全概括了const的所有用法，因此ESP(译者：Embedded Systems Programming)的每一位读者应该非常熟悉const能做什么和不能做什么.</span></p>
如果你从没有读到那篇文章，只要能说出const意味着&ldquo;只读&rdquo;就可以了。尽管这个答案不是完全的答案，但我接受它作为一个正确的答案。（如果你想知道更详细的答案，仔细读一下Saks的文章吧。）如果应试者能正确回答这个问题，我将问他一个附加的问题：下面的声明都是什么意思？</span></p>
<pre class="language-c"><code>const int a;
int const a;
const int *a;
int * const a;
int const * a const;</code></pre>
前两个的作用是一样，a是一个常整型数。第三个意味着a是一个指向常整型数的指针（也就是，整型数是不可修改的，但指针可以）。第四个意思a是一个指向整型数的常指针（也就是说，指针指向的整型数是可以修改的，但指针是不可修改的）。最后一个意味着a是一个指向常整型数的常指针（也就是说，指针指向的整型数是不可修改的，同时指针也是不可修改的）。如果应试者能正确回答这些问题，那么他就给我留下了一个好印象。顺带提一句，也许你可能会问，即使不用关键字const，也还是能很容易写出功能正确的程序，那么我为什么还要如此看重关键字const呢？我也如下的几下理由：</span></p>
1). 关键字const的作用是为给读你代码的人传达非常有用的信息，实际上，声明一个参数为常量是为了告诉了用户这个参数的应用目的。如果你曾花很多时间清理其它人留下的垃圾，你就会很快学会感谢这点多余的信息。（当然，懂得用const的程序员很少会留下的垃圾让别人来清理的。）</span></p>
2). 通过给优化器一些附加的信息，使用关键字const也许能产生更紧凑的代码。</span></p>
3). 合理地使用关键字const可以使编译器很自然地保护那些不希望被改变的参数，防止其被无意的代码修改。简而言之，这样可以减少bug的出现。</span></p>
<h2>Volatile</span></h2>
<h4>8.关键字volatile有什么含意 并给出三个不同的例子。（定义寄存器地址前面用volatile）</span></h4>
一个定义为volatile的变量是说这变量可能会被意想不到地改变，这样，编译器就不会去假设这个变量的值了。精确地说就是，优化器在用到这个变量时必须每次都小心地重新读取这个变量的值，而不是使用保存在寄存器里的备份。下面是volatile变量的几个例子：</span></p>
1). 并行设备的硬件寄存器（如：状态寄存器）</span></p>
2). 一个中断服务子程序中会访问到的非自动变量(Non-automatic variables)</span></p>
3). 多线程应用中被几个任务共享的变量</span></p>
回答不出这个问题的人是不会被雇佣的。我认为这是区分C程序员和嵌入式系统程序员的最基本的问题。嵌入式系统程序员经常同硬件、中断、RTOS等等打交道，所用这些都要求volatile变量。不懂得volatile内容将会带来灾难。</span></p>
假设被面试者正确地回答了这是问题（嗯，怀疑这否会是这样），我将稍微深究一下，看一下这家伙是不是直正懂得volatile完全的重要性。</span></p>
1). 一个参数既可以是const还可以是volatile吗？解释为什么。</span></p>
2). 一个指针可以是volatile 吗？解释为什么。</span></p>
3). 下面的函数有什么错误：</span></p>
<pre class="language-c"><code>int square(volatile int *ptr)&#123;
    return ptr *ptr;
&#125;</code></pre>
下面是答案：</span></p>
1). 是的。一个例子是只读的状态寄存器。它是volatile因为它可能被意想不到地改变。它是const因为程序不应该试图去修改它。</span></p>
2). 是的。尽管这并不很常见。一个例子是当一个中服务子程序修该一个指向一个buffer的指针时。</span></p>
3). 这段代码的有个恶作剧。这段代码的目的是用来返指针*ptr指向值的平方，但是，由于*ptr指向一个volatile型参数，编译器将产生类似下面的代码：</span></p>
<pre class="language-c"><code>int square(volatile int *ptr)&#123;
  int a, b;
  a = *ptr;
  b = *ptr;
  return a * b;
&#125;</code></pre>
由于*ptr的值可能被意想不到地该变，因此a和b可能是不同的。结果，这段代码可能返不是你所期望的平方值！正确的代码如下：</span></p>
<pre class="language-c"><code>long square(volatile int *ptr)&#123;
  int a;
  a = *ptr;
  return a * a;
&#125;</code></pre>
<h2>位操作（Bit manipulation）</span></h2>
<h4>9.嵌入式系统总是要用户对变量或寄存器进行位操作。给定一个整型变量a，写两段代码，第一个设置a的bit 3，第二个清除a 的bit 3。在以上两个操作中，要保持其它位不变。</span></h4>
对这个问题有三种基本的反应</span></p>
1). 不知道如何下手。该被面者从没做过任何嵌入式系统的工作。</span></p>
2). 用bit fields。Bit fields是被扔到C语言死角的东西，它保证你的代码在不同编译器之间是不可移植的，同时也保证了的你的代码是不可重用的。我最近不幸看到Infineon为其较复杂的通信芯片写的驱动程序，它用到了bit fields因此完全对我无用，因为我的编译器用其它的方式来实现bit fields的。从道德讲：永远不要让一个非嵌入式的家伙粘实际硬件的边。</span></p>
3). 用 #defines 和 bit masks 操作。这是一个有极高可移植性的方法，是应该被用到的方法。最佳的解决方案如下：</span></p>
<pre class="language-c"><code>#define BIT3 (0x1&lt;&lt;3)
static int a;
void set_bit3(void)&#123;
    a |= BIT3;
&#125;
void clear_bit3(void)&#123;
    a &amp;= ~BIT3;
&#125;</code></pre>
一些人喜欢为设置和清除值而定义一个掩码同时定义一些说明常数，这也是可以接受的。我希望看到几个要点：说明常数、|=和&amp;=~操作。</span></p>
<h2>访问固定的内存位置（Accessing fixed memory locations） C/C++ Development</span></h2>
<h4>10.嵌入式系统经常具有要求程序员去访问某特定的内存位置的特点。在某工程中，要求设置一绝对地址为0x67a9的整型变量的值为0xaa66。编译器是一个纯粹的ANSI编译器。写代码去完成这一任务。</span></h4>
这一问题测试你是否知道为了访问一绝对地址把一个整型数强制转换（typecast）为一指针是合法的。这一问题的实现方式随着个人风格不同而不同。典型的类似代码如下：</span></p>
<pre class="language-c"><code>int *ptr;
ptr = (int *)0x67a9;
*ptr = 0xaa55;</code></pre>
一个较晦涩的方法是：</span></p>
<pre class="language-c"><code>*(int * const)(0x67a9) = 0xaa55;</code></pre>
即使你的品味更接近第二种方案，但我建议你在面试时使用第一种方案。</span></p>
<h2>中断（Interrupts）</span></h2>
<h4>11.中断是嵌入式系统中重要的组成部分，这导致了很多编译开发商提供一种扩展&mdash;让标准C支持中断。具代表事实是，产生了一个新的关键字__interrupt。下面的代码就使用了__interrupt关键字去定义了一个中断服务子程序(ISR)，请评论一下这段代码的。</span></h4>
<pre class="language-c"><code>__interrupt double compute_area (double radius)&#123;
  double area = PI * radius * radius;
  printf(&rdquo; Area = %f&rdquo;, area);
  return area;
&#125;</code></pre>
这个函数有太多的错误了，以至让人不知从何说起了：</span></p>
1). ISR 不能返回一个值。如果你不懂这个，那么你不会被雇用的。</span></p>
2). ISR 不能传递参数。如果你没有看到这一点，你被雇用的机会等同第一项。</span></p>
3). 在许多的处理器/编译器中，浮点一般都是不可重入的。有些处理器/编译器需要让额处的寄存器入栈，有些处理器/编译器就是不允许在ISR中做浮点运算。此外，ISR应该是短而有效率的，在ISR中做浮点运算是不明智的。</span></p>
4). 与第三点一脉相承，printf()经常有重入和性能上的问题。如果你丢掉了第三和第四点，我不会太为难你的。不用说，如果你能得到后两点，那么你的被雇用前景越来越光明了。</span></p>
<h2>代码例子（Code examples）</span></h2>
<h4>12.下面的代码输出是什么，为什么？</span></h4>
<pre class="language-c"><code>void foo(void)&#123;
  unsigned int a = 6;
  int b = -20;
  (a+b &gt; 6) puts("&gt; 6") : puts("&lt;= 6");
&#125;</code></pre>
这个问题测试你是否懂得C语言中的整数自动转换原则，我发现有些开发者懂得极少这些东西。不管如何，这无符号整型问题的答案是输出是&ldquo;&gt;6&rdquo;。原因是当表达式中存在有符号类型和无符号类型时所有的操作数都自动转换为无符号类型。 因此-20变成了一个非常大的正整数，所以该表达式计算出的结果大于6。这一点对于应当频繁用到无符号数据类型的嵌入式系统来说是丰常重要的。如果你答错了这个问题，你也就到了得不到这份工作的边缘。</span></p>
<h4>13.评价下面的代码片断：</span></h4>
<pre class="language-c"><code>unsigned int zero = 0;
unsigned int compzero = 0xFFFF;
/*1's complement of zero */</code></pre>
对于一个int型不是16位的处理器为说，上面的代码是不正确的。应编写如下：</span></p>
<pre class="language-c"><code>unsigned int compzero = ~0;</code></pre>
这一问题真正能揭露出应试者是否懂得处理器字长的重要性。在我的经验里，好的嵌入式程序员非常准确地明白硬件的细节和它的局限，然而PC机程序往往把硬件作为一个无法避免的烦恼。</span></p>
到了这个阶段，应试者或者完全垂头丧气了或者信心满满志在必得。如果显然应试者不是很好，那么这个测试就在这里结束了。但如果显然应试者做得不错，那么我就扔出下面的追加问题，这些问题是比较难的，我想仅仅非常优秀的应试者能做得不错。提出这些问题，我希望更多看到应试者应付问题的方法，而不是答案。不管如何，你就当是这个娱乐吧&hellip;</span></p>
<h2>动态内存分配（Dynamic memory allocation）</span></h2>
<h4>14.尽管不像非嵌入式计算机那么常见，嵌入式系统还是有从堆（heap）中动态分配内存的过程的。那么嵌入式系统中，动态分配内存可能发生的问题是什么？</span></h4>
这里，我期望应试者能提到内存碎片，碎片收集的问题，变量的持行时间等等。这个主题已经在ESP杂志中被广泛地讨论过了（主要是 P.J. Plauger, 他的解释远远超过我这里能提到的任何解释），所有回过头看一下这些杂志吧！让应试者进入一种虚假的安全感觉后，我拿出这么一个小节目：下面的代码片段的输出是什么，为什么？</span></p>
<pre class="language-c"><code>char *ptr;
if ((ptr = (char *)malloc(0)) == NULL)
    puts(&ldquo;Got a null pointer&rdquo;);
else
    puts(&ldquo;Got a valid pointer&rdquo;);</code></pre>
这是一个有趣的问题。最近在我的一个同事不经意把0值传给了函数malloc，得到了一个合法的指针之后，我才想到这个问题。这就是上面的代码，该代码的输出是&ldquo;Got a valid pointer&rdquo;。我用这个来开始讨论这样的一问题，看看被面试者是否想到库例程这样做是正确。得到正确的答案固然重要，但解决问题的方法和你做决定的基本原理更重要些。</span></p>
<h2>Typedef</span></h2>
<h4>15.Typedef 在C语言中频繁用以声明一个已经存在的数据类型的同义字。也可以用预处理器做类似的事。例如，思考一下下面的例子：</span></h4>
<pre class="language-c"><code>#define dPS struct s *
typedef struct s * tPS;</code></pre>
以上两种情况的意图都是要定义dPS 和 tPS 作为一个指向结构s指针。哪种方法更好呢？（如果有的话）为什么？</span></p>
这是一个非常微妙的问题，任何人答对这个问题（正当的原因）是应当被恭喜的。答案是：typedef更好。思考下面的例子：</span></p>
<pre class="language-c"><code>dPS p1,p2;
tPS p3,p4;</code></pre>
第一个扩展为</span></p>
<pre class="language-c"><code>struct s * p1, p2;</code></pre>
上面的代码定义p1为一个指向结构的指，p2为一个实际的结构，这也许不是你想要的。第二个例子正确地定义了p3 和p4 两个指针。</span></p>
<h2>晦涩的语法</span></h2>
<h4>16.C语言同意一些令人震惊的结构,下面的结构是合法的吗，如果是它做些什么？</span></h4>
<pre class="language-c"><code>int a = 5, b = 7, c;
c = a+++b;</code></pre>
这个问题将做为这个测验的一个愉快的结尾。不管你相不相信，上面的例子是完全合乎语法的。问题是编译器如何处理它？水平不高的编译作者实际上会争论这个问题，根据最处理原则，编译器应当能处理尽可能所有合法的用法。因此，上面的代码被处理成：</span></p>
<pre class="language-c"><code>c = a++ + b;</code></pre>
因此, 这段代码持行后a = 6, b = 7, c = 12。</span></p>
如果你知道答案，或猜出正确答案，做得好。如果你不知道答案，我也不把这个当作问题。我发现这个问题的最大好处是:这是一个关于代码编写风格，代码的可读性，代码的可修改</span></p>
<p>&nbsp;</p>]]></content>
      <tags>
        <tag>嵌入式</tag>
      </tags>
  </entry>
</search>
